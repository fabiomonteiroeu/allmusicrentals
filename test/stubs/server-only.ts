/**
 * Stub de `server-only` para o Jest.
 * O pacote real lança ao ser importado fora do ambiente de servidor do Next;
 * nos testes de unidade da camada CMS só precisamos que o import seja inócuo.
 */
export {};
