# dependências
/node_modules
/.pnp
.pnp.*

# build / cache
/.next
/out
/build
/coverage
*.tsbuildinfo
next-env.d.ts
/.swc
/.turbo
/.vercel

# testes
/playwright-report
/test-results
/blob-report
/.playwright
/playwright/.cache
/e2e/.auth

# ambiente
.env
.env.local
.env.*.local
!.env.example

# sistema
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editores e ferramentas de agente
.idea
.vscode/*
!.vscode/extensions.json
# Preferências locais de permissão do Claude Code — por máquina, nunca compartilhadas.
# Hoje só o gitignore global do dev protege isto; aqui vale para qualquer clone.
/.claude/settings.local.json

# strapi (quando adicionado em cms/)
/cms/.tmp
/cms/build
/cms/.cache
/cms/.strapi
/cms/data
/cms/public/uploads

# screenshots de concorrentes (referência, fora do versionamento)
projeto-base/uploads/FireShot*
