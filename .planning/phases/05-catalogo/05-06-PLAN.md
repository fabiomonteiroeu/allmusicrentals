---
phase: 05-catalogo
plan: 06
type: execute
wave: 5
depends_on: ['05-05']
files_modified:
  - src/components/catalogo/DrawerDeFiltros.tsx
  - src/components/catalogo/DrawerDeFiltros.test.tsx
  - src/components/catalogo/ChipsDeFiltroAtivo.tsx
  - src/components/catalogo/ChipsDeFiltroAtivo.test.tsx
  - src/app/[locale]/catalogo/page.tsx
autonomous: true
requirements: [CATA-03, CATA-05]
must_haves:
  truths:
    - "No mobile o visitante abre o drawer de filtros, aplica, fecha e vê os chips ativos"
    - "Cada chip remove exatamente o seu filtro e nenhum outro"
    - "LIMPAR TUDO remove todos os filtros e preserva o termo buscado"
    - "Esc fecha o drawer e o foco volta para o botão FILTROS que o abriu"
    - "Com o drawer aberto, o Tab não escapa para o conteúdo de trás"
  artifacts:
    - path: "src/components/catalogo/DrawerDeFiltros.tsx"
      provides: "Drawer mobile com foco preso, Esc e retorno de foco, via Radix Dialog"
      contains: "@radix-ui/react-dialog"
      min_lines: 90
    - path: "src/components/catalogo/ChipsDeFiltroAtivo.tsx"
      provides: "Chips de filtro ativo derivados da URL, com remoção individual e LIMPAR TUDO"
      contains: "ChipFiltro"
  key_links:
    - from: "src/components/catalogo/ChipsDeFiltroAtivo.tsx"
      to: "src/lib/catalogo/filtros.ts"
      via: "descreverChips deriva os chips da mesma URL que alimenta o painel"
      pattern: "descreverChips"
    - from: "src/components/catalogo/DrawerDeFiltros.tsx"
      to: "src/store/slices/uiSlice.ts"
      via: "drawerFiltrosAberto controla a abertura, comandada pela toolbar"
      pattern: "drawerFiltrosAberto"
---

<objective>
Entregar o acesso mobile aos filtros e o feedback do que está aplicado: o drawer e os chips de filtro
ativo. É o plano que fecha o critério de sucesso 2 da fase ("no mobile o visitante abre o drawer,
aplica, fecha e vê os chips ativos, removendo cada um individualmente") e a metade de acessibilidade
do critério 3.

**Aviso de precedente, verificado no código:** RESEARCH §3 e UI-SPEC Bloco 6 afirmam que
`src/components/chrome/MobileMenu.tsx` é um precedente de drawer com `@radix-ui/react-dialog`, foco
preso, `Esc` e retorno de foco. **Isso não é verdade.** `grep -rln "from '@radix-ui" src` não retorna
nada: `@radix-ui/react-dialog` está instalado desde a Fase 1 e nunca foi importado. `MobileMenu.tsx`
é `display: none/block` por media query, comandado por um booleano do Redux — sem gestão de foco
nenhuma. Este plano é o **primeiro consumidor real** do pacote, e por isso os critérios de foco preso,
`Esc` e retorno de foco abaixo são verificáveis e explícitos, em vez de "seguir o padrão existente".

Decisões honradas: **D-06** (`'use client'` em tudo que declara `styled`), **D-07** (o drawer aparece
por `media.mobile`, o breakpoint único de 1080px, sem breakpoint novo), **D-09** (o drawer reusa o
mesmo `PainelDeFiltros`, então a semântica AND/OR é idêntica nos dois contextos por construção).

Purpose: sem o drawer, abaixo de 1080px não há como filtrar; sem os chips, o visitante não sabe o
que está aplicado nem consegue desfazer item a item.
Output: drawer e chips montados na rota, ambos lendo e escrevendo a mesma URL que o painel do aside.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catalogo/05-CONTEXT.md
@.planning/phases/05-catalogo/05-UI-SPEC.md
@.planning/phases/05-catalogo/05-PATTERNS.md
@.planning/phases/05-catalogo/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `ChipsDeFiltroAtivo` — chips derivados da URL, com remoção individual e LIMPAR TUDO</name>
  <files>src/components/catalogo/ChipsDeFiltroAtivo.tsx, src/components/catalogo/ChipsDeFiltroAtivo.test.tsx</files>
  <read_first>
    - `src/components/primitives/Chip.tsx` (o componente `ChipFiltro` já existe pronto, com `rotulo`, `valor`, `onRemover` e `aria-label={`Remover filtro ${rotulo}: ${valor}`}` — **não recriar**, só compor)
    - `src/lib/catalogo/filtros.ts` (criado em 05-02 — `descreverChips` devolve `{ grupo, rotuloDoGrupo, valor, rotuloDoValor }` por valor ativo, e `alternarValor` remove um valor específico)
    - `projeto-base/All Music Rentals - Catalogo.dc.html` linhas 175-181 (a barra real: o rótulo `FILTROS ATIVOS`, os chips e o botão de texto sublinhado `LIMPAR TUDO`, com borda inferior separando da grade)
    - `.planning/phases/05-catalogo/05-UI-SPEC.md` (Bloco 5 — para o filtro de cor o chip mostra **o nome** da cor; o estado dos chips deve espelhar a URL)
    - `.planning/phases/05-catalogo/05-RESEARCH.md` (§6 armadilha 2 — "chips dessincronizados da URL: o chip some mas o filtro continua na query, ou vice-versa; só aparece ao usar de verdade, voltando no histórico ou recarregando")
    - `src/components/catalogo/PainelDeFiltros.tsx` (criado em 05-05 — o mesmo padrão de "sem estado local, tudo vem de `useSearchParams`")
  </read_first>
  <action>
Criar `src/components/catalogo/ChipsDeFiltroAtivo.tsx` com `'use client'`. Compõe o `ChipFiltro`
existente; não criar componente de chip novo.

Regra estrutural que fecha a armadilha 2 do RESEARCH §6: o componente **não recebe a lista de chips
por prop e não guarda estado**. Ele lê `useSearchParams()`, roda `parseFiltrosDaUrl` com as
allowlists recebidas por prop e chama `descreverChips` — a mesma função e a mesma fonte que o painel
usa. Um chip só pode existir se o valor estiver na URL, e só pode sumir se o valor sair da URL. Não
há caminho de código onde os dois divirjam.

Estrutura, com a cópia literal do layout:
- a barra só é renderizada quando há ao menos um filtro ativo (o termo de busca `q` **não** conta
  como filtro e não vira chip — ele tem campo próprio no hero, e transformá-lo em chip criaria dois
  controles para o mesmo dado)
- um `<span>` com `FILTROS ATIVOS` como rótulo da barra
- um `ChipFiltro` por valor ativo, com `rotulo` = rótulo do grupo e `valor` = `rotuloDoValor`. Para o
  grupo `cor`, `rotuloDoValor` é o **nome** da cor (`Bege`, `Preto`, `Branco`), nunca o hex
- `onRemover` chama `alternarValor(params, chave, valor)` e faz `router.push` dentro de
  `useTransition`
- um `<button type="button">` com o texto `LIMPAR TUDO`, estilo de link sublinhado, alvo de toque
  ≥32px de altura como no layout. Ele remove **todas** as chaves de filtro e **preserva** `q` e
  `ordenar` — limpar filtro não é o mesmo que apagar a busca, e o layout mantém a busca no campo
- borda inferior separando a barra da grade, `padding-bottom: 20px; margin-bottom: 20px`
- envolver a barra num contêiner com `aria-live="polite"` para que a remoção de um chip seja
  anunciada

Testes (mockando `next/navigation` com `useSearchParams` retornando URLs reais):
- sem filtro na URL, nada é renderizado
- com `?cor=Bege&tipo=pacote`, dois chips aparecem, com os rótulos de grupo corretos
- clicar no X de um chip chama `router.push` **sem** aquele valor e **com** o outro preservado
- com `?cor=Bege&cor=Preto`, dois chips aparecem e remover um preserva o outro
- com `?q=mesa&cor=Bege`, existe um chip só (o de cor) e `LIMPAR TUDO` chama `router.push` mantendo
  `q=mesa`
- `jest-axe` sem violação séria
  </action>
  <verify>
    <automated>npx jest src/components/catalogo/ChipsDeFiltroAtivo.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from '@/components/primitives/Chip'" src/components/catalogo/ChipsDeFiltroAtivo.tsx` sai 0
    - `grep -c 'useState' src/components/catalogo/ChipsDeFiltroAtivo.tsx` retorna 0 — nenhum estado local
    - `grep -q 'descreverChips' src/components/catalogo/ChipsDeFiltroAtivo.tsx` sai 0
    - `grep -q 'FILTROS ATIVOS' src/components/catalogo/ChipsDeFiltroAtivo.tsx` e `grep -q 'LIMPAR TUDO' src/components/catalogo/ChipsDeFiltroAtivo.tsx` saem 0
    - `grep -Eq '#[0-9A-Fa-f]{6}' src/components/catalogo/ChipsDeFiltroAtivo.tsx` sai 1 — o chip mostra nome de cor, não hex
    - Existe teste que assere que remover um chip de `?cor=Bege&cor=Preto` preserva o outro valor no push
    - Existe teste que assere que `LIMPAR TUDO` preserva `q=mesa`
    - `npx jest src/components/catalogo/ChipsDeFiltroAtivo.test.tsx` sai 0
  </acceptance_criteria>
  <done>Os chips não podem divergir da URL porque derivam dela pela mesma função que o painel usa, e cada um remove só o seu valor.</done>
</task>

<task type="auto">
  <name>Task 2: `DrawerDeFiltros` — Radix Dialog com foco preso, Esc e retorno de foco</name>
  <files>src/components/catalogo/DrawerDeFiltros.tsx, src/components/catalogo/DrawerDeFiltros.test.tsx</files>
  <read_first>
    - `node_modules/@radix-ui/react-dialog/dist/index.d.ts` (a API real: `Root` com `open`/`onOpenChange`, `Portal`, `Overlay`, `Content`, `Title`, `Description`, `Close`. O pacote **nunca foi importado em `src/`** — não há analog interno de mecanismo)
    - `src/components/chrome/MobileMenu.tsx` (analog **apenas de estilo e de padrão de estado**: cores, espaçamento, e a convenção de que estado de UI efêmero mora no `uiSlice`. NÃO copiar o mecanismo — ele não tem foco preso, `Esc` nem retorno de foco)
    - `src/store/slices/uiSlice.ts` (`drawerFiltrosAberto` e `definirDrawerFiltros`, já existentes e ainda sem consumidor)
    - `src/components/catalogo/PainelDeFiltros.tsx` (criado em 05-05 — o drawer renderiza **o mesmo** componente, com `idPrefixo="drawer"`)
    - `src/components/catalogo/ToolbarDoCatalogo.tsx` (criado em 05-05 — o botão `FILTROS` que despacha a abertura; é para ele que o foco precisa voltar)
    - `projeto-base/All Music Rentals - Catalogo.dc.html` linhas 346-362 (o drawer real: overlay `rgba(11,12,13,0.6)`, painel ancorado embaixo com `max-height: 88vh`, cabeçalho com `<h2>Filtros</h2>` e botão de fechar `aria-label="Fechar"`, rodapé com `LIMPAR` e o CTA `VER N PRODUTOS`) e linha 23 (`@keyframes amrDrawer` — a animação `translateY(100%)` → `translateY(0)`)
    - `src/lib/theme/media.ts` (`media.mobile` — o drawer só existe abaixo de 1080px)
  </read_first>
  <action>
Criar `src/components/catalogo/DrawerDeFiltros.tsx` com `'use client'`, usando
`@radix-ui/react-dialog` em modo controlado.

Abertura: `<Dialog.Root open={drawerFiltrosAberto} onOpenChange={(v) => dispatch(definirDrawerFiltros(v))}>`,
lendo `drawerFiltrosAberto` de `useAppSelector((s) => s.ui.drawerFiltrosAberto)`. O disparo vem da
toolbar (05-05); este componente **não** renderiza `Dialog.Trigger`, para que o botão possa viver na
toolbar e ainda assim receber o foco de volta — o Radix devolve o foco ao elemento que estava ativo
antes da abertura, que é exatamente o botão `FILTROS`.

Estrutura: `Dialog.Portal` > `Dialog.Overlay` (fundo `rgba(11,12,13,0.6)`) + `Dialog.Content`
ancorado na base (`position: fixed; inset-inline: 0; bottom: 0; max-height: 88vh; display: flex;
flex-direction: column`).

Dentro do `Content`:
- cabeçalho fixo com `Dialog.Title` renderizado como `h2` com o texto `Filtros` e um `Dialog.Close`
  de 44×44px com `aria-label="Fechar"` e o glifo `×`. `Dialog.Title` é obrigatório — sem ele o Radix
  emite aviso de acessibilidade e o diálogo fica sem nome acessível. Se o título visível já servir
  como nome, não acrescentar `aria-label` redundante no `Content`
- corpo rolável (`flex: 1; overflow-y: auto`) com o **mesmo** `PainelDeFiltros` de 05-05, passando
  `idPrefixo="drawer"` para não colidir `id` de checkbox com o painel do aside, que pode estar
  montado na mesma árvore
- rodapé fixo com dois botões: `LIMPAR` (remove todos os filtros preservando `q` e `ordenar`, mesma
  regra do `LIMPAR TUDO` da Task 1) e o CTA de aplicar, cujo rótulo é `VER 1 PRODUTO` quando o total
  for 1 e `VER ${total} PRODUTOS` caso contrário — cópia literal do layout. "Aplicar" **fecha o
  drawer**: como o painel já escreve na URL a cada alternância, o botão não precisa acumular estado,
  só despachar `definirDrawerFiltros(false)`

Comportamento de acessibilidade — estabelecido aqui pela primeira vez no projeto, e por isso cada
item é critério de aceitação, não suposição:
- foco preso dentro do `Content` (o Radix faz por padrão; **não** desativar com `onOpenAutoFocus`
  nem `trapped={false}`)
- `Esc` fecha (padrão do Radix; não interceptar `onEscapeKeyDown` com `preventDefault`)
- ao fechar, o foco volta para o botão `FILTROS` (padrão do Radix; não sobrescrever
  `onCloseAutoFocus`)
- scroll do fundo bloqueado enquanto aberto (padrão do Radix via `Dialog.Overlay`; não desativar
  `modal`)
- a animação de entrada replica `@keyframes amrDrawer` (`translateY(100%)` → `translateY(0)`,
  `.22s ease-out`) e é anulada dentro de `@media (prefers-reduced-motion: reduce)` — esta é a única
  media query literal permitida aqui, porque é uma consulta de preferência do usuário e não um
  breakpoint de largura (D-07 restringe breakpoint, não `prefers-reduced-motion`)
- o drawer inteiro só renderiza abaixo de 1080px: envolver em contêiner com `display: none` dentro
  de `media.desktop` (per D7)

Testes em `DrawerDeFiltros.test.tsx`, com store de teste e `next/navigation` mockados:
- com `drawerFiltrosAberto: false` o conteúdo não está no documento
- com `true`, o diálogo aparece com `role="dialog"` e nome acessível `Filtros`
- `userEvent.keyboard('{Escape}')` despacha `definirDrawerFiltros(false)`
- clicar no CTA de aplicar despacha `definirDrawerFiltros(false)`
- o CTA mostra `VER 1 PRODUTO` para total 1 e `VER 10 PRODUTOS` para total 10
- `LIMPAR` chama `router.push` sem nenhuma chave de filtro e com `q` preservado
- `jest-axe` sem violação séria

Registrar no SUMMARY que **foco preso e retorno de foco não são verificáveis com fidelidade em
jsdom** (RESEARCH §6 armadilha 3): o teste unitário cobre montagem, `Esc` e cópia; a prova real de
foco fica no e2e Playwright de 05-08, que é onde `document.activeElement` se comporta como num
navegador.
  </action>
  <verify>
    <automated>npx jest src/components/catalogo/DrawerDeFiltros.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@radix-ui/react-dialog" src/components/catalogo/DrawerDeFiltros.tsx` sai 0
    - `grep -q 'Dialog.Title' src/components/catalogo/DrawerDeFiltros.tsx` sai 0 — o diálogo tem nome acessível
    - `grep -Eq 'onEscapeKeyDown|onCloseAutoFocus|onOpenAutoFocus|trapped' src/components/catalogo/DrawerDeFiltros.tsx` sai 1 — nenhum comportamento padrão de foco do Radix foi sobrescrito
    - `grep -q 'idPrefixo="drawer"' src/components/catalogo/DrawerDeFiltros.tsx` sai 0
    - `grep -q 'prefers-reduced-motion' src/components/catalogo/DrawerDeFiltros.tsx` sai 0
    - `grep -c '@media' src/components/catalogo/DrawerDeFiltros.tsx` retorna 1, e a única ocorrência é `prefers-reduced-motion` (a troca de largura usa `media.desktop`)
    - `grep -q 'VER 1 PRODUTO' src/components/catalogo/DrawerDeFiltros.tsx` sai 0
    - Existe teste com `userEvent.keyboard('{Escape}')` que assere o despacho de fechamento
    - `npx jest src/components/catalogo/DrawerDeFiltros.test.tsx` sai 0
  </acceptance_criteria>
  <done>O drawer abre pelo botão da toolbar, prende o foco, fecha com Esc e devolve o foco — usando o mecanismo do Radix, não uma reimplementação.</done>
</task>

<task type="auto">
  <name>Task 3: Montar drawer e chips na rota</name>
  <files>src/app/[locale]/catalogo/page.tsx</files>
  <read_first>
    - `src/app/[locale]/catalogo/page.tsx` (o arquivo a modificar — o estado após 05-05, com `HeroCatalogo`, `LayoutCatalogo`, `PainelDeFiltros` no aside e `ToolbarDoCatalogo` no main)
    - `src/components/catalogo/LayoutCatalogo.tsx` (as props `aside` e `children`)
    - `src/components/catalogo/DrawerDeFiltros.tsx` e `src/components/catalogo/ChipsDeFiltroAtivo.tsx` (criados nas tarefas anteriores — as props exatas que cada um espera)
    - `projeto-base/All Music Rentals - Catalogo.dc.html` linhas 155-181 (a ordem real dentro do `<main>`: toolbar, depois a barra de chips com borda inferior, depois a grade)
  </read_first>
  <action>
No `<main>` de `src/app/[locale]/catalogo/page.tsx`, inserir `ChipsDeFiltroAtivo` imediatamente
**depois** da `ToolbarDoCatalogo` e antes do ponto onde a grade entrará em 05-07 — é a ordem do
layout. Passar as mesmas allowlists (`categorias`, `tiposDeEvento` visíveis, `cores`) que já foram
montadas para o `PainelDeFiltros`, para que chips e painel resolvam rótulo pela mesma fonte.

Montar `DrawerDeFiltros` uma única vez, no fim da árvore da página, recebendo as mesmas opções de
grupo e o `total`. Uma instância só: o `Dialog.Portal` do Radix já move o conteúdo para fora da
hierarquia visual, então não há motivo para renderizá-lo dentro do `LayoutCatalogo`, e duas
instâncias criariam dois diálogos disputando o mesmo booleano do Redux.

Confirmar que o `PainelDeFiltros` do aside continua com `idPrefixo="aside"` e o do drawer com
`idPrefixo="drawer"` — com os dois montados na mesma página, `id` duplicado de checkbox quebra a
associação `label`/`input` e o axe reporta violação.

Não acrescentar consulta nova ao CMS: `total` e as opções já estão em escopo desde 05-04/05-05.
  </action>
  <verify>
    <automated>npx jest src/app/[locale]/catalogo src/components/catalogo && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DrawerDeFiltros' 'src/app/[locale]/catalogo/page.tsx'` mostra uma única montagem do componente
    - `grep -q 'ChipsDeFiltroAtivo' 'src/app/[locale]/catalogo/page.tsx'` sai 0
    - `grep -c 'idPrefixo' 'src/app/[locale]/catalogo/page.tsx'` retorna 1 (o do aside; o do drawer está dentro do próprio `DrawerDeFiltros`)
    - `grep -c 'getProdutos' 'src/app/[locale]/catalogo/page.tsx'` continua retornando 1 — nenhuma consulta nova
    - `npm run build` sai 0
    - Com o Strapi de pé, `/pt-BR/catalogo?cor=Bege` renderiza a barra `FILTROS ATIVOS` com um chip `Cor: Bege`
  </acceptance_criteria>
  <done>Drawer e chips estão na página, com uma instância de cada e sem consulta extra ao CMS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interação no drawer/chips → `searchParams` | Toda ação de filtro e remoção reescreve a URL, que volta como entrada não confiável no servidor |
| `uiSlice` (estado global) → visibilidade do diálogo | Booleano compartilhado entre toolbar e drawer |
| conteúdo por trás do overlay → foco do teclado | Com o diálogo aberto, conteúdo de fundo não deve ser alcançável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering | Estado de filtro duplicado entre chips e painel | mitigate | Nenhum dos dois guarda estado: ambos derivam de `useSearchParams` pela mesma `parseFiltrosDaUrl`/`descreverChips`. Critério com `grep -c 'useState'` retornando 0 nos dois arquivos |
| T-05-25 | Elevation of Privilege | Foco escapando do diálogo para o conteúdo de fundo | mitigate | Foco preso do `@radix-ui/react-dialog` em modo modal, com proibição explícita de sobrescrever `onEscapeKeyDown`, `onCloseAutoFocus`, `onOpenAutoFocus` e `trapped`; verificado no e2e de 05-08 com `document.activeElement` |
| T-05-26 | Denial of Service | Diálogo que não fecha, prendendo o visitante | mitigate | Três saídas independentes: `Esc`, botão de fechar do cabeçalho e clique no overlay — todas padrão do Radix e nenhuma desativada |
| T-05-27 | Tampering | `id` duplicado de checkbox com painel montado duas vezes | mitigate | `idPrefixo` distinto (`aside`/`drawer`) e verificação por axe no teste e no e2e |
| T-05-28 | Information Disclosure | Rótulo de filtro renderizado no chip | mitigate | `ChipFiltro` renderiza texto por React (escapa por padrão); o valor exibido vem da allowlist resolvida pelo servidor, nunca da string crua da URL |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo. `@radix-ui/react-dialog@^1.1.23` já está em `package.json`/`package-lock.json` desde a Fase 1; este plano apenas o importa pela primeira vez. Instalar qualquer biblioteca de drawer/modal é proibido — parar e escalar |
</threat_model>

<verification>
1. `npm run typecheck`, `npm run lint` e `npm run build` saem 0
2. `npx jest src/components/catalogo src/app` sai 0
3. `grep -rc 'useState' src/components/catalogo/ChipsDeFiltroAtivo.tsx src/components/catalogo/PainelDeFiltros.tsx` retorna 0 nos dois
4. Em 375px, com o Strapi de pé: abrir `FILTROS`, marcar um valor, tocar em `VER N PRODUTOS`, ver o chip aparecer, remover o chip e ver o resultado voltar
5. Voltar no histórico após remover um chip restaura o filtro e o chip juntos
6. `grep -rn '@media' src/components/catalogo/` só mostra `prefers-reduced-motion`

</verification>

<success_criteria>
- O drawer abre pelo botão `FILTROS`, prende o foco, fecha com `Esc` e devolve o foco ao botão
- O CTA do drawer mostra `VER 1 PRODUTO` / `VER N PRODUTOS` com a contagem real
- Cada chip remove exatamente o seu filtro; `LIMPAR TUDO` remove todos e preserva a busca
- Chips e painel nunca divergem, porque nenhum dos dois guarda estado
- Nenhum breakpoint novo foi criado
</success_criteria>

<output>
Create `.planning/phases/05-catalogo/05-06-SUMMARY.md` when done
</output>
