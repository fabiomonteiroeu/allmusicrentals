---
phase: 05-catalogo
plan: 08
type: execute
wave: 7
depends_on: ['05-07']
files_modified:
  - e2e/catalogo-filtros.spec.ts
  - e2e/catalogo-acessibilidade.spec.ts
  - e2e/utils/axe.ts
autonomous: false
requirements: [CATA-01, CATA-02, CATA-03, CATA-04, CATA-05, CATA-06]
must_haves:
  truths:
    - "O visitante opera busca, filtros, drawer e ordenação só pelo teclado, com foco sempre visível"
    - "Esc fecha o drawer e o foco volta ao botão FILTROS, verificado em navegador real"
    - "Chips e URL nunca divergem, inclusive ao recarregar e ao voltar no histórico"
    - "axe não reporta violação séria ou crítica em /pt-BR/catalogo, em desktop e em 375px"
    - "search, filter_applied e view_item_list chegam ao dataLayer com os parâmetros corretos e sem campo de valor"
    - "A tela em 375px não tem scroll horizontal"
  artifacts:
    - path: "e2e/catalogo-filtros.spec.ts"
      provides: "Cobertura e2e do fluxo de filtro, drawer, chips e eventos"
      min_lines: 100
    - path: "e2e/catalogo-acessibilidade.spec.ts"
      provides: "axe em navegador real, navegação por teclado e ausência de scroll horizontal"
    - path: "e2e/utils/axe.ts"
      provides: "Injeção de axe-core na página sem dependência nova"
      contains: "axe-core"
  key_links:
    - from: "e2e/catalogo-filtros.spec.ts"
      to: "window.dataLayer"
      via: "asserção sobre a fila de eventos real do navegador"
      pattern: "dataLayer"
    - from: "e2e/catalogo-filtros.spec.ts"
      to: "document.activeElement"
      via: "prova de retorno de foco que jsdom não reproduz"
      pattern: "activeElement|toBeFocused"
---

<objective>
Provar em navegador real o que jsdom não consegue provar, e conferir a fidelidade visual lado a lado
com o layout-fonte.

A Fase 4 fechou com três defeitos que passaram por 163 testes verdes e só apareceram no navegador
(contraste 1.00, chave React duplicada, mídia com URL relativa). Os equivalentes prováveis aqui,
listados em `05-RESEARCH.md` §6, são: contagem divergente da grade, chips dessincronizados da URL,
foco perdido ao fechar o drawer e acento/caixa na busca. Nenhum deles é pego por teste unitário —
os dois primeiros porque a fixture bate e a produção não, o terceiro porque jsdom não reproduz
gerenciamento de foco com fidelidade, o quarto porque exige dado real.

Esta é a **primeira suíte Playwright do projeto além do smoke da Fase 1**. `playwright.config.ts` e
`e2e/smoke.spec.ts` já existem, com dois projetos (`desktop-chromium` e `mobile-chromium`) e
`webServer` rodando `npm run build && npm run start`.

Decisões honradas: **D-04** (a suíte assere ausência de qualquer preço na tela), **D-05** (os eventos
são conferidos na fila real do `window.dataLayer`), **D-07** (a troca aside↔drawer é conferida nos
dois lados do breakpoint único de 1080px), **D-09** (AND/OR conferido com os dados reais dos 10
produtos, não com fixture).

Purpose: fechar os critérios de sucesso 2, 3 e 5 da fase com evidência de navegador, e o 4 com
conferência humana lado a lado.
Output: duas suítes e2e verdes e um checkpoint de fidelidade aprovado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catalogo/05-CONTEXT.md
@.planning/phases/05-catalogo/05-UI-SPEC.md
@.planning/phases/05-catalogo/05-RESEARCH.md
@.planning/phases/05-catalogo/05-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suíte e2e do fluxo de filtro, drawer, chips e eventos</name>
  <files>e2e/catalogo-filtros.spec.ts</files>
  <read_first>
    - `e2e/smoke.spec.ts` (o único spec existente — o estilo: `import { test, expect } from '@playwright/test'`, `page.goto` com caminho relativo ao `baseURL`, seletores por papel com `getByRole`)
    - `playwright.config.ts` (os dois projetos `desktop-chromium` e `mobile-chromium`, o `webServer` com `npm run build && npm run start` e `reuseExistingServer` fora do CI, `baseURL` em `http://localhost:3000`)
    - `.planning/phases/05-catalogo/05-CONTEXT.md` (`<decisions>` 3 — a tabela dos 10 produtos com slug, categoria, `tipoDeItem` e cores; é a **fonte da verdade das contagens esperadas**)
    - `.planning/phases/05-catalogo/05-RESEARCH.md` (§1 — as combinações já executadas contra o Strapi e seus resultados exatos; §6 armadilha "a busca por 'mesa' traz 5 produtos, não 2, porque a palavra aparece no nome deles — contar sempre pelo dado real")
    - `src/components/catalogo/ToolbarDoCatalogo.tsx`, `src/components/catalogo/ChipsDeFiltroAtivo.tsx`, `src/components/catalogo/DrawerDeFiltros.tsx` (os rótulos e `aria-label` reais que os seletores vão usar)
    - `src/lib/analytics/dataLayer.ts` (o formato exato dos três eventos na fila)
  </read_first>
  <action>
Criar `e2e/catalogo-filtros.spec.ts`.

**Pré-requisito de ambiente, a documentar no topo do arquivo:** a suíte precisa do Strapi de pé com
os 10 produtos publicados e a taxonomia migrada (`docker compose --profile cms up -d cms`). Sem isso
a rota renderiza a tela de erro e todos os testes falham por motivo errado. Acrescentar um
`test.beforeAll` que faz `page.goto('/pt-BR/catalogo')` e falha com mensagem explícita se a contagem
inicial não for 10 — um diagnóstico claro vale mais que dez falhas confusas.

Usar viewport explícito por bloco, e não confiar no projeto do Playwright, para que cada teste seja
determinístico dos dois lados do breakpoint de 1080px:
`test.describe('desktop', () => { test.use({ viewport: { width: 1280, height: 900 } }); ... })` e
`test.describe('mobile', () => { test.use({ viewport: { width: 390, height: 844 } }); ... })`.

**Bloco desktop — filtro, contagem, ordenação:**
- `/pt-BR/catalogo` mostra `10 PRODUTOS` na toolbar e 10 cards na grade; asserir os **dois** números
  e que são iguais — é a armadilha 1 do RESEARCH §6, e só uma asserção cruzada a pega
- marcar `Pacote` e `Serviço técnico` no grupo "Tipo de item" leva a 2 resultados
  (`led-pacote`, `operacao-led`) — prova o OR dentro do grupo
- marcar `Móveis` em Categoria e `Bege` + `Preto` em Cor leva a 4 resultados (`capa-6`,
  `mesa-bistro`, `capa-spandex`, `lounge`) — prova o AND entre grupos combinado com OR dentro,
  exatamente a combinação já executada contra o Strapi no RESEARCH §1
- buscar `mesa` retorna **5** produtos, não 2 — o nome de `capa-6`, `capa-spandex` e `lounge` contém
  "Mesa". Deixar isso comentado no teste, porque parece bug a olho nu e um futuro leitor vai
  "corrigir" a expectativa
- buscar `painéis` e `paineis` — asserir o comportamento **observado** e registrá-lo no SUMMARY:
  `$containsi` é indiferente a caixa mas não a acento. Se as duas grafias divergirem, isso é um
  achado a registrar, não a mascarar com um teste frouxo
- trocar a ordenação para `Nome de Z a A` muda a ordem do primeiro card, e a URL passa a conter
  `ordenar=nome-desc`
- submeter a busca vazia mostra a mensagem de erro inline e **não** muda a URL

**Bloco desktop — chips e URL (armadilha 2 do RESEARCH §6):**
- aplicar dois filtros → dois chips; a URL contém os dois parâmetros
- remover um chip → some só aquele chip, o outro fica, e a URL perde só aquele parâmetro
- `page.reload()` → os chips restantes reaparecem idênticos (o estado sobreviveu porque mora na URL)
- `page.goBack()` → o chip removido volta **junto** com o parâmetro na URL. Este é o teste que pega
  a dessincronização; asserir chip e URL na mesma verificação
- `LIMPAR TUDO` remove todos os chips e preserva `q` na URL

**Bloco mobile — drawer:**
- em 390px, o aside de filtros não está visível e o botão `FILTROS` está
- clicar em `FILTROS` abre o diálogo (`getByRole('dialog', { name: 'Filtros' })`)
- com o diálogo aberto, `page.keyboard.press('Tab')` repetidas vezes nunca leva o foco para fora do
  diálogo — asserir com `dialog.evaluate((el) => el.contains(document.activeElement))`
- `Escape` fecha o diálogo **e** o foco volta ao botão `FILTROS`:
  `await expect(page.getByRole('button', { name: /Filtros/ })).toBeFocused()`. Esta é a asserção que
  jsdom não consegue fazer com fidelidade e por isso ela vive aqui
- marcar um filtro dentro do drawer e clicar em `VER N PRODUTOS` fecha o drawer, e o chip
  correspondente aparece na tela

**Bloco de eventos (CATA-06):**
- instalar `window.dataLayer = []` antes da navegação com
  `page.addInitScript(() => { (window as any).dataLayer = []; })`, para capturar desde a primeira
  emissão
- `/pt-BR/catalogo?q=mesa` produz um evento `search` com `search_term: 'mesa'`
- marcar um filtro produz um `filter_applied` com o `filter_type` e o `filter_value` corretos
- a grade produz um `view_item_list` com `item_list_id: 'catalogo_resultados'`
- recarregar a página com filtro já na URL **não** produz `filter_applied` novo — a supressão da
  primeira montagem, decidida em 05-07
- **nenhum** evento da fila tem chave `value`, `price`, `currency` ou `revenue`: varrer a fila
  inteira e asserir. É a prova de comportamento de PRECO-04, complementar à garantia de compilação
  </action>
  <verify>
    <automated>npx playwright test e2e/catalogo-filtros.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/catalogo-filtros.spec.ts` sai 0 nos dois projetos
    - Existe teste que assere que o número da toolbar e a quantidade de cards são iguais
    - Existe teste que assere 2 resultados para `Pacote` + `Serviço técnico` e 4 para `Móveis` + `Bege`/`Preto`
    - Existe teste com `page.goBack()` que assere chip **e** URL na mesma verificação
    - Existe teste com `toBeFocused()` sobre o botão `FILTROS` após `Escape`
    - Existe teste que varre `window.dataLayer` e assere ausência de `value`/`price`/`currency`/`revenue` em todos os eventos
    - O comentário sobre "buscar mesa retorna 5" está no arquivo
  </acceptance_criteria>
  <done>Os três modos de falha que testes unitários não pegam — contagem cruzada, chip↔URL no histórico e retorno de foco — têm teste em navegador real.</done>
</task>

<task type="auto">
  <name>Task 2: axe em navegador real, teclado ponta a ponta e 375px sem scroll horizontal</name>
  <files>e2e/catalogo-acessibilidade.spec.ts, e2e/utils/axe.ts</files>
  <read_first>
    - `e2e/smoke.spec.ts` e `playwright.config.ts` (o estilo e a configuração da suíte)
    - `src/components/blocos/SearchBarGrande.test.tsx` (como o projeto já usa `jest-axe` no nível de componente — a cobertura e2e é complementar, não substituta)
    - `node_modules/axe-core/axe.min.js` (o pacote já está na árvore como dependência transitiva de `jest-axe`, versão 4.13.0 — é o que permite rodar axe no Playwright **sem instalar nada**)
    - `.planning/phases/05-catalogo/05-UI-SPEC.md` ("Regras estruturais herdadas": alvo ≥44px, foco visível, contraste AA, `prefers-reduced-motion`, 375px sem scroll horizontal; e o alerta de que a Fase 4 teve 4 títulos com contraste 1.00)
    - `src/components/catalogo/HeroCatalogo.tsx` (o bloco escuro desta fase — é onde o defeito de contraste da Fase 4 se repetiria)
  </read_first>
  <action>
Criar `e2e/utils/axe.ts` — um utilitário que injeta o axe na página e devolve o resultado, **sem
acrescentar dependência nova**: `page.addScriptTag({ path: require.resolve('axe-core/axe.min.js') })`
seguido de `page.evaluate(() => (window as any).axe.run(document, { resultTypes: ['violations'] }))`.
`require.resolve` localiza o arquivo independentemente de como o npm elevou a árvore, então isso não
depende de layout de `node_modules`.

**Não instalar `@axe-core/playwright` nem qualquer outro pacote.** Se o executor concluir que a
injeção não funciona e que um pacote é necessário, deve **parar e escalar** em vez de instalar: o
projeto não tem tabela de auditoria de legitimidade de pacotes em `05-RESEARCH.md`, e instalar sem
ela é justamente o que a política de supply chain proíbe.

Criar `e2e/catalogo-acessibilidade.spec.ts` com quatro blocos.

**axe.** Rodar em `/pt-BR/catalogo` em 1280px e em 375px, e também com o drawer **aberto** em 375px
(o diálogo é conteúdo novo na árvore e precisa ser auditado no estado aberto, não só fechado).
Falhar quando houver violação de impacto `serious` ou `critical`. Ao falhar, imprimir `id`,
`impact` e os seletores dos nós — uma falha de axe sem o seletor é inacionável.

**Contraste.** Além do axe geral, uma asserção dirigida ao hero escuro: o `h1` e o rótulo do card
`SOBRE OS VALORES` precisam ter cor computada distinta da cor de fundo computada do bloco. É a
verificação específica do defeito de contraste 1.00 da Fase 4, que aconteceu por omitir
`$sobreEscuro`; axe pega contraste baixo, mas uma asserção explícita torna a regressão legível.

**Teclado ponta a ponta (critério de sucesso 3 da fase).** Partindo de `/pt-BR/catalogo`, percorrer
só com `Tab`/`Enter`/`Space`/setas: alcançar o campo de busca e submeter; alcançar um cabeçalho de
grupo do acordeão e alternar com `Enter`; alcançar um checkbox e marcar com `Space`; alcançar o
`<select>` de ordenação e trocar de opção; em 375px, alcançar o botão `FILTROS`, abrir com `Enter`,
operar dentro do drawer e fechar com `Escape`. Em cada parada, asserir que o elemento focado tem
contorno de foco visível — comparar `outlineStyle`/`outlineWidth` computados no estado focado com o
estado não focado, para que "foco visível" seja medido e não presumido.

**375px sem scroll horizontal.** Com viewport de 375px, asserir
`document.documentElement.scrollWidth <= document.documentElement.clientWidth` em três estados:
sem filtro, com dois filtros aplicados (a barra de chips cresce) e com o drawer aberto.

Registrar no SUMMARY qualquer violação de impacto `moderate` ou `minor` que tenha sido deixada
passar, com a justificativa — silenciar sem registrar é o que transforma dívida de acessibilidade em
surpresa na Fase 16 (QA-03).
  </action>
  <verify>
    <automated>npx playwright test e2e/catalogo-acessibilidade.spec.ts && git diff --exit-code -- package.json package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/catalogo-acessibilidade.spec.ts` sai 0
    - `git diff --exit-code -- package.json package-lock.json` sai 0 — nenhuma dependência foi acrescentada
    - `grep -q "require.resolve('axe-core/axe.min.js')" e2e/utils/axe.ts` sai 0
    - Existe teste que roda axe com o drawer aberto, além dos estados de página fechada
    - Existe asserção que compara a cor computada do `h1` do hero com a cor de fundo computada do bloco
    - Existe asserção de `scrollWidth <= clientWidth` em 375px nos três estados
    - Existe asserção que compara `outline` computado entre focado e não focado
  </acceptance_criteria>
  <done>Acessibilidade da rota é medida em navegador real, incluindo o estado aberto do drawer, e o defeito de contraste da Fase 4 tem teste dirigido contra a reincidência.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferência de fidelidade lado a lado com o layout-fonte</name>
  <files>(nenhum arquivo modificado — conferência visual)</files>
  <read_first>
    - `projeto-base/All Music Rentals - Catalogo.dc.html` (o layout-fonte inteiro — abrir no navegador ao lado da aplicação)
    - `.planning/phases/05-catalogo/05-UI-SPEC.md` (os 8 blocos e a escala de `clamp()` extraída do layout)
    - `docs/divergencias.md` (D5, D6, D7, D8 e D9 — o que **deve** estar diferente do layout, e por quê; divergência registrada não é defeito. D8 é a origem da lista de cores do filtro, escrita por 05-01; D9 é a cópia do estado "catálogo vazio", escrita por 05-07)
    - `.planning/phases/04-home/04-VERIFICATION.md` (os 3 defeitos que o checkpoint da Fase 4 pegou e os 163 testes não: contraste, chave React duplicada e mídia com URL relativa — procurar os equivalentes)
  </read_first>
  <what-built>
    A rota `/[locale]/catalogo` completa: hero com o card SOBRE OS VALORES, busca com validação e
    estado `busy`, painel de filtros em acordeão com os 5 grupos, toolbar com contagem e ordenação de
    5 opções, chips de filtro ativo, drawer mobile, grade de produtos e os quatro estados. Duas
    suítes e2e verdes cobrem filtro, drawer, foco, chips, eventos e axe.
  </what-built>
  <how-to-verify>
    Com `docker compose --profile cms up -d cms` e `npm run dev` de pé, abrir
    `projeto-base/All Music Rentals - Catalogo.dc.html` e `http://localhost:3000/pt-BR/catalogo` lado
    a lado, em desktop largo e em 375px.

    1. **Hero e card.** O H1, o parágrafo e o card `SOBRE OS VALORES` batem em texto e hierarquia.
       Confirmar que o título e o rótulo do card são **legíveis** sobre o fundo escuro — foi este o
       defeito de contraste 1.00 da Fase 4.
    2. **Busca.** O rótulo `BUSCAR PRODUTOS NO CATÁLOGO` e o placeholder estão iguais. Submeter vazio
       mostra o erro inline. Submeter `mesa` mostra o estado `BUSCANDO` no botão antes do resultado.
    3. **Painel.** Os 5 grupos aparecem na ordem do layout, com Categoria, Tipo de item e Cor abertos
       e Tipo de evento e Ambiente fechados. As duas notas de microcopy estão presentes. O filtro
       Categoria mostra as **5 categorias reais** (`Estruturas`, `Telas de LED`, `Luz & Som`,
       `Tendas`, `Móveis`) e **não** os rótulos de subcategoria do layout — isso é D-02, é
       intencional, não é defeito.
    4. **Toolbar.** A contagem e as 5 opções de ordenação estão com os rótulos literais. Confirmar
       que **nenhuma** opção é por valor.
    5. **Chips.** Aplicar três filtros, remover um a um, usar `LIMPAR TUDO`. Recarregar com filtros na
       URL e voltar no histórico — os chips têm que acompanhar sempre.
    6. **Drawer em 375px.** Abrir, rolar, marcar, aplicar, fechar. Confirmar que o fundo não rola com
       o drawer aberto e que o CTA mostra a contagem correta.
    7. **Os quatro estados.** Grade cheia; `?q=zzzzzz` para o estado sem correspondência (conferir as
       três sugestões e o CTA); recarregar com rede lenta no DevTools para ver os skeletons; parar o
       container do CMS e recarregar para ver a tela de erro com `TENTAR NOVAMENTE`.
    8. **Regra sem preço.** Varrer a tela inteira, em todos os estados: nenhum símbolo monetário,
       nenhum "a partir de", nenhum valor.
    9. **Console do navegador.** Nenhum aviso de chave React duplicada e nenhum erro de hidratação —
       foram dois dos três defeitos da Fase 4 e nenhum aparece em teste.
    10. **Imagens.** Confirmar que as imagens dos cards carregam com URL absoluta do CMS, e não
        quebrada por caminho relativo — o terceiro defeito da Fase 4.
  </how-to-verify>
  <acceptance_criteria>
    - Cada um dos 10 pontos acima foi conferido e o resultado registrado (conforme / divergente)
    - Toda divergência encontrada que **não** esteja em `docs/divergencias.md` como D5, D6, D7, D8 ou D9 é tratada como defeito e corrigida antes da aprovação
    - O console do navegador não mostra aviso de chave duplicada nem erro de hidratação em nenhum dos quatro estados
    - Nenhuma tela, em nenhum estado, exibe valor monetário
    - O checkpoint só é aprovado depois de `npm run check` e `npx playwright test` saírem 0
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou liste, ponto a ponto, o que divergiu</resume-signal>
  <done>A fidelidade foi conferida por um humano contra o layout-fonte, com atenção dirigida aos três modos de falha que a Fase 4 provou que os testes não pegam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte e2e → aplicação em execução | Os testes exercitam a rota real com o CMS real, atravessando todas as fronteiras das fases anteriores de uma vez |
| `axe-core` injetado → página sob teste | Script de terceiro executado no contexto da página, em ambiente de teste |
| fila `window.dataLayer` → asserção de teste | Superfície onde vazamento de campo monetário seria observável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-35 | Tampering | Dependência nova entrando pela porta dos testes | mitigate | `@axe-core/playwright` **não** é instalado; o axe vem de `require.resolve('axe-core/axe.min.js')`, já presente na árvore como transitiva de `jest-axe`. Critério de aceitação: `git diff --exit-code -- package.json package-lock.json` sai 0. Se um pacote parecer necessário, o executor para e escala — não existe tabela de auditoria de legitimidade em `05-RESEARCH.md`, e instalar sem ela é proibido |
| T-05-36 | Information Disclosure | Campo monetário em evento (PRECO-04) | mitigate | Varredura da fila `window.dataLayer` inteira em navegador real, complementando a garantia de compilação da união tipada |
| T-05-37 | Elevation of Privilege | Foco escapando do drawer | mitigate | Asserção com `dialog.contains(document.activeElement)` após múltiplos `Tab`, e `toBeFocused()` no botão de origem após `Escape` |
| T-05-38 | Information Disclosure | Detalhe de erro visível na tela de falha do CMS | mitigate | O passo 7 do checkpoint derruba o container do CMS de propósito e confere que a tela mostra cópia genérica, sem `message`, `digest`, URL nem status |
| T-05-39 | Repudiation | Suíte verde por motivo errado (Strapi fora do ar) | mitigate | `test.beforeAll` falha com mensagem explícita quando a contagem inicial não é 10, em vez de deixar dez testes falharem por sintoma |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo em nenhuma das três tarefas; `@playwright/test` e `axe-core` já estão na árvore desde as Fases 1 e 2. `git diff` de `package.json`/`package-lock.json` é critério de aceitação explícito |
</threat_model>

<verification>
1. `npm run check` (typecheck + lint + jest) sai 0
2. `npx playwright test` sai 0 nos dois projetos
3. `npm run verifica:bundle-segredo` sai 0
4. `git diff --exit-code -- package.json package-lock.json` sai 0
5. O checkpoint de fidelidade foi aprovado, com os 10 pontos registrados
6. `grep -rn 'CATA-0' .planning/phases/05-catalogo/*-SUMMARY.md` mostra os 6 requisitos cobertos

</verification>

<success_criteria>
- O fluxo completo de filtro é exercitado em navegador real, com as contagens conferidas contra os 10 produtos reais
- Foco preso, `Esc` e retorno de foco do drawer são provados com `document.activeElement`
- Chips e URL são conferidos inclusive após `reload` e `goBack`
- axe não reporta violação séria ou crítica, inclusive com o drawer aberto
- 375px não tem scroll horizontal em nenhum dos três estados testados
- Nenhuma dependência nova entrou no projeto
- O checkpoint de fidelidade foi aprovado
</success_criteria>

<output>
Create `.planning/phases/05-catalogo/05-08-SUMMARY.md` when done
</output>
