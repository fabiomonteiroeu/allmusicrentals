---
phase: 05-catalogo
plan: 05
type: execute
wave: 4
depends_on: ['05-04']
files_modified:
  - src/components/catalogo/PainelDeFiltros.tsx
  - src/components/catalogo/PainelDeFiltros.test.tsx
  - src/components/catalogo/SwatchesDeCor.tsx
  - src/components/catalogo/SwatchesDeCor.test.tsx
  - src/components/catalogo/ToolbarDoCatalogo.tsx
  - src/components/catalogo/ToolbarDoCatalogo.test.tsx
  - src/app/[locale]/catalogo/page.tsx
autonomous: true
requirements: [CATA-02, CATA-03, CATA-05]
must_haves:
  truths:
    - "O visitante vê os 5 grupos de filtro em acordeão, com Categoria, Tipo de item e Cor abertos de saída"
    - "Marcar valores em grupos diferentes combina com AND; marcar dois valores no mesmo grupo combina com OR"
    - "O visitante escolhe entre as 5 opções de ordenação e o resultado muda"
    - "A contagem exibida é a mesma quantidade de itens que a grade recebe"
    - "Cabeçalho de grupo, checkbox, swatch e select são alcançáveis e operáveis por teclado, com foco visível"
    - "Os swatches exibidos são exatamente a lista de cores recebida por prop (derivada do CMS, per D8) — o componente não tem lista de cores própria"
  artifacts:
    - path: "src/components/catalogo/PainelDeFiltros.tsx"
      provides: "Acordeão múltiplo dos 5 grupos, ligado à URL"
      contains: "@radix-ui/react-accordion"
      min_lines: 120
    - path: "src/components/catalogo/SwatchesDeCor.tsx"
      provides: "Multi-seleção de cor por swatch sobre a lista recebida por prop, sem alterar o primitivo de seleção única do card"
      contains: "cores.map("
    - path: "src/components/catalogo/ToolbarDoCatalogo.tsx"
      provides: "Contagem, ordenação de 5 opções e o botão FILTROS do mobile"
      contains: "Ordenar por"
  key_links:
    - from: "src/components/catalogo/PainelDeFiltros.tsx"
      to: "src/lib/catalogo/filtros.ts"
      via: "alternarValor reescreve a URL, que é a única fonte de verdade do filtro"
      pattern: "alternarValor"
    - from: "src/components/catalogo/ToolbarDoCatalogo.tsx"
      to: "src/lib/catalogo/filtros.ts"
      via: "ORDENACOES_UI alimenta o select"
      pattern: "ORDENACOES_UI"
    - from: "src/app/[locale]/catalogo/page.tsx"
      to: "src/components/catalogo/SwatchesDeCor.tsx"
      via: "getCoresDisponiveis resolve no servidor e desce por prop até os swatches, per D8"
      pattern: "coresDisponiveis"
---

<objective>
Entregar o coração do catálogo: o painel de filtros em acordeão com os 5 grupos e a toolbar com
contagem e ordenação. Depois deste plano, **D-09** (AND entre grupos, OR dentro do grupo) deixa de
ser lógica testada em isolamento e vira comportamento observável na tela.

Este é o **primeiro consumidor real de `@radix-ui/react-accordion` no projeto**. O pacote está
instalado desde a Fase 1 e nunca foi importado em `src/`. Não existe acordeão anterior para copiar, e
o `MobileMenu.tsx` — citado como precedente em RESEARCH e UI-SPEC — **não** usa Radix nem gerencia
foco: é `display: none/block` por media query comandado por um booleano do Redux. Os critérios de
teclado abaixo, portanto, estabelecem o padrão do zero em vez de herdá-lo.

Decisões honradas: **D-02** (o grupo "Categoria" lista as 5 categorias reais do CMS, não os rótulos
de subcategoria do layout), **D-06** (`'use client'` em tudo que declara `styled`), **D-07** (o botão
`FILTROS` aparece/some por `media.mobile`/`media.desktop`, o breakpoint único de 1080px, per D7),
**D-09** (AND/OR). Honra também a divergência **D8** de `docs/divergencias.md`: a lista de cores do
grupo `Cor` é a que veio do CMS, não os três valores fixos do layout.

Purpose: sem o painel, os 5 grupos existem só como contrato; sem a toolbar, a ordenação de 5 opções
de CATA-03 não tem interface.
Output: painel, swatches multi-seleção e toolbar montados no `page.tsx`, com a URL como única fonte
de verdade do estado de filtro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catalogo/05-CONTEXT.md
@.planning/phases/05-catalogo/05-UI-SPEC.md
@.planning/phases/05-catalogo/05-PATTERNS.md
@.planning/phases/05-catalogo/05-02-SUMMARY.md
@.planning/phases/05-catalogo/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-seleção de cor (`SwatchesDeCor`) sem tocar no primitivo do card</name>
  <files>src/components/catalogo/SwatchesDeCor.tsx, src/components/catalogo/SwatchesDeCor.test.tsx</files>
  <read_first>
    - `src/components/primitives/ColorSwatches.tsx` (arquivo inteiro — a API atual é seleção **única** (`selecionada?: string`, `onSelecionar`), o rótulo é `COR · {selecionada|OBRIGATÓRIO}` e o `aria-pressed` compara com a única selecionada; é o componente usado pelo `ProductCard`, e alterá-lo mudaria o comportamento do card)
    - `src/lib/site/navigation.ts` linhas ~74-80 (`coresProduto` — o mapa nome→hex, fonte única; `Bege #D8C9A8`, `Preto #0B0C0D`, `Branco #FFFFFF` são exatamente as três primeiras entradas)
    - `projeto-base/All Music Rentals - Catalogo.dc.html` linha ~403 (`const HEX = { 'Bege': '#D8C9A8', 'Preto': '#0B0C0D', 'Branco': '#FFFFFF' }` — confirma que os hex do filtro são os mesmos do mapa existente)
    - `.planning/phases/05-catalogo/05-UI-SPEC.md` (Bloco 3, linha do grupo `cor`, e Bloco 5 — no chip aparece o **nome** da cor, o swatch fica no painel)
    - `.planning/phases/05-catalogo/05-PATTERNS.md` (seção "ColorSwatches — reuso no filtro Cor" — a instrução explícita de não montar a variante multi-select dentro do primitivo compartilhado)
    - `docs/divergencias.md` (**D8**, escrita em 05-01 — a lista de cores exibida é derivada do CMS, não os 3 valores fixos do layout; e a rubrica de escopo que explica por que a allowlist de parse tem 5 nomes e o painel exibe 3)
    - `src/app/[locale]/catalogo/page.tsx` (após 05-04 — a chamada a `getCoresDisponiveis` e o marcador de texto no `aside` que a Task 3 deste plano substitui)
  </read_first>
  <action>
Criar `src/components/catalogo/SwatchesDeCor.tsx` com `'use client'`. É um componente **novo**, local
ao catálogo — `src/components/primitives/ColorSwatches.tsx` **não pode ser modificado**, porque sua
semântica de seleção única com rótulo `COR · OBRIGATÓRIO` pertence ao `ProductCard` e mudá-la
alteraria o card em todas as páginas.

Props: `cores: string[]` (os nomes já resolvidos e ordenados pelo servidor), `selecionadas: string[]`
e `onAlternar: (cor: string) => void`.

**A lista exibida é a prop `cores`, e nada mais.** O componente renderiza `cores.map(...)` e não tem
lista de cores própria: nenhum array literal de nomes, nenhum `Object.keys(coresProduto)`, nenhum
valor de fallback do tipo `cores ?? ['Bege', 'Preto', 'Branco']`. A origem é `getCoresDisponiveis`
(05-02), chamada no Server Component e descida por prop — é o que a divergência **D8** registra e o
que torna verdadeira a microcopy do layout "Outras cores cadastradas aparecem aqui.": cadastrar um
produto `Bordô` no CMS faz o swatch aparecer sem deploy, e isso só é verdade se a lista não estiver
congelada neste arquivo.

`coresProduto` é importado com **uma única finalidade**: resolver o hex de cada nome recebido
(`coresProduto[cor]`). Não é fonte da lista de opções. Se um nome chegar sem entrada no `Record` — o
que `getCoresDisponiveis` já impede por construção, porque filtra pela paleta —, tratar como caso
degenerado silencioso (não renderizar aquele swatch) em vez de inventar um hex.
Se `cores` vier vazio, renderizar apenas a nota auxiliar, sem swatch e sem cabeçalho órfão: catálogo
sem nenhuma variação cadastrada é estado possível, não erro.

Cada swatch é um `<button type="button">` com: `aria-pressed={selecionadas.includes(cor)}`,
`aria-label={cor}` (o nome legível, porque a cor é a única informação e ela é visual),
`title={cor}`, alvo de toque de no mínimo 44×44px, `background` vindo de `coresProduto[cor]` —
**importar o mapa, nunca redigitar hex**. O estado selecionado precisa ser distinguível sem depender
de cor (anel/borda de seleção mais espessa), porque `Branco #FFFFFF` sobre fundo claro é
praticamente invisível e a distinção não pode ser só cromática. Foco visível com o mesmo
`outline: 2px solid` teal já usado no design system.

Abaixo dos swatches, a nota literal do layout como texto auxiliar: `Outras cores cadastradas
aparecem aqui.`
  </action>
  <verify>
    <automated>npx jest src/components/catalogo/SwatchesDeCor.test.tsx && npm run typecheck && npm run lint && git diff --exit-code -- src/components/primitives/ColorSwatches.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/components/catalogo/SwatchesDeCor.tsx` é `'use client';`
    - `grep -q "from '@/lib/site/navigation'" src/components/catalogo/SwatchesDeCor.tsx` sai 0 e `grep -Eq '#[0-9A-Fa-f]{6}' src/components/catalogo/SwatchesDeCor.tsx` sai 1 (nenhum hex redigitado)
    - `grep -q 'aria-pressed' src/components/catalogo/SwatchesDeCor.tsx` sai 0
    - `grep -q 'Outras cores cadastradas aparecem aqui.' src/components/catalogo/SwatchesDeCor.tsx` sai 0
    - `grep -q 'cores.map(' src/components/catalogo/SwatchesDeCor.tsx` sai 0 — os swatches saem da prop recebida
    - `grep -Eq "'(Bege|Preto|Branco|Azul-marinho|Bordô)'" src/components/catalogo/SwatchesDeCor.tsx` sai 1 — nenhum nome de cor literal no componente
    - `grep -q 'Object.keys(coresProduto)' src/components/catalogo/SwatchesDeCor.tsx` sai 1 — a lista de opções não é derivada da paleta
    - Fora da linha de import, toda menção à paleta é acesso indexado para hex: `grep -n 'coresProduto' src/components/catalogo/SwatchesDeCor.tsx | grep -v "from '@/lib/site/navigation'" | grep -vc 'coresProduto\['` retorna `0`
    - Existe teste que passa `cores={['Bege', 'Preto', 'Branco', 'Bordô']}` e assere **4** swatches na tela — prova de que o componente exibe o que recebe, e não uma lista fixa de 3
    - Existe teste que passa `cores={[]}` e assere zero swatches, sem quebrar
    - `git diff --exit-code -- src/components/primitives/ColorSwatches.tsx` sai 0 — o primitivo do card não foi tocado
    - `npm run typecheck` e `npm run lint` saem 0
  </acceptance_criteria>
  <done>O filtro de cor tem multi-seleção acessível sobre a lista vinda do CMS, sem lista de cores própria e sem que o `ProductCard` mude de comportamento.</done>
</task>

<task type="auto">
  <name>Task 2: `PainelDeFiltros` — acordeão múltiplo dos 5 grupos ligado à URL</name>
  <files>src/components/catalogo/PainelDeFiltros.tsx, src/components/catalogo/PainelDeFiltros.test.tsx</files>
  <read_first>
    - `src/lib/catalogo/filtros.ts` (criado em 05-02 — `GRUPOS_DE_FILTRO` com `id`/`rotulo`/`abertoPorPadrao`/`swatch`/`nota`, `alternarValor`, `parseFiltrosDaUrl`; é o contrato completo, não reinventar)
    - `node_modules/@radix-ui/react-accordion/dist/index.d.ts` (a API real: `Root` com `type="multiple"` e `defaultValue: string[]`, `Item value`, `Header`, `Trigger`, `Content`; o pacote está instalado e **nunca foi importado em `src/`** — não há analog interno)
    - `src/components/catalogo/SwatchesDeCor.tsx` (criado na Task 1 — o grupo `cor` o usa)
    - `src/components/chrome/MobileMenu.tsx` (analog **apenas de estilo**: cores, espaçamento, tratamento de lista. NÃO é precedente de gestão de foco — é CSS por media query comandado por `s.ui.menuMobileAberto` do Redux, sem foco preso, sem `Esc`)
    - `projeto-base/All Music Rentals - Catalogo.dc.html` linhas 388-401 (o array `GRUPOS`: os `id`, os `rotulo`, o `aberto` de cada um e as `nota`) e linhas 145-152 (o aside com `max-height: calc(100vh - 200px)` e `overflow-y: auto`)
    - `src/components/blocos/SearchBarGrande.test.tsx` (o padrão de teste com `jest-axe` e Testing Library, incluindo o mock de `next/navigation`)
  </read_first>
  <action>
Criar `src/components/catalogo/PainelDeFiltros.tsx` com `'use client'`.

Props: `grupos` (as opções resolvidas — o grupo `categoria` recebe as 5 categorias reais do CMS per
**D-02**, o grupo `evento` recebe só os tipos de evento com `exibirNoFiltroDoCatalogo` verdadeiro, o
que mantém `Outro` disponível para o formulário da Fase 9 e fora daqui, e o grupo `cor` recebe o
retorno de `getCoresDisponiveis` per **D8** — os três chegam prontos do servidor, e nenhum dos três
tem lista literal neste arquivo) e `idPrefixo` (para que o
painel possa ser renderizado duas vezes na página — aside e drawer — sem colidir `id` de checkbox;
o drawer de 05-06 vai usar isso).

Estrutura com Radix: `Accordion.Root type="multiple"` com
`defaultValue={GRUPOS_DE_FILTRO.filter(g => g.abertoPorPadrao).map(g => g.id)}`, o que abre
Categoria, Tipo de item e Cor e deixa Tipo de evento e Ambiente fechados — exatamente o estado
`aberto` do array `GRUPOS` do layout. `type="multiple"` é obrigatório: o UI-SPEC exige acordeão
múltiplo, não exclusivo, e três grupos precisam estar abertos ao mesmo tempo na carga.

Cada grupo é um `Accordion.Item value={grupo.id}` com `Accordion.Header` > `Accordion.Trigger`
(rótulo do grupo + indicador de aberto/fechado que gire por `[data-state="open"]`) e
`Accordion.Content` com as opções. O `Trigger` do Radix já é `<button>` com `aria-expanded` e
`aria-controls` corretos e responde a `Enter`/`Space` — **não** reimplementar handler de teclado por
cima, e **não** trocar o `Trigger` por `div`. Estilizar com `styled(Accordion.Trigger)` para
preservar o comportamento.

Conteúdo por grupo:
- `categoria`, `tipo`, `evento`, `ambiente`: lista de `<label>` + `<input type="checkbox">`, com `id`
  derivado de `${idPrefixo}-${grupo.id}-${valor}` e `checked` vindo do filtro corrente. Alvo de toque
  ≥44px na área clicável do label.
- `cor`: `SwatchesDeCor` da Task 1, recebendo em `cores` exatamente a lista que veio por prop do
  servidor. O painel **repassa**, não decide: nenhuma cor é acrescentada, removida ou reordenada aqui.
- Notas literais onde o layout as tem: em `categoria`, `Novas categorias entram nesta lista conforme
  forem cadastradas.`; a nota de `cor` já vive dentro do `SwatchesDeCor`.

Ligação com a URL — este é o ponto onde a armadilha 2 do RESEARCH §6 (chips dessincronizados) é
fechada na origem: o painel **não tem estado próprio de filtro**. Ele lê o filtro corrente de
`useSearchParams()` e, a cada alternância, chama `alternarValor(params, chave, valor)` de
`filtros.ts` e faz `router.push(\`?\${novosParams}\`)` dentro de `useTransition`. Nenhum
`useState` de valores selecionados. Consequência desejada: recarregar a página, voltar no histórico
ou colar a URL reconstroem exatamente o mesmo estado, porque não existe outro lugar onde o estado
possa morar.

O que **não** entra aqui: o estado de aberto/fechado do acordeão é UI efêmera e pode viver no Radix
(não vai para a URL); e o painel não emite `filter_applied` — a emissão é centralizada em 05-07 para
não duplicar evento entre o painel do aside e o do drawer, que renderizam o mesmo componente.

Testes em `PainelDeFiltros.test.tsx` (mockando `next/navigation`):
- os 5 rótulos de grupo estão na tela
- Categoria, Tipo de item e Cor começam expandidos (`aria-expanded="true"` nos três triggers) e
  Tipo de evento e Ambiente começam recolhidos
- `Tab` alcança os triggers e `Enter` alterna `aria-expanded` (usar `userEvent.keyboard`)
- clicar num checkbox chama `router.push` com o parâmetro correspondente acrescentado
- clicar num checkbox **já marcado** chama `router.push` com o parâmetro removido
- marcar dois valores do mesmo grupo produz o parâmetro repetido duas vezes (a base do OR)
- o grupo `categoria` renderiza as categorias recebidas por prop, e nenhum dos rótulos de
  subcategoria do layout (`Área externa`, `Mesas de coquetel`, `Capas de mesa`, `Painéis de LED`)
  aparece — a asserção que prova D-02
- `jest-axe` sem violação séria
  </action>
  <verify>
    <automated>npx jest src/components/catalogo/PainelDeFiltros.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@radix-ui/react-accordion" src/components/catalogo/PainelDeFiltros.tsx` sai 0
    - `grep -q 'type="multiple"' src/components/catalogo/PainelDeFiltros.tsx` sai 0
    - `grep -c 'useState' src/components/catalogo/PainelDeFiltros.tsx` retorna 0 — nenhum estado local de filtro
    - `grep -q 'alternarValor' src/components/catalogo/PainelDeFiltros.tsx` sai 0
    - `grep -q 'Novas categorias entram nesta lista conforme forem cadastradas.' src/components/catalogo/PainelDeFiltros.tsx` sai 0
    - Existe teste que assere que `Mesas de coquetel` **não** está no documento (prova de D-02)
    - `grep -Eq "'(Bege|Preto|Branco)'" src/components/catalogo/PainelDeFiltros.tsx` sai 1 — o painel também não tem lista de cores literal
    - Existe teste que passa uma cor arbitrária da paleta no grupo `cor` e assere que ela é renderizada — o painel repassa a lista recebida sem filtrar
    - Existe teste com `userEvent.keyboard('{Enter}')` sobre um trigger que assere a troca de `aria-expanded`
    - Existe teste que assere que marcar dois valores do grupo `tipo` gera dois valores da mesma chave no `router.push`
    - `npx jest src/components/catalogo/PainelDeFiltros.test.tsx` sai 0, incluindo o caso de `jest-axe`
  </acceptance_criteria>
  <done>Os 5 grupos filtram de verdade, a URL é a única fonte de verdade do estado, e o teclado opera o acordeão sem código de teclado próprio.</done>
</task>

<task type="auto">
  <name>Task 3: `ToolbarDoCatalogo` (contagem, ordenação de 5 opções, botão FILTROS) e montagem no `page.tsx`</name>
  <files>src/components/catalogo/ToolbarDoCatalogo.tsx, src/components/catalogo/ToolbarDoCatalogo.test.tsx, src/app/[locale]/catalogo/page.tsx</files>
  <read_first>
    - `projeto-base/All Music Rentals - Catalogo.dc.html` linhas 155-172 (a toolbar real: botão `FILTROS` com badge de contagem, o parágrafo de contagem, e o `<label>Ordenar por` com as 5 `<option>` literais) e linha ~701 (a regra da contagem: `CARREGANDO PRODUTOS` / `1 PRODUTO` / `N PRODUTOS`)
    - `src/lib/catalogo/filtros.ts` (`ORDENACOES_UI` com as 5 chaves e rótulos, e `contarFiltrosAtivos` para o badge)
    - `src/app/[locale]/catalogo/page.tsx` (o arquivo a modificar — o shell criado em 05-04, com os pontos `aside` e `children` do `LayoutCatalogo` ainda com marcadores)
    - `src/components/catalogo/LayoutCatalogo.tsx` (as props `aside` e `children`)
    - `src/lib/theme/media.ts` (`media.mobile` / `media.desktop` — o botão `FILTROS` só aparece em `media.mobile`, per D7)
    - `src/components/primitives/Button.tsx` (as variantes disponíveis e o alvo de toque de 44px já resolvido)
    - `src/store/slices/uiSlice.ts` (o campo `drawerFiltrosAberto` e a ação `definirDrawerFiltros`, criados na Fase 2 e ainda sem consumidor — é o que o botão `FILTROS` vai acionar)
  </read_first>
  <action>
Criar `src/components/catalogo/ToolbarDoCatalogo.tsx` com `'use client'`. Três elementos numa linha
`flex` com `flex-wrap`, `gap: 16px` e `justify-content: space-between`, como no layout.

**Botão `FILTROS`.** `<button type="button">` com rótulo `FILTROS` e, quando houver filtro ativo, um
badge com `contarFiltrosAtivos(filtro)`. Visível apenas abaixo de 1080px: `display: none` dentro de
`media.desktop` (per D7 — nenhum breakpoint novo). O `aria-label` inclui a contagem quando houver
badge (por exemplo `Filtros, 3 ativos`), porque o número sozinho não é anunciado com sentido.
`onClick` despacha `definirDrawerFiltros(true)` do `uiSlice` — o drawer em si é 05-06; aqui só a
origem do comando.

**Contagem.** Um `<p>` com a cópia exata do layout: `1 PRODUTO` quando houver um, `${n} PRODUTOS`
caso contrário. A contagem vem do **comprimento do mesmo array** que a grade recebe — passar
`total={produtos.length}` a partir do `page.tsx`, nunca de uma segunda consulta ao CMS. Isso fecha a
armadilha 1 do RESEARCH §6 e é a razão do `porPagina: 100` estabelecido em 05-04. Marcar o elemento
com `aria-live="polite"` para que a mudança de contagem após aplicar um filtro seja anunciada.

**Ordenação.** `<label>` com o texto `Ordenar por` envolvendo um `<select>` com as 5 `<option>` de
`ORDENACOES_UI`, na ordem literal do layout: `Produtos em destaque`, `Mais solicitados`,
`Mais recentes`, `Nome de A a Z`, `Nome de Z a A`. O `value` do `<option>` é a chave (`destaque`,
`solicitados`, `recentes`, `nome-asc`, `nome-desc`), não o rótulo. `onChange` reescreve a chave
`ordenar` nos `searchParams` e faz `router.push` dentro de `useTransition`, igual ao painel. Altura
mínima 44px e foco visível. Nenhuma das 5 opções é monetária — o layout já respeitava a regra sem
preço, e nenhuma opção nova pode ser acrescentada.

Em `src/app/[locale]/catalogo/page.tsx`, substituir os marcadores deixados em 05-04:
- o `aside` do `LayoutCatalogo` recebe o `PainelDeFiltros` com `idPrefixo="aside"`, dentro de um
  contêiner com `max-height: calc(100vh - 200px)` e `overflow-y: auto`, como no layout
- o conteúdo do `<main>` passa a começar pela `ToolbarDoCatalogo`, recebendo `total`, o filtro
  corrente e a chave de ordenação corrente
- as opções resolvidas dos grupos são montadas aqui, no servidor: `categoria` a partir de
  `getCategorias` (per D-02), `evento` a partir de `getTiposDeEvento` filtrado por
  `exibirNoFiltroDoCatalogo`, e `cor` a partir de `getCoresDisponiveis` (per **D8**) — a chamada já
  existe desde 05-04, então aqui é só redirecionar o valor: o marcador de texto que 05-04 deixou no
  `aside` **sai**, e a mesma variável passa a alimentar a prop do grupo `cor` do `PainelDeFiltros`.
  Não acrescentar nem a allowlist de parse nem nenhuma lista literal de cores: a assimetria 5≠3 entre
  a allowlist de `parseFiltrosDaUrl` e o conjunto exibido é intencional e já está comentada no
  arquivo por 05-04

Testes em `ToolbarDoCatalogo.test.tsx` (mockando `next/navigation` e a store do Redux no padrão já
usado pelos testes de chrome): as 5 opções aparecem com os rótulos literais; trocar a seleção chama
`router.push` com `ordenar=<chave>`; a contagem mostra `1 PRODUTO` para 1 e `10 PRODUTOS` para 10; o
badge mostra o número de filtros ativos; clicar em `FILTROS` despacha `definirDrawerFiltros(true)`;
`jest-axe` sem violação séria.
  </action>
  <verify>
    <automated>npx jest src/components/catalogo src/app/[locale]/catalogo && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Produtos em destaque\|Mais solicitados\|Mais recentes\|Nome de A a Z\|Nome de Z a A" src/components/catalogo/ToolbarDoCatalogo.tsx` retorna ≥5, ou os rótulos vêm de `ORDENACOES_UI` e o teste assere os 5 na tela
    - `grep -q 'Ordenar por' src/components/catalogo/ToolbarDoCatalogo.tsx` sai 0
    - `grep -q 'aria-live' src/components/catalogo/ToolbarDoCatalogo.tsx` sai 0
    - `grep -q 'media.desktop' src/components/catalogo/ToolbarDoCatalogo.tsx` sai 0 e `grep -c '@media' src/components/catalogo/ToolbarDoCatalogo.tsx` retorna 0
    - `grep -q 'definirDrawerFiltros' src/components/catalogo/ToolbarDoCatalogo.tsx` sai 0
    - `grep -c 'getProdutos' 'src/app/[locale]/catalogo/page.tsx'` retorna 1 — uma consulta só alimenta contagem e grade
    - `grep -q 'getCoresDisponiveis' 'src/app/[locale]/catalogo/page.tsx'` sai 0 e o valor alimenta a prop do grupo `cor`; o marcador de texto de cores deixado por 05-04 no `aside` não está mais presente
    - `grep -Eq "'(Bege|Preto|Branco|Azul-marinho|Bordô)'" 'src/app/[locale]/catalogo/page.tsx'` sai 1 — nenhuma cor literal na página
    - Existe teste que assere `1 PRODUTO` para total 1 e `10 PRODUTOS` para total 10
    - Existe teste que assere que trocar o select chama `router.push` contendo `ordenar=nome-desc`
    - `npm run build` sai 0
  </acceptance_criteria>
  <done>Painel e toolbar estão montados na rota, a ordenação de 5 opções funciona, e a contagem não pode divergir da grade porque vem do mesmo array.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interação do visitante → `searchParams` | Checkbox, swatch e select reescrevem a URL, que na próxima renderização volta como entrada não confiável do servidor |
| opções de filtro vindas do CMS → rótulos na tela | Nomes de categoria e de tipo de evento são conteúdo editorial renderizado na interface |
| botão FILTROS → `uiSlice` | Estado de UI compartilhado entre toolbar e drawer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Tampering | URL reescrita pelo painel e pela toolbar | mitigate | O painel e a toolbar só escrevem valores que vieram das opções resolvidas pelo servidor ou das chaves de `ORDENACOES_UI`; e mesmo assim a URL é reprocessada por `parseFiltrosDaUrl` no servidor a cada renderização — o cliente não é confiado como validador |
| T-05-20 | Information Disclosure | Rótulos de categoria e tipo de evento vindos do Strapi | mitigate | Renderizados como texto por React, que escapa por padrão; nenhum `dangerouslySetInnerHTML` neste plano. Rich text sanitizado (`HtmlSeguro`) não é usado aqui porque nenhum campo destes grupos é rich text |
| T-05-21 | Denial of Service | Marcação rápida de muitos filtros gerando muitas navegações | mitigate | Cada alternância roda dentro de `useTransition`, que o React coalesce; a consulta ao CMS é limitada por `porPagina: 100` e servida com cache por tag |
| T-05-22 | Spoofing | Ordenação com chave forjada (`?ordenar=<qualquer>`) | mitigate | `parseFiltrosDaUrl` descarta chave de ordenação fora das 5 permitidas e `getProdutos` cai no padrão `destaque`; nenhuma string da URL vira campo de `sort` do Strapi |
| T-05-23 | Elevation of Privilege | Filtro por campo não previsto (ex.: `publicationState`, `populate`) | mitigate | A allowlist de chaves de 05-02 aceita exatamente 7 nomes; os nomes de chave da query do Strapi são literais no código de `getProdutos` |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo é instalado. `@radix-ui/react-accordion@^1.2.20` já consta em `package.json` e no `package-lock.json` desde a Fase 1 — este plano é o primeiro a **importá-lo**, o que não altera a árvore de dependências. Se o executor concluir que precisa de biblioteca nova de acordeão, deve parar e escalar: a stack é fechada |
</threat_model>

<verification>
1. `npm run typecheck`, `npm run lint` e `npm run build` saem 0
2. `npx jest src/components/catalogo src/app` sai 0
3. `git diff --exit-code -- src/components/primitives/` sai 0 — nenhum primitivo compartilhado foi alterado
4. Com o Strapi de pé: `/pt-BR/catalogo?tipo=pacote&tipo=servico-tecnico` mostra 2 produtos (OR dentro do grupo)
5. `/pt-BR/catalogo?categoria=moveis&cor=Bege&cor=Preto` mostra 4 produtos (AND entre grupos, OR dentro)
6. `grep -rn '@media' src/components/catalogo/` só retorna ocorrências que venham de `media.mobile`/`media.desktop`

</verification>

<success_criteria>
- Os 5 grupos aparecem em acordeão múltiplo com os 3 primeiros abertos de saída
- AND entre grupos e OR dentro do grupo são observáveis na tela, não só em teste unitário
- As 5 opções de ordenação existem com os rótulos literais e nenhuma é monetária
- A contagem exibida é `produtos.length` do mesmo array que a grade recebe
- O acordeão é operável só por teclado, com foco visível, e o axe não reporta violação séria
- `src/components/primitives/ColorSwatches.tsx` está intacto
- Os swatches de cor exibem a lista vinda de `getCoresDisponiveis` (D8); nem `SwatchesDeCor`, nem `PainelDeFiltros`, nem `page.tsx` contêm nome de cor literal
</success_criteria>

<output>
Create `.planning/phases/05-catalogo/05-05-SUMMARY.md` when done
</output>
