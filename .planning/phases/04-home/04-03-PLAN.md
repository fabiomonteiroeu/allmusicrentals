---
phase: 04-home
plan: 03
type: execute
wave: 2
depends_on: ['04-01', '04-02']
files_modified:
  - src/components/blocos/HeroBloco.tsx
  - src/components/blocos/HeroBloco.test.tsx
  - src/components/blocos/ChamadaFinalBloco.tsx
  - src/components/blocos/ChamadaFinalBloco.test.tsx
autonomous: true
requirements: [HOME-01, HOME-04]

must_haves:
  truths:
    - "O visitante vê o título 'O palco é seu. Nós levamos a estrutura.' no maior tipo da página, sobre fundo escuro"
    - "O mosaico do hero revela as células em cascata, e não anima nada sob preferência de movimento reduzido"
    - "Sem imagem cadastrada no bloco hero, a seção mostra fundo sólido escuro em vez de quebrar"
    - "Os dois CTAs do hero levam a /{locale}/catalogo e /{locale}/solicitar-orcamento"
    - "A página fecha com o CTA final 'Comece a montar seu evento' sobre fundo escuro"
  artifacts:
    - path: "src/components/blocos/HeroBloco.tsx"
      provides: "Bloco 1 da Home — hero com mosaico decorativo em CSS"
      min_lines: 120
      contains: "clamp(64px, 9vw, 144px)"
    - path: "src/components/blocos/ChamadaFinalBloco.tsx"
      provides: "Bloco 9 da Home — CTA final"
      contains: "SectionDivider"
  key_links:
    - from: "src/components/blocos/HeroBloco.tsx"
      to: "src/components/primitives/Typography.tsx"
      via: "Eyebrow $sobreEscuro e Heading $nivel=h1"
      pattern: "\\$sobreEscuro"
    - from: "src/components/blocos/HeroBloco.tsx"
      to: "src/lib/cms/adapters.ts"
      via: "tipo Bloco estreitado por __component"
      pattern: "from '@/lib/cms/adapters'"
---

<objective>
Entregar os dois blocos escuros que abrem e fecham a Home — Bloco 1 (Hero) e Bloco 9 (Chamada final) — e, com
eles, **fixar o contrato de props que todos os blocos desta fase seguem** (`{ bloco, locale }`, tipo estreitado
da união `Bloco`).

Propósito: o Hero é a âncora visual da página, o único bloco com o maior tipo (`clamp(40px, 5.6vw, 72px)` no
layout-fonte, `line-height: 0.92`) e o mosaico animado, que é o único elemento novo da fase sem análogo no
projeto. O Bloco 9 é a irmã estrutural dele (mesma seção escura, mesmo grid `auto-fit`), então sai no mesmo
plano por reuso de padrão.

Saída: `HeroBloco` e `ChamadaFinalBloco` como Server Components puros (sem `'use client'`), com testes de
render e asserções sobre os valores exatos do UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-home/04-UI-SPEC.md
@.planning/phases/04-home/04-PATTERNS.md
@.planning/phases/04-home/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroBloco — Bloco 1, com mosaico decorativo em CSS puro</name>
  <files>src/components/blocos/HeroBloco.tsx, src/components/blocos/HeroBloco.test.tsx</files>
  <read_first>
    - `src/lib/cms/schemas.ts` linhas 187-195 — `blocoHero`: campos reais `eyebrow`, `titulo` (obrigatório), `subtitulo`, `citacao`, `imagem`, `ctaPrimarioRotulo`, `ctaPrimarioUrl`, `ctaSecundarioRotulo`, `ctaSecundarioUrl`. **Não** inventar campo que não está aqui.
    - `src/lib/cms/adapters.ts` linhas 415-427 — o tipo `Bloco` (união) e, linhas 48-53, `Imagem` (`url`, `alt`, `largura?`, `altura?`).
    - `src/components/chrome/TopBar.tsx` linhas 7-13 — molde de seção escura com `styled` no tema (`theme.cor.tinta900` / `theme.cor.tinta800`).
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Bloco 1 — Hero" (tabela de conteúdo completa) e a seção "Regras estruturais herdadas".
    - `projeto-base/All Music Rentals - Home.dc.html` linhas 616-655 — função `heroFundo()`, de onde saem os números do mosaico.
    - `src/components/primitives/Container.tsx` e `src/components/feedback/SectionDivider.tsx` (16 linhas) — **atenção:** `SectionDivider` tem prop booleana `$claro`, não variante string. Escura = `<SectionDivider />` sem prop.
  </read_first>
  <action>
Criar `src/components/blocos/HeroBloco.tsx` — **Server Component, sem `'use client'`**. Nenhum `useState`,
`useEffect` ou `window` neste arquivo. `styled-components` em Server Component é o padrão já usado por
`TopBar`/`Header` do projeto via `StyledRegistry`.

**Contrato de props (é o contrato de TODOS os blocos desta fase — repetir nos outros planos):**
`export interface HeroBlocoProps { bloco: Extract<Bloco, { __component: 'blocos.hero' }>; locale: Locale }`,
importando `type Bloco` de `@/lib/cms/adapters` e `type Locale` de `@/i18n/config`. O renderizador (plano
04-07) chama `<HeroBloco bloco={bloco} locale={locale} />` e o `switch` já entrega o membro estreitado da união.
**Não** usar spread `{...bloco}`.

**Seção:** `styled.section` com `background: theme.cor.tinta900`, `color: theme.cor.fundo`,
`position: relative`, `overflow: hidden`. Dentro, o mosaico (posicionado absoluto) e um `Container` com
`position: relative; z-index: 1; padding-block: clamp(64px, 9vw, 144px);`.

**Mosaico (decorativo, `aria-hidden="true"`, `zIndex: 0`, `position: absolute; inset: 0`).** Valores exatos do
layout-fonte, `cols = 12` e `rows = 6` **fixos** (não dependem de viewport, não há leitura de `window`):
- Grade: `position: absolute; inset: 0; display: grid; grid-template-columns: repeat(12, 1fr);
  grid-template-rows: repeat(6, 1fr); gap: 2px; background: ${theme.cor.tinta900};`
- Gerar as 72 células com dois laços (`r` de 0 a 5, `c` de 0 a 11) e um `styled.div` com props transientes
  `$src`, `$posX`, `$posY`, `$delay`:
  - `background-image: url(${$src})`
  - `background-size: 1200% 600%` (é `cols*100% rows*100%`)
  - `background-position: ${$posX}% ${$posY}%`, onde `$posX = (c / 11) * 100` e `$posY = (r / 5) * 100`
  - `animation: amrMod 0.34s ease-out both;`
  - `animation-delay: ${$delay}s`, onde `$delay = ((r + c) * 0.045 + 0.15).toFixed(3)`
- `amrMod` já existe em `GlobalStyle.ts` (plano 04-02) — **não** redeclarar keyframe local.
- **Movimento reduzido:** nenhuma checagem em JS. O bloco global de `GlobalStyle.ts` já zera
  `animation-duration`, `animation-iteration-count` **e** `animation-delay` (linha adicionada no plano 04-02).
  Registrar isso como comentário: é o que permite o mosaico ser CSS renderizado no servidor, sem
  `window.matchMedia` e sem risco de hidratação — divergência deliberada e melhor que o layout-fonte, que usava
  JS.
- Scrim por cima da grade, `position: absolute; inset: 0`, com o gradiente exato:
  `linear-gradient(100deg, rgba(11,12,13,0.96) 0%, rgba(11,12,13,0.88) 40%, rgba(11,12,13,0.66) 62%, rgba(11,12,13,0.40) 80%, rgba(11,12,13,0.12) 100%)`.
- **Sem imagem cadastrada (`bloco.imagem` ausente/`url` vazia):** não renderizar mosaico nem scrim — só o fundo
  sólido `tinta900`. Não há nada para dar legibilidade contra, e um scrim sozinho escureceria de graça.

**Conteúdo** (coluna única, `max-width: 640px`, `position: relative`), na ordem e com os valores exatos:
| Elemento | Spec |
|---|---|
| Eyebrow | `<Eyebrow $sobreEscuro>` com `bloco.eyebrow` (renderizar só se houver valor), `margin-bottom: 20px` |
| H1 | `<Heading as="h1" $nivel="h1">` com `bloco.titulo`, `margin-bottom: 24px`, `text-wrap: balance` |
| Body 1 | `bloco.subtitulo` (só se houver): `font-size: theme.fluido.corpoGrande`, `line-height: 1.55`, `color: theme.cor.navInativo`, `max-width: 52ch`, `margin-bottom: 20px` |
| Body 2 (citação) | `bloco.citacao` (só se houver): `border-left: 2px solid ${theme.cor.teal}`, `padding-left: 16px`, `font-size: theme.tamanho[15]`, `line-height: 1.5`, `color: theme.cor.navInativo`, `max-width: 52ch`, `margin-bottom: 32px` |
| CTAs | wrapper `display: flex; flex-wrap: wrap; gap: 12px`. Primário: `<Button as="a" href={...} $variante="primario" $tamanho="lg">` com rótulo `bloco.ctaPrimarioRotulo ?? 'EXPLORAR CATÁLOGO'` e href `bloco.ctaPrimarioUrl ?? `/${locale}/catalogo``. Secundário: `$variante="outlineClaro" $tamanho="lg"`, rótulo `bloco.ctaSecundarioRotulo ?? 'SOLICITAR ORÇAMENTO'`, href `bloco.ctaSecundarioUrl ?? `/${locale}/solicitar-orcamento`` |
| Legenda de vídeo | texto fixo `VÍDEO · PALCO COM PAINEL DE LED · 16:9`: `font-family: theme.fonte.mono`, `font-size: theme.tamanho[13]`, `line-height: 1.4`, `letter-spacing: theme.tracking.rotulo`, `color: theme.cor.textoMutedClaro`, `margin-top: 32px`. **Não** implementar player — no layout-fonte é só rótulo textual |

**Destino dos CTAs:** `/${locale}/catalogo` e `/${locale}/solicitar-orcamento`. No layout-fonte o primário é a
âncora `#destaques`; a decisão travada do CONTEXT.md ("CTAs apontam para as rotas finais, mesmo antes de
existirem") manda usar a rota real. Dá 404 até a Fase 5 — aceito, **não** desabilitar o botão.

Fechar a seção com `<SectionDivider />` (variante escura = **sem** a prop `$claro`), fora do `Container`.

**`HeroBloco.test.tsx`** com `renderComProviders` de `@/test-utils`:
1. Com bloco completo (título, eyebrow, subtitulo, citacao, imagem com `url: 'http://localhost:1337/uploads/x.jpg'`) — o título aparece, o eyebrow aparece, e `container.querySelectorAll('[aria-hidden="true"] > div > div')` (ou seletor equivalente para as células) tem **72** elementos.
2. Sem `imagem` — o título continua aparecendo e a contagem de células é **0**.
3. Os dois CTAs têm `href` `/pt-BR/catalogo` e `/pt-BR/solicitar-orcamento` quando o CMS não traz `ctaPrimarioUrl`/`ctaSecundarioUrl` (`locale="pt-BR"`).
4. `axe` sem violações (`expect(await axe(container)).toHaveNoViolations()`), no padrão de `src/components/__tests__/a11y.test.tsx`.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/HeroBloco.test.tsx && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/HeroBloco.test.tsx` passa com 4 testes, incluindo a contagem de 72 células e o caso sem imagem (0 células).
    - `grep -c "clamp(64px, 9vw, 144px)" src/components/blocos/HeroBloco.tsx` retorna 1.
    - `grep -c "1200% 600%" src/components/blocos/HeroBloco.tsx` retorna 1.
    - `grep -c "amrMod 0.34s ease-out both" src/components/blocos/HeroBloco.tsx` retorna 1.
    - `grep -c "0.045 + 0.15" src/components/blocos/HeroBloco.tsx` retorna 1.
    - `grep -c "rgba(11,12,13,0.12) 100%" src/components/blocos/HeroBloco.tsx` retorna 1.
    - `grep -c "52ch" src/components/blocos/HeroBloco.tsx` retorna 2 (subtítulo e citação).
    - `grep -Ec "use client|useState|useEffect|matchMedia|window\." src/components/blocos/HeroBloco.tsx` retorna 0 — o bloco é Server Component puro.
    - `grep -c "\\$claro" src/components/blocos/HeroBloco.tsx` retorna 0 — o divisor escuro é sem prop.
  </acceptance_criteria>
  <done>O Hero renderiza título, eyebrow em teal, subtítulo, citação com barra teal, dois CTAs para rotas reais e a legenda de vídeo, sobre um mosaico de 72 células em cascata que degrada para fundo sólido sem imagem — tudo em Server Component, sem JS de cliente.</done>
</task>

<task type="auto">
  <name>Task 2: ChamadaFinalBloco — Bloco 9, CTA final</name>
  <files>src/components/blocos/ChamadaFinalBloco.tsx, src/components/blocos/ChamadaFinalBloco.test.tsx</files>
  <read_first>
    - `src/lib/cms/schemas.ts` linhas 244-249 — `blocoChamadaFinal`: `titulo`, `subtitulo` e os 4 campos de CTA (`ctaPrimarioRotulo`/`Url`, `ctaSecundarioRotulo`/`Url`).
    - `src/components/blocos/HeroBloco.tsx` (recém-criado na Task 1) — reusar o padrão de seção escura, o contrato de props `{ bloco, locale }` e o estilo de composição.
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Bloco 9 — CTA final".
    - `src/components/chrome/Footer.tsx` linhas 1-40 — o `Footer` que vem logo depois já abre com `border-top: 1px solid tinta800`; por isso este bloco **não** tem divisor de rodapé.
  </read_first>
  <action>
Criar `src/components/blocos/ChamadaFinalBloco.tsx` — **Server Component**, mesmo contrato de props:
`{ bloco: Extract<Bloco, { __component: 'blocos.chamada-final' }>; locale: Locale }`.

Estrutura, com os valores exatos:
- Abre com `<SectionDivider />` (escura, sem `$claro`).
- `styled.section`: `background: theme.cor.tinta900`, `color: theme.cor.fundo`.
- `Container` com `padding-block: clamp(64px, 9vw, 144px)`.
- Grid interno: `display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 40px;
  align-items: end;`. Nenhuma `@media`.
- Coluna esquerda:
  - `<Heading as="h2" $nivel="h2">` com `bloco.titulo ?? 'Comece a montar seu evento'`, `margin-bottom: 16px`.
  - Parágrafo com `bloco.subtitulo ?? 'Explore o catálogo, selecione os produtos e envie sua solicitação para nossa equipe.'`: `font-size: theme.tamanho[17]`, `line-height: 1.55`, `color: theme.cor.superficie200`, `max-width: 52ch`.
- Coluna direita: `display: flex; flex-wrap: wrap; gap: 12px`, com
  - CTA 1: `<Button as="a" $variante="outlineClaro" $tamanho="lg">`, rótulo `bloco.ctaPrimarioRotulo ?? 'VER TODOS OS PRODUTOS'`, href `bloco.ctaPrimarioUrl ?? `/${locale}/catalogo``.
  - CTA 2: `<Button as="a" $variante="primario" $tamanho="lg">`, rótulo `bloco.ctaSecundarioRotulo ?? 'SOLICITAR ORÇAMENTO'`, href `bloco.ctaSecundarioUrl ?? `/${locale}/solicitar-orcamento``.
- **Sem** `SectionDivider` de rodapé — o `Footer` já abre com a própria borda superior. Comentar isso no arquivo
  para ninguém "consertar" depois.

Destino do CTA 1: no layout-fonte é a âncora `#destaques` (rola até o slider da própria Home). Usar
`/${locale}/catalogo`, pela mesma decisão travada do CONTEXT.md aplicada ao Hero — comentar no código com a
referência à decisão, para a conferência de fidelidade não tratar como defeito.

**`ChamadaFinalBloco.test.tsx`** com `renderComProviders`:
1. Bloco vazio (só `__component`) — os textos padrão do layout aparecem ("Comece a montar seu evento" e o parágrafo) e os dois botões têm `href` `/pt-BR/catalogo` e `/pt-BR/solicitar-orcamento`.
2. Bloco com `titulo`/`subtitulo`/`ctaPrimarioUrl` vindos do CMS — os valores do CMS vencem os defaults.
3. `axe` sem violações.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/ChamadaFinalBloco.test.tsx && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/ChamadaFinalBloco.test.tsx` passa com 3 testes.
    - `grep -c "repeat(auto-fit, minmax(300px, 1fr))" src/components/blocos/ChamadaFinalBloco.tsx` retorna 1.
    - `grep -c "clamp(64px, 9vw, 144px)" src/components/blocos/ChamadaFinalBloco.tsx` retorna 1.
    - `grep -c "superficie200" src/components/blocos/ChamadaFinalBloco.tsx` retorna 1.
    - `grep -c "SectionDivider" src/components/blocos/ChamadaFinalBloco.tsx` retorna 2 (import + uso único de abertura).
    - `grep -Ec "use client|useState|useEffect" src/components/blocos/ChamadaFinalBloco.tsx` retorna 0.
    - `grep -c "@media" src/components/blocos/ChamadaFinalBloco.tsx` retorna 0.
  </acceptance_criteria>
  <done>O bloco 9 renderiza título, apoio e dois CTAs em grid fluido de 2 colunas sobre fundo escuro, abre com divisor escuro, não fecha com divisor, e usa as rotas reais de catálogo e orçamento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strapi → HTML renderizado | `titulo`, `subtitulo`, `citacao`, rótulos e URLs de CTA vêm do CMS |
| Strapi → `next/image` / `background-image` | `bloco.imagem.url` alimenta a URL da célula do mosaico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering (XSS) | `HeroBloco` / `ChamadaFinalBloco` | mitigate | Todo texto do CMS é interpolado como filho JSX (React escapa). **Zero** `dangerouslySetInnerHTML` nestes dois arquivos — nenhum campo de `blocos.hero`/`blocos.chamada-final` é rich text, e a guarda `src/__tests__/guards/html-sanitizado.test.ts` barra o uso |
| T-04-10 | Tampering (injeção de CSS via url()) | mosaico do `HeroBloco` | mitigate | `bloco.imagem.url` já passa por `adaptarImagem` (Zod na borda + normalização para URL absoluta a partir de `NEXT_PUBLIC_STRAPI_MEDIA_URL`). Só renderizar o mosaico quando `bloco.imagem?.url` for verdadeiro; nunca concatenar valor cru do CMS em `url()` sem esse caminho |
| T-04-11 | Tampering (open redirect) | hrefs de CTA vindos de `ctaPrimarioUrl`/`ctaSecundarioUrl` | accept | O valor é editável só por ator confiável do Strapi (mesmo nível de quem publica a Home) e o fallback é sempre uma rota interna com prefixo de locale. Registrar como concern para a Fase 12 (SEO/canônicas), que vai precisar de validação de URL interna de qualquer forma |
| T-04-12 | Denial of service (CLS/render) | 72 células do mosaico | accept | Contagem fixa de 12×6, sem JS, sem leitura de viewport. Orçamento de render é da Fase 14 |
| T-04-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `npm run check` verde.
- `npx jest src/components/blocos` passa com 7 testes deste plano.
- Nenhum dos dois arquivos contém `'use client'`, `useState`, `useEffect`, `window.` ou `@media`.
- Nenhum dos dois arquivos contém cor/tamanho literal fora dos gradientes e `clamp()` do layout-fonte: `grep -E "#[0-9A-Fa-f]{6}" src/components/blocos/HeroBloco.tsx src/components/blocos/ChamadaFinalBloco.tsx` retorna vazio (o scrim usa `rgba(11,12,13,...)`, que é o valor exato do layout-fonte e é aceito).
</verification>

<success_criteria>
- Bloco 1 e Bloco 9 existem como Server Components puros, com o contrato de props `{ bloco, locale }` que os planos 04-04 a 04-07 seguem.
- O mosaico reproduz os números exatos do layout-fonte (12×6, `1200% 600%`, delay `(r+c)*0.045+0.15`) sem uma linha de JS.
- Movimento reduzido é respeitado sem `window.matchMedia`, pela regra global do plano 04-02.
- Ambos os blocos degradam sem quebrar quando o CMS não traz campos opcionais.
</success_criteria>

<output>
Criar `.planning/phases/04-home/04-03-SUMMARY.md` ao concluir.
</output>
