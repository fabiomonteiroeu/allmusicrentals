---
phase: 04-home
plan: 04
type: execute
wave: 2
depends_on: ['04-01', '04-02']
files_modified:
  - src/components/blocos/SearchBarGrande.tsx
  - src/components/blocos/SearchBarGrande.test.tsx
  - src/components/blocos/BuscaBloco.tsx
  - src/components/blocos/GradeDeCategoriasBloco.tsx
  - src/components/blocos/GradeDeCategoriasBloco.test.tsx
autonomous: true
requirements: [HOME-01, HOME-04, HOME-05]

must_haves:
  truths:
    - "O visitante digita um termo, aperta BUSCAR e é levado para /{locale}/catalogo?q=termo"
    - "Submeter a busca vazia mostra 'Digite um produto, equipamento ou solução para buscar.' com role=alert, sem navegar"
    - "Enquanto a navegação está pendente o botão mostra BUSCANDO com spinner e fica desabilitado"
    - "O visitante vê 5 categorias reais: telas-de-led como card-bandeira escuro e as outras 4 em grade fluida"
    - "Cada card de categoria leva para /{locale}/categoria/{slug}, nunca para uma âncora"
    - "view_item_list sai uma vez por montagem da grade, com as 5 categorias na ordem renderizada e sem nenhum campo monetário"
  artifacts:
    - path: "src/components/blocos/SearchBarGrande.tsx"
      provides: "Composto E5 da busca grande (input sem borda + botão pretoSolido + spinner + erro)"
      contains: "'use client'"
    - path: "src/components/blocos/BuscaBloco.tsx"
      provides: "Bloco 2 da Home (Server Component que compõe o cabeçalho + SearchBarGrande)"
    - path: "src/components/blocos/GradeDeCategoriasBloco.tsx"
      provides: "Bloco 3 da Home — card-bandeira LED + 4 cards + emissão de view_item_list"
      min_lines: 180
  key_links:
    - from: "src/components/blocos/SearchBarGrande.tsx"
      to: "next/navigation"
      via: "useRouter().push para /{locale}/catalogo?q="
      pattern: "catalogo\\?q="
    - from: "src/components/blocos/GradeDeCategoriasBloco.tsx"
      to: "src/components/analytics/EmissorViewItemList.tsx"
      via: "componente de emissão de evento"
      pattern: "EmissorViewItemList"
---

<objective>
Entregar o Bloco 2 (busca grande) e o Bloco 3 (grade de categorias com card-bandeira LED), incluindo a extensão
E5 do design system e a **primeira emissão real de `view_item_list`** (HOME-05).

Propósito: estes dois blocos são a porta de entrada de navegação da Home. A busca é o composto mais interativo
da fase (validação, estado pendente, navegação) e será reusada no Catálogo (Fase 5). A grade de categorias é
onde o item 6 de `docs/00-divergencias.md` termina de fechar: os cards apontam para os slugs reais
(`estruturas`, `telas-de-led`, `luz-e-som`, `tendas`, `moveis`), não para as âncoras `#led`/`#luzsom`.

Saída: `SearchBarGrande` (client), `BuscaBloco` (server), `GradeDeCategoriasBloco` (server) com
`EmissorViewItemList`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-home/04-UI-SPEC.md
@.planning/phases/04-home/04-PATTERNS.md
@.planning/phases/04-home/04-01-SUMMARY.md
@.planning/phases/04-home/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchBarGrande (E5) e BuscaBloco — Bloco 2</name>
  <files>src/components/blocos/SearchBarGrande.tsx, src/components/blocos/SearchBarGrande.test.tsx, src/components/blocos/BuscaBloco.tsx</files>
  <read_first>
    - `src/lib/cms/schemas.ts` linhas 196-200 — `blocoBusca`: só `titulo`, `subtitulo`, `placeholder`. Nenhuma lógica de busca vem do CMS.
    - `src/components/primitives/Field.tsx` linhas 10-34 (o `campoBase` com `border: 1px solid borda` que precisa de override local) e linhas 96-103 (`MensagemErro`, com `role="alert"` e ícone quadrado já embutidos — reusar direto, não recriar).
    - `src/components/primitives/Button.tsx` — a variante `pretoSolido` criada no plano 04-02.
    - `src/components/feedback/Spinner.tsx` — criado no plano 04-02.
    - `src/components/product/ProductCard.tsx` linhas 168-200 — padrão de `useState` local em componente-folha, sem Redux.
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Bloco 2 — Busca grande" (specs completas) e "Copywriting Contract" (texto de erro exato).
  </read_first>
  <action>
**`src/components/blocos/SearchBarGrande.tsx`** — `'use client'`. Vive em `src/components/blocos/`, **não** em
`primitives/`: é composição de primitivos, não um primitivo novo.

Props: `{ locale: Locale; placeholder?: string | null }`.

Estado: `const [termo, setTermo] = useState('')`, `const [erro, setErro] = useState(false)` e
`const [pendente, iniciarTransicao] = useTransition()` (de `react`). Usar `useTransition` — **não** `setTimeout`
de 1400ms como o layout-fonte, que era simulação. `pendente` é o estado real de navegação e some sozinho.

Estrutura:
- `<form noValidate action={`/${locale}/catalogo`} method="get" onSubmit={...}>`. O `action`/`method` nativos são
  o caminho sem JS (navegação GET com `?q=`); o `onSubmit` faz `e.preventDefault()` e assume quando há JS.
- No `onSubmit`: se `termo.trim() === ''` → `setErro(true)` e **não navegar**. Senão → `setErro(false)` e
  `iniciarTransicao(() => router.push(`/${locale}/catalogo?q=${encodeURIComponent(termo.trim())}`))`,
  com `router` de `useRouter()` de `next/navigation`. `encodeURIComponent` é obrigatório.
- Caixa: `styled.div` com `display: flex; border: 1px solid ${theme.cor.tinta900}; background: ${theme.cor.branco};
  border-radius: ${theme.raio.base};`. A borda é da caixa, não do input.
- `Input` (de `Field.tsx`) com `type="search"`, `name="q"`, `flex: 1`, `min-width: 200px`, `padding: 16px`,
  `font-size: theme.tamanho[16]`, e override obrigatório: `border: none; border-radius: 0;` porque `campoBase`
  traz `border: 1px solid borda` e ficaria borda dentro de borda. Foco: `&:focus-visible { outline: 2px solid
  ${theme.cor.teal}; outline-offset: -2px; }` — offset **negativo**, para dentro, já que o input não tem borda
  própria. Placeholder: `placeholder ?? 'Busque por mesa, capa, guarda-sol, painel de LED...'`.
  `aria-invalid={erro}` e, quando `erro`, `aria-describedby` apontando para o id da mensagem.
- `Button type="submit" $variante="pretoSolido"` com `border-left: 1px solid ${theme.cor.tinta900}; padding: 16px
  28px; border-radius: 0;`, `disabled={pendente}`, rótulo `pendente ? 'BUSCANDO' : 'BUSCAR'` e, quando
  `pendente`, um `<Spinner />` ao lado do rótulo (`display: inline-flex; gap: 8px; align-items: center`).
- Quando `erro`, renderizar `<MensagemErro id="erro-busca-home">` com o texto **exato**: `Digite um produto,
  equipamento ou solução para buscar.` — nada de "tente de novo", que não existe no layout-fonte. `margin-top:
  10px` no wrapper.

**`src/components/blocos/BuscaBloco.tsx`** — Server Component (sem `'use client'`), contrato de props do plano
04-03: `{ bloco: Extract<Bloco, { __component: 'blocos.busca' }>; locale: Locale }`.
- `styled.section` com `background: theme.cor.fundo`.
- `Container` com `padding-block: clamp(48px, 6vw, 64px)`.
- Grid interno: `display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 32px;
  align-items: end;`.
- Coluna esquerda: `<Heading as="h2" $nivel="h2">` com `bloco.titulo ?? 'Encontre o que seu evento precisa'`,
  `margin-bottom: 12px`; parágrafo com `bloco.subtitulo`, `font-size: theme.tamanho[17]`, `line-height: 1.55`,
  `color: theme.cor.tinta600`, `max-width: 52ch`. **Usar `theme.cor.tinta600` explícito, não `Body $mid`** — a
  prop `$mid` aponta para `theme.cor.textoMid` (`#5A5F61`), e o layout-fonte usa `#4A4E50` (`tinta600`) nos
  parágrafos de introdução de seção. Essa é a nota de fidelidade do UI-SPEC e vale para todos os blocos claros.
- Coluna direita: `<SearchBarGrande locale={locale} placeholder={bloco.placeholder} />`.
- Fechar com `<SectionDivider $claro />` **dentro** do `Container` (variante clara = com a prop booleana
  `$claro`; não existe prop `variante`).

**`SearchBarGrande.test.tsx`** com `renderComProviders` + `userEvent`, mockando `next/navigation`
(`jest.mock('next/navigation', () => ({ useRouter: () => ({ push: mockPush }) }))`):
1. submeter vazio → `mockPush` não é chamado e o texto "Digite um produto, equipamento ou solução para buscar." aparece com `role="alert"`.
2. digitar "painel de led" e submeter → `mockPush` chamado com `'/pt-BR/catalogo?q=painel%20de%20led'`.
3. digitar termo só com espaços → não navega, mostra erro.
4. depois de um submit válido, o erro anterior desaparece (submeter vazio, depois preencher e submeter).
5. `axe` sem violações.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/SearchBarGrande.test.tsx && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/SearchBarGrande.test.tsx` passa com 5 testes.
    - `grep -c "Digite um produto, equipamento ou solução para buscar." src/components/blocos/SearchBarGrande.tsx` retorna 1.
    - `grep -c "encodeURIComponent" src/components/blocos/SearchBarGrande.tsx` retorna 1.
    - `grep -c "useTransition" src/components/blocos/SearchBarGrande.tsx` retorna 1 e `grep -c "setTimeout" src/components/blocos/SearchBarGrande.tsx` retorna 0.
    - `grep -c "outline-offset: -2px" src/components/blocos/SearchBarGrande.tsx` retorna 1.
    - `grep -c "pretoSolido" src/components/blocos/SearchBarGrande.tsx` retorna 1.
    - `grep -c "clamp(48px, 6vw, 64px)" src/components/blocos/BuscaBloco.tsx` retorna 1.
    - `grep -c "SectionDivider \$claro" src/components/blocos/BuscaBloco.tsx` retorna 1.
    - `grep -c "Body \$mid" src/components/blocos/BuscaBloco.tsx` retorna 0.
    - `grep -Ec "use client" src/components/blocos/BuscaBloco.tsx` retorna 0.
  </acceptance_criteria>
  <done>A busca grande valida campo vazio com a cópia exata do layout, navega para `/{locale}/catalogo?q=` com o termo codificado, mostra BUSCANDO + spinner enquanto a navegação está pendente, e funciona sem JS pelo `action`/`method` nativos.</done>
</task>

<task type="auto">
  <name>Task 2: GradeDeCategoriasBloco — cabeçalho e card-bandeira LED</name>
  <files>src/components/blocos/GradeDeCategoriasBloco.tsx</files>
  <read_first>
    - `src/lib/cms/schemas.ts` linha 202 — `blocoGradeDeCategorias` usa `cabecalhoDeSecao` (só `eyebrow`, `titulo`, `subtitulo`). **Não há campo `destaque` no schema de categoria.**
    - `src/lib/cms/adapters.ts` linhas 277-318 — tipo `Categoria` (`id`, `slug`, `nome`, `descricao`, `subcategorias`, `hero: Imagem | null`, `ordem`, `produtos`, `seo`) e `getCategorias(locale)`, que já ordena por `ordem:asc`.
    - `src/components/media/ImagePlaceholder.tsx` linhas 30-47 — props `legenda`, `variante`, `ratio` (default `'4 / 3'`).
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Bloco 3 — Grade de categorias", em especial a tabela "Diferença exata card-bandeira vs. card padrão".
    - `src/components/blocos/HeroBloco.tsx` (plano 04-03) — contrato de props e padrão de seção.
  </read_first>
  <action>
Criar `src/components/blocos/GradeDeCategoriasBloco.tsx` — **Server Component**.

Props: `{ bloco: Extract<Bloco, { __component: 'blocos.grade-de-categorias' }>; locale: Locale; categorias: Categoria[] }`.
As categorias vêm por prop porque o componente do CMS não as carrega (só traz o cabeçalho da seção); quem busca
é `page.tsx` (plano 04-07), via `getCategorias(locale)`.

**Seleção do card-bandeira:** `const bandeira = categorias.find((c) => c.slug === 'telas-de-led')` e
`const padrao = categorias.filter((c) => c.slug !== 'telas-de-led')`. Identificação por **slug**, porque não
existe campo `destaque` no schema de categoria. Comentar isso no código para ninguém procurar o campo. Se
`bandeira` for `undefined` (categoria não publicada), renderizar só a grade dos padrão — sem buraco, sem erro.

**Seção e cabeçalho:**
- `styled.section` com `background: theme.cor.fundo`; `Container` com `padding-block: clamp(64px, 9vw, 144px)`.
- `<Eyebrow>` (sem `$sobreEscuro` — fundo claro) com `bloco.eyebrow ?? 'Catálogo'`.
- `<Heading as="h2" $nivel="h2">` com `bloco.titulo ?? 'Explore nossas categorias'`, `margin-bottom: 12px`.
- Parágrafo com `bloco.subtitulo`: `font-size: theme.tamanho[17]`, `line-height: 1.55`,
  `color: theme.cor.tinta600`, `max-width: 58ch`, `margin-bottom: 40px`.

**Card-bandeira LED** — `<Link href={`/${locale}/categoria/${bandeira.slug}`}>` (de `next/link`) estilizado,
`margin-bottom: 24px`, `display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));`
(as duas "colunas" são imagem + conteúdo; empilha fluido, sem `@media`).
- `background: theme.cor.tinta900; color: theme.cor.fundo; border: 1px solid ${theme.cor.tinta900};
  border-radius: ${theme.raio.base}; overflow: hidden;`
- `&:hover { border-color: ${theme.cor.teal}; }` e
  `&:focus-visible { outline: 2px solid ${theme.cor.tealLink}; outline-offset: 3px; }`
- Imagem: wrapper `position: relative; aspect-ratio: 16 / 9;` com `next/image` `fill`, `style={{ objectFit:
  'cover' }}`, `alt={bandeira.hero.alt}`, `sizes="(max-width: 760px) 100vw, 50vw"`. Sem `hero`, renderizar
  `<ImagePlaceholder legenda={`FOTO · ${bandeira.nome}`} ratio="16 / 9" />` — nunca imagem inventada.
- Conteúdo: `padding: clamp(24px, 3vw, 40px); display: flex; flex-direction: column; gap: 12px;`
  - `<Eyebrow $sobreEscuro>Produto-bandeira</Eyebrow>` (teal, porque é fundo escuro — extensão E1).
  - Título: `<Heading as="h3" $nivel="h3">{bandeira.nome}</Heading>`. **Nota de fidelidade:** o layout-fonte usa
    `clamp(28px, 3vw, 40px)` e o token `h3` do tema é `clamp(24px, 2.8vw, 30px)`. Usar o **token do tema** e não
    inventar `clamp` novo — decisão registrada no UI-SPEC; anotar como comentário no código.
  - Descrição: `bandeira.descricao` com `font-size: theme.tamanho[17]`, `line-height: 1.5`,
    `color: theme.cor.superficie200`, `max-width: 44ch`.
  - Link visual "VER {NOME} →": **não** é `Button`; é um span dentro do mesmo `Link`, com
    `font-family: theme.fonte.display; font-weight: theme.peso.display; font-size: theme.tamanho[15];
    text-transform: uppercase; color: ${theme.cor.teal}; display: inline-flex; gap: 10px;` e a seta em
    `<span aria-hidden="true">→</span>`.
- **Um único elemento focável por card:** o card inteiro é o `Link`. Não colocar um segundo `<a>` dentro (o
  "VER ... →" é texto, não link), senão o `Tab` para duas vezes no mesmo destino.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slug === 'telas-de-led'" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 2 (find e filter).
    - `grep -c "clamp(64px, 9vw, 144px)" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -c "repeat(auto-fit, minmax(320px, 1fr))" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -c "clamp(24px, 3vw, 40px)" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -c "sobreEscuro" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -c "44ch" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -Ec "use client|useState|useEffect" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 0.
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>O card-bandeira LED renderiza escuro, com eyebrow teal, imagem 16/9 (ou placeholder), descrição em `superficie200` e o CTA textual em teal, apontando para `/{locale}/categoria/telas-de-led`, e a ausência da categoria não quebra o bloco.</done>
</task>

<task type="auto">
  <name>Task 3: Os 4 cards padrão, grade fluida e emissão de view_item_list</name>
  <files>src/components/blocos/GradeDeCategoriasBloco.tsx, src/components/blocos/GradeDeCategoriasBloco.test.tsx</files>
  <read_first>
    - `src/components/blocos/GradeDeCategoriasBloco.tsx` — o arquivo criado na Task 2, para continuar dentro dele.
    - `src/components/analytics/EmissorViewItemList.tsx` (plano 04-01) — props `{ listaId, listaNome, itens }` e o tipo `ItemDeListaGA4` (campos permitidos: `item_id`, `item_name`, `item_category`, `item_variant`, `index`).
    - `src/components/product/ProductCard.tsx` linhas 94-120 — os estilos `Nome` (Public Sans 500, 22px, line-height 1.3) e `Descricao` (15px, `tinta600`), que os cards de categoria replicam.
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Bloco 3", bloco "4 cards padrão", e a seção "Eventos desta fase (`view_item_list`)".
    - `.planning/phases/04-home/04-PLAN-OUTLINE.md` seção "Q1" — a decisão de grid que esta tarefa aplica.
  </read_first>
  <action>
**Grade dos 4 cards padrão** — `display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
gap: 24px;`.

**Decisão Q1 aplicada aqui (comentar no código com a justificativa):** o layout-fonte calcula
`grid-template-columns` em JS a partir de `window.innerWidth` (1 coluna `<760px`, 2 `<1180px`, 4 `≥1180px`).
Isso é exatamente o padrão que D1 de `docs/divergencias.md` rejeitou (mismatch de hidratação + CLS), e a fase
proíbe `@media` nova. Substituído por `auto-fit/minmax(260px, 1fr)`, que é o mesmo padrão já usado no
card-bandeira desta seção e no estado "carregando" do Bloco 8 no próprio layout-fonte. Custo aceito: em larguras
intermediárias o número de colunas pode diferir em ±1 dos degraus originais — registrado como divergência D3 no
plano 04-07.

**Cada card padrão** — `<Link href={`/${locale}/categoria/${cat.slug}`}>` estilizado:
- `display: flex; flex-direction: column; background: ${theme.cor.branco}; border: 1px solid ${theme.cor.borda};
  border-radius: ${theme.raio.base}; overflow: hidden;`
- `&:hover { border-color: ${theme.cor.tinta900}; }` (→ `tinta900`, diferente do bandeira, que vai a `teal`)
- `&:focus-visible { outline: 2px solid ${theme.cor.tealLink}; outline-offset: 3px; }`
- Imagem: wrapper `position: relative; aspect-ratio: 16 / 9; border-bottom: 1px solid ${theme.cor.borda};` com
  `next/image` `fill` + `objectFit: 'cover'` + `sizes="(max-width: 760px) 100vw, 25vw"`; sem `hero`,
  `<ImagePlaceholder legenda={`FOTO · ${cat.nome}`} ratio="16 / 9" />`.
- Corpo: `padding: 24px` (**fixo**, não `clamp` — diferente do bandeira), `display: flex; flex-direction: column;
  gap: 12px; flex: 1;`
  - Nome: elemento `h3` com `font-family: theme.fonte.corpo; font-weight: theme.peso.medio;
    font-size: theme.tamanho[22]; line-height: 1.3; color: theme.cor.tinta900;` — **não** usar o primitivo
    `Heading` (que é display/uppercase). É o mesmo padrão do `Nome` do `ProductCard`.
  - Descrição: `cat.descricao` com `font-size: theme.tamanho[15]; line-height: 1.5;
    color: theme.cor.tinta600; flex: 1;`
  - CTA textual "VER {NOME} →": `font-family: theme.fonte.display; font-weight: theme.peso.display;
    font-size: theme.tamanho[14]` (**14px**, 1px menor que o do bandeira), `color: ${theme.cor.tealLink}`
    (**tealLink**, não `teal` — o card é sobre fundo claro), `gap: 8px`, seta com `aria-hidden`.

**Emissão de `view_item_list` (HOME-05).** No fim do JSX da seção, renderizar:
`<EmissorViewItemList listaId="home_categorias" listaNome="Categorias — Home" itens={itensDoEvento} />`
onde `itensDoEvento` é construído **na ordem renderizada** (bandeira primeiro, se existir, depois os 4 padrão):
`[...(bandeira ? [bandeira] : []), ...padrao].map((c, i) => ({ item_id: c.slug, item_name: c.nome, index: i }))`.
Nada além de `item_id`, `item_name` e `index`. **Nunca** `price`, `value`, `currency`, `revenue`, `quantity`.
Comentar que a lista é de categorias, então `item_category` fica de fora por redundância.

`EmissorViewItemList` é `'use client'` e retorna `null` — pode ser renderizado dentro de um Server Component sem
transformar o bloco em client. Não adicionar `'use client'` neste arquivo.

**`GradeDeCategoriasBloco.test.tsx`** com `renderComProviders` e `jest.mock('@/lib/analytics/dataLayer')` no
topo (`const emitir = jest.mocked(emitirEvento)`). **Nunca** inspecionar `window.dataLayer` num teste de
componente: a guarda `src/__tests__/guards/dataLayer-porta-unica.test.ts` tem allowlist fechada e falharia, e
`eslint.config.mjs` não deve ser editado por este plano (planos paralelos brigariam pelo arquivo). Montar 5
categorias fake, incluindo `telas-de-led` e uma sem `hero`:
1. O card-bandeira aparece com o eyebrow "Produto-bandeira" e `href="/pt-BR/categoria/telas-de-led"`; existem 4 links restantes, um por categoria padrão, com os `href` de slug corretos.
2. Categoria sem `hero` renderiza o placeholder (`getByRole('img', { name: /FOTO · / })` ou `getByLabelText`), não uma `<img>` quebrada.
3. Sem a categoria `telas-de-led` na lista, o bloco renderiza 4 cards e nenhum bandeira, sem lançar.
4. `emitir` é chamado **exatamente 1** vez, com `event: 'view_item_list'`, `item_list_id: 'home_categorias'`, `items.length === 5`, `items[0].item_id === 'telas-de-led'` e `items[0].index === 0`; e `Object.keys(items[0])` não contém `price`/`value`/`currency`/`quantity`.
5. `axe` sem violações.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/GradeDeCategoriasBloco.test.tsx src/__tests__/guards && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/GradeDeCategoriasBloco.test.tsx` passa com 5 testes, incluindo o do evento.
    - `grep -c "window.dataLayer" src/components/blocos/GradeDeCategoriasBloco.test.tsx` retorna 0 e `git diff --name-only` não lista `eslint.config.mjs` nem `src/__tests__/guards/dataLayer-porta-unica.test.ts`.
    - `grep -c "repeat(auto-fit, minmax(260px, 1fr))" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -c "window.innerWidth" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 0 e `grep -c "@media" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 0.
    - `grep -c "home_categorias" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1.
    - `grep -Ec "price|currency|revenue|value:" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 0.
    - `grep -c "tamanho\[14\]" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 1 (CTA do card padrão) e `grep -c "tamanho\[15\]" src/components/blocos/GradeDeCategoriasBloco.tsx` retorna 2 (CTA do bandeira + descrição do card padrão).
    - `npx jest src/__tests__/guards` continua verde **sem** nenhuma mudança na allowlist.
  </acceptance_criteria>
  <done>Os 4 cards padrão renderizam em grade fluida sem JS de viewport, cada um linkando o slug real da categoria, e a montagem da grade emite exatamente um `view_item_list` com as 5 categorias na ordem visual e sem nenhum campo monetário.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante → URL (`?q=`) | O termo digitado passa a fazer parte da URL de destino |
| Strapi → `href` de card | `categoria.slug` compõe o caminho `/{locale}/categoria/{slug}` |
| navegador → `window.dataLayer` | Nomes e slugs de categoria saem para a fila de eventos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering (XSS refletido) | `SearchBarGrande` | mitigate | O termo nunca é renderizado de volta na Home (o bloco não exibe "você buscou por..."), e a navegação usa `encodeURIComponent` antes de compor `?q=`. Nenhum `dangerouslySetInnerHTML`. A exibição do termo no Catálogo é responsabilidade da Fase 5, que herda a mesma regra |
| T-04-14 | Tampering (path traversal / open redirect) | `href` dos cards de categoria | mitigate | O `href` é sempre `/${locale}/categoria/${slug}` com `slug` validado por Zod na borda (`categoriaColecao`) e vindo de um campo `uid` do Strapi. O componente nunca usa uma URL absoluta vinda do CMS neste bloco |
| T-04-15 | Information disclosure | payload de `view_item_list` | mitigate | Só `item_id` (slug), `item_name` (nome público da categoria) e `index`. Nenhum dado pessoal é representável no tipo, e nenhum campo monetário existe (verificado por `grep` no critério de aceite) |
| T-04-16 | Denial of service | submits repetidos da busca | mitigate | `disabled={pendente}` no botão durante a transição impede rajada de navegações pelo mesmo clique |
| T-04-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `npm run check` verde.
- `npx jest src/components/blocos` passa com os 10 testes deste plano.
- Nenhum arquivo do plano contém `@media`, `window.innerWidth` ou `setTimeout`.
- `BuscaBloco.tsx` e `GradeDeCategoriasBloco.tsx` não contêm `'use client'`; só `SearchBarGrande.tsx` contém.
- `npx jest src/__tests__/guards` verde sem mudança de allowlist; nenhum teste deste plano menciona `window.dataLayer`.
</verification>

<success_criteria>
- Bloco 2 e Bloco 3 existem, seguem o contrato de props `{ bloco, locale, ... }` e são Server Components, com a fronteira client só no composto de busca.
- A busca valida, navega para a rota final com o termo codificado e mostra estado pendente real (não simulado).
- As 5 categorias reais aparecem com o tratamento de bandeira por slug, apontando para os slugs reais — item 6 de `docs/divergencias.md` fechado na Home.
- `view_item_list` é emitido uma vez por montagem, com payload auditável e sem campo monetário.
- Grade sem JS de viewport e sem `@media` nova (decisão Q1).
</success_criteria>

<output>
Criar `.planning/phases/04-home/04-04-SUMMARY.md` ao concluir.
</output>
