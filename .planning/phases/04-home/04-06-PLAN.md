---
phase: 04-home
plan: 06
type: execute
wave: 2
depends_on: ['04-02']
files_modified:
  - src/components/blocos/ComoFuncionaBloco.tsx
  - src/components/blocos/ComoFuncionaBloco.test.tsx
  - src/components/blocos/DiferenciaisBloco.tsx
  - src/components/blocos/DiferenciaisBloco.test.tsx
  - src/components/blocos/AvaliacoesBloco.tsx
  - src/components/blocos/AvaliacoesBloco.test.tsx
  - src/components/showcase/Showcase.tsx
autonomous: true
requirements: [HOME-03, HOME-04]

must_haves:
  truths:
    - "O visitante vê as 4 etapas numeradas de 'Monte seu orçamento em quatro etapas' com o aviso de não-reserva"
    - "O visitante vê os 5 diferenciais em uma grade cujas divisórias são linhas de 1px"
    - "Com o CMS sem avaliação publicada, o visitante vê o estado vazio real com a cópia 'Publicamos apenas avaliações reais de clientes.' — nunca depoimento inventado"
    - "Com avaliações publicadas, cada uma mostra nota formatada pelo locale, citação, nome, empresa (omitida se vazia), cidade e tipo de evento"
    - "O esqueleto de avaliação existe, tem 3 cards com as larguras do layout, aparece na showcase e é coberto por teste"
  artifacts:
    - path: "src/components/blocos/ComoFuncionaBloco.tsx"
      provides: "Bloco 6 da Home — 4 etapas + Notice de não-reserva"
    - path: "src/components/blocos/DiferenciaisBloco.tsx"
      provides: "Bloco 7 da Home — grade de 5 diferenciais com divisórias de 1px"
      contains: "gap: 1px"
    - path: "src/components/blocos/AvaliacoesBloco.tsx"
      provides: "Bloco 8 da Home — estados cheio, vazio e carregando"
      min_lines: 220
  key_links:
    - from: "src/components/blocos/AvaliacoesBloco.tsx"
      to: "src/components/feedback/Skeleton.tsx"
      via: "reuso de SkeletonBar nas 4 barras de cada card-esqueleto"
      pattern: "SkeletonBar"
    - from: "src/components/showcase/Showcase.tsx"
      to: "src/components/blocos/AvaliacoesBloco.tsx"
      via: "AvaliacaoSkeleton exibido na vitrine do design system"
      pattern: "AvaliacaoSkeleton"
---

<objective>
Entregar os três blocos de prova e processo da Home — Bloco 6 (como funciona), Bloco 7 (diferenciais) e Bloco 8
(avaliações, com os três estados) — fechando HOME-03.

Propósito: HOME-03 tem uma sutileza que o requisito foi reescrito para registrar. O **estado vazio** das
avaliações é garantia de produção: com o CMS recém-instalado e sem depoimento publicado, é o que o visitante vê
de fato, e por isso recebe a mesma especificação detalhada que o estado cheio. O **estado carregando** é
componente testável, **não** garantia de produção: sem `cacheComponents` e com busca em Server Component, o
`<Suspense>` resolve no próprio prerender e o visitante nunca vê o fallback. Ele existe, aparece na showcase e é
coberto por teste — e o aceite não promete que seja visto.

Regra inviolável reforçada aqui: **nunca semear nem renderizar depoimento fictício.** Os 4 nomes do HTML-fonte
(Marina Alcântara, Rodrigo Beltrão, Camila Ferreira, Diego Nascimento) são exemplos de design, fechados como
INTENCIONAL no item 13 de `docs/00-divergencias.md`. Nenhum deles entra no código, nem como fixture de produção
(fixture de teste é permitida e deve usar nomes obviamente fictícios).

Saída: `ComoFuncionaBloco`, `DiferenciaisBloco`, `AvaliacoesBloco` (cheio/vazio/carregando) e o esqueleto na
showcase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-home/04-UI-SPEC.md
@.planning/phases/04-home/04-PATTERNS.md
@.planning/phases/04-home/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComoFuncionaBloco e DiferenciaisBloco — Blocos 6 e 7</name>
  <files>src/components/blocos/ComoFuncionaBloco.tsx, src/components/blocos/ComoFuncionaBloco.test.tsx, src/components/blocos/DiferenciaisBloco.tsx, src/components/blocos/DiferenciaisBloco.test.tsx</files>
  <read_first>
    - `src/lib/cms/schemas.ts` linhas 217-242 — `blocoComoFunciona` (`titulo`, `passos: { titulo, texto }[]`, `aviso`) e `blocoDiferenciais` (`titulo`, `itens: { titulo, texto }[]`).
    - `src/components/feedback/Notice.tsx` linhas 10 e 59-70 — `VarianteAviso` (`'padrao' | 'neutro' | 'escuro'`) e `NoticeProps` (`rotulo`, `children`, `variante`). A variante `padrao` é a faixa `superficie150` + `border-left: 2px solid tealLink` que o UI-SPEC pede.
    - `src/components/product/ProductCard.tsx` linhas 101-120 — os estilos `Nome` (Public Sans 500, 22px/1.3) e `Descricao` (15px, `tinta600`), replicados nos títulos de etapa e de diferencial.
    - `.planning/phases/04-home/04-UI-SPEC.md` seções "Bloco 6 — Como funciona" e "Bloco 7 — Diferenciais" (copy exata das 4 etapas e dos 5 diferenciais, e o detalhe do padding fixo de 64px).
    - `src/components/blocos/GradeDeCategoriasBloco.tsx` (plano 04-04) — contrato de props e padrão de seção clara já usado na fase.
  </read_first>
  <action>
**`src/components/blocos/ComoFuncionaBloco.tsx`** — Server Component. Props:
`{ bloco: Extract<Bloco, { __component: 'blocos.como-funciona' }> }` (não precisa de `locale`: não há href aqui).
- `styled.section` com `background: theme.cor.fundo`; `Container` com `padding-block: clamp(64px, 9vw, 144px)`.
- `<Heading as="h2" $nivel="h2">` com `bloco.titulo ?? 'Monte seu orçamento em quatro etapas'`,
  `margin-bottom: 40px`.
- `<ol>` com `list-style: none; margin: 0 0 40px; padding: 0; display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 24px;`
- Cada `<li>` de `bloco.passos ?? []`: `border-top: 2px solid ${theme.cor.tinta900}; padding-top: 20px;`
  - Número (índice + 1): `font-family: theme.fonte.display; font-variation-settings: 'wdth' 75;
    font-weight: theme.peso.display; font-size: 44px; line-height: 1; color: ${theme.cor.tealLink};
    margin-bottom: 16px;` — cor `tealLink`, **não** `teal` (é fundo claro).
  - Título do passo: `h3` com `font-family: theme.fonte.corpo; font-weight: theme.peso.medio;
    font-size: theme.tamanho[22]; line-height: 1.3; margin-bottom: 8px;` — **não** o primitivo `Heading`.
  - Texto: `font-size: theme.tamanho[15]; line-height: 1.5; color: ${theme.cor.tinta600};`
- **Aviso final:** `<Notice rotulo="AVISO" variante="padrao">` com `max-width: 840px` no wrapper e o conteúdo
  `bloco.aviso ?? 'Os produtos não ficam reservados ao serem adicionados ao carrinho. A disponibilidade será confirmada pela equipe.'`
  **Nota de correção ao UI-SPEC (registrar como comentário no código e no SUMMARY):** o UI-SPEC diz que este texto
  vem de `settings-globais`, mas o `settingsGlobaisSchema` real (`src/lib/cms/schemas.ts` linhas 53-65) **não tem
  campo de microcopy legal**, enquanto `blocos.como-funciona` **tem** o campo `aviso`. A fonte correta é
  `bloco.aviso`, com o texto do layout-fonte como fallback. Não inventar campo no schema para "obedecer" a spec.
- Fecha com `<SectionDivider $claro />` dentro do `Container`.

**`src/components/blocos/DiferenciaisBloco.tsx`** — Server Component. Props:
`{ bloco: Extract<Bloco, { __component: 'blocos.diferenciais' }> }`.
- `styled.section` com `background: theme.cor.fundo`; `Container` com **`padding: 64px 20px` fixo, não
  `clamp()`** — é o único bloco da Home sem padding fluido, e é assim no layout-fonte. Comentário obrigatório:
  "Fixo de propósito — o layout-fonte não usa `clamp()` neste bloco. Não 'corrigir' para fluido."
  (Se o `Container` já aplica `padding-left/right: 20px`, aplicar só `padding-block: 64px` e comentar isso.)
- `<Heading as="h2" $nivel="h2">` com `bloco.titulo ?? 'Estrutura e suporte para seu evento'`,
  `margin-bottom: 32px`.
- Grade: `display: grid; grid-template-columns: repeat(auto-fit, minmax(230px, 1fr)); gap: 1px;
  background: ${theme.cor.borda}; border: 1px solid ${theme.cor.borda};`
  Comentário obrigatório: "O efeito de divisórias finas vem do `gap: 1px` sobre o fundo cinza da grade — as
  células não têm borda individual. Não trocar por `border` por célula, o resultado visual muda (bordas duplas)."
- Cada célula de `bloco.itens ?? []`: `background: ${theme.cor.fundo}; padding: 24px;`
  - Título: mesmo padrão de `h3` Public Sans 500 22px/1.3, `margin-bottom: 8px`.
  - Texto: 15px/1.5, `color: ${theme.cor.tinta600}`.
- Fecha com `<SectionDivider $claro />` dentro do `Container`.

**Testes.** Em cada arquivo, com `renderComProviders`:
- `ComoFuncionaBloco.test.tsx`: (1) com 4 passos → os 4 títulos aparecem e a numeração vai de 1 a 4;
  (2) sem `aviso` no CMS → o texto de fallback exato aparece dentro de um `Notice`;
  (3) com `aviso` do CMS → o texto do CMS vence; (4) `passos` vazio/ausente → não lança e o H2 continua;
  (5) `axe` sem violações.
- `DiferenciaisBloco.test.tsx`: (1) com 5 itens → os 5 títulos aparecem; (2) `itens` ausente → não lança;
  (3) `axe` sem violações.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/ComoFuncionaBloco.test.tsx src/components/blocos/DiferenciaisBloco.test.tsx && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/ComoFuncionaBloco.test.tsx` passa com 5 testes; `DiferenciaisBloco.test.tsx` com 3.
    - `grep -c "Os produtos não ficam reservados ao serem adicionados ao carrinho." src/components/blocos/ComoFuncionaBloco.tsx` retorna 1.
    - `grep -c "minmax(240px, 1fr)" src/components/blocos/ComoFuncionaBloco.tsx` retorna 1.
    - `grep -c "clamp(64px, 9vw, 144px)" src/components/blocos/ComoFuncionaBloco.tsx` retorna 1.
    - `grep -c "tealLink" src/components/blocos/ComoFuncionaBloco.tsx` retorna 1 (o número da etapa).
    - `grep -c "gap: 1px" src/components/blocos/DiferenciaisBloco.tsx` retorna 1 e `grep -c "minmax(230px, 1fr)" src/components/blocos/DiferenciaisBloco.tsx` retorna 1.
    - `grep -c "clamp(" src/components/blocos/DiferenciaisBloco.tsx` retorna 0 — o padding deste bloco é fixo de propósito.
    - `grep -Ec "use client|@media" src/components/blocos/ComoFuncionaBloco.tsx src/components/blocos/DiferenciaisBloco.tsx` retorna 0 em cada arquivo.
  </acceptance_criteria>
  <done>Bloco 6 renderiza as 4 etapas numeradas com o aviso de não-reserva vindo de `bloco.aviso`, e Bloco 7 renderiza os 5 diferenciais em grade com divisórias de 1px e padding fixo de 64px, ambos degradando sem lançar quando a lista vem vazia.</done>
</task>

<task type="auto">
  <name>Task 2: AvaliacoesBloco — estados cheio e vazio (extensão E6 por composição local)</name>
  <files>src/components/blocos/AvaliacoesBloco.tsx, src/components/blocos/AvaliacoesBloco.test.tsx</files>
  <read_first>
    - `src/lib/cms/schemas.ts` linha 204 (`blocoAvaliacoes` usa `cabecalhoDeSecao`) e linhas 152-165 (`avaliacaoSchema`: `nome`, `empresa`, `cidade`, `tipoDeEvento`, `nota: number | null`, `texto`, `verificada`, `publicada`).
    - `src/lib/cms/adapters.ts` linhas 370-395 — `interface Avaliacao` e `getAvaliacoes(limite)`, que já filtra `publicada`.
    - `src/components/feedback/EmptyState.tsx` (arquivo inteiro, 52 linhas) — em especial a `Caixa` (linhas 10-18): borda, `background`, `border-radius` e padding que o estado vazio deste bloco replica.
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Bloco 8 — Avaliações", subseções "Estado cheio" e "Estado vazio" (copy exata, incluindo a caixa tracejada "ESTRUTURA DA AVALIAÇÃO").
    - `.planning/phases/04-home/04-PLAN-OUTLINE.md` seção "Q4" — a decisão de E6 (rota 2, composição local).
  </read_first>
  <action>
Criar `src/components/blocos/AvaliacoesBloco.tsx` — Server Component. Props:
`{ bloco: Extract<Bloco, { __component: 'blocos.avaliacoes' }>; locale: Locale; avaliacoes: Avaliacao[] }`.

**Cabeçalho comum:** `styled.section` com `background: theme.cor.fundo`; `Container` com
`padding-block: clamp(56px, 7vw, 96px)`; `<Eyebrow>` com `bloco.eyebrow ?? 'Avaliações'`;
`<Heading as="h2" $nivel="h2">` com `bloco.titulo ?? 'A confiança de quem já realizou eventos conosco'`,
`margin-bottom: 12px`; parágrafo com `bloco.subtitulo`, `font-size: theme.tamanho[17]`, `line-height: 1.55`,
`color: theme.cor.tinta600`, `max-width: 58ch`, `margin-bottom: 32px`.

**Ramificação:** `avaliacoes.length === 0` → estado vazio; senão → estado cheio.

**Estado cheio.** Grade: `display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px;`
— **decisão Q1** aplicada aqui igual ao Bloco 3 (o layout-fonte usava `gridQuatro` calculado por
`window.innerWidth`; substituído por `auto-fit`, sem `@media` e sem JS de viewport; custo de ±1 coluna em larguras
intermediárias registrado como divergência D3 no plano 04-07). Comentar.

Cada avaliação é um `<figure>` com `margin: 0; background: ${theme.cor.branco};
border: 1px solid ${theme.cor.borda}; border-radius: ${theme.raio.base}; padding: 24px;
display: flex; flex-direction: column; gap: 14px;`
- **Nota** (só se `avaliacao.nota !== null`): `display: flex; align-items: baseline; gap: 10px;`
  - valor: `font-family: theme.fonte.display; font-variation-settings: 'wdth' 75;
    font-weight: theme.peso.display; font-size: 28px; line-height: 1; color: ${theme.cor.tinta900};`
  - sufixo: `font-family: theme.fonte.mono; font-weight: theme.peso.medio; font-size: theme.tamanho[12];
    line-height: 1; letter-spacing: theme.tracking.rotuloForte; color: ${theme.cor.tealLink};`
  - **Formatação (fecha a pendência de i18n do UI-SPEC):** criar uma vez por render
    `const formatador = new Intl.NumberFormat(locale, { minimumFractionDigits: 1, maximumFractionDigits: 1 });`
    e usar `formatador.format(avaliacao.nota)` no valor **e** `` `/ ${formatador.format(5)}` `` no sufixo. Isso dá
    "5,0" e "/ 5,0" em `pt-BR` e "5.0" / "/ 5.0" em `en`/`es`. **Não** fixar a vírgula decimal no código.
- **Citação:** `<blockquote>` com `margin: 0; font-size: theme.tamanho[15]; line-height: 1.55;
  color: ${theme.cor.tinta750}; flex: 1; text-wrap: pretty;` contendo `avaliacao.texto`.
- **`<figcaption>`:** `border-top: 1px solid ${theme.cor.borda}; padding-top: 14px;`
  - nome: `font-size: theme.tamanho[17]; line-height: 1.3; font-weight: theme.peso.medio;
    color: ${theme.cor.tinta900}; margin-bottom: 2px;`
  - empresa: `font-size: theme.tamanho[15]; line-height: 1.4; color: ${theme.cor.tinta600};
    margin-bottom: 8px;` — **se `empresa` for vazia ou `null`, omitir a linha inteira**, sem reservar espaço em
    branco. O layout-fonte tem um exemplo com empresa vazia; a decisão do UI-SPEC é omitir.
  - cidade + tipo de evento: `font-family: theme.fonte.mono; font-size: theme.tamanho[13]; line-height: 1.4;
    letter-spacing: theme.tracking.rotulo; color: ${theme.cor.textoMuted};` em duas linhas, com `<br />` entre
    `cidade` e `tipoDeEvento`. Se um dos dois faltar, renderizar só o presente, sem `<br />` órfão.

**Estado vazio (extensão E6 — rota 2, composição local; NÃO tocar em `EmptyState.tsx`).**
Justificativa a comentar no código: `EmptyState` de coluna única já é consumido pelas Fases 5–6; adicionar prop de
layout condicional aumentaria a superfície de teste de um primitivo compartilhado por um caso de uso único.

Replicar a `Caixa` de `EmptyState.tsx` num `styled.div` local: `border: 1px solid ${theme.cor.borda};
background: ${theme.cor.branco}; border-radius: ${theme.raio.base}; padding: clamp(28px, 4vw, 48px);`
Dentro, grid de 2 colunas: `display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
gap: 32px; align-items: center;`
- **Coluna esquerda** (copy exata, não reescrever):
  - `<Eyebrow>` com `NENHUMA AVALIAÇÃO PUBLICADA`, mas com `color: ${theme.cor.textoMuted}` sobrescrito
    localmente — aqui o eyebrow é **neutro**, não teal/tealLink. Fazer o override via `styled(Eyebrow)` local, sem
    adicionar prop ao primitivo.
  - `h3` Public Sans 500 22px/1.3, `margin-bottom: 10px`: `Publicamos apenas avaliações reais de clientes.`
  - parágrafo 15px/1.5 `tinta600` `max-width: 46ch`:
    `Assim que os primeiros eventos forem entregues, as avaliações verificadas aparecem aqui, com cidade, tipo de evento e nota.`
- **Coluna direita:**
  - Caixa tracejada: `border: 1px dashed ${theme.cor.borda}; padding: 16px;
    border-radius: ${theme.raio.base};` com rótulo mono 12px `textoMuted` `ESTRUTURA DA AVALIAÇÃO` e 4 linhas
    mono 13px/1.4 `textoMuted`, nesta ordem exata: `NOME · EMPRESA`, `CIDADE · TIPO DE EVENTO`,
    `NOTA · 0,0 / 5`, `TEXTO COMPLETO DA AVALIAÇÃO`. É bloco **decorativo/explicativo** (mostra a "forma" de uma
    avaliação, não dado real) — reproduzir literalmente, inclusive o `0,0` com vírgula, que aqui é rótulo de
    exemplo e não número formatado. Marcar o conjunto com `aria-hidden="true"`? **Não** — é informação útil ao
    leitor de tela sobre o que será publicado; deixar acessível.
  - CTA: `<Button as="a" $variante="outlinePreto" $tamanho="sm" href={`/${locale}/solicitar-orcamento`}>SOLICITAR ORÇAMENTO</Button>`.
    No layout-fonte é a âncora `#solicitar`; usar a rota real, mesma decisão travada aplicada aos outros CTAs.

Fecha o bloco com `<SectionDivider $claro />` dentro do `Container`.

**`AvaliacoesBloco.test.tsx`** com `renderComProviders`. Fixtures com nomes obviamente fictícios de teste (ex.:
"Cliente Um", "Cliente Dois") — **nunca** os 4 nomes do HTML-fonte, para não plantar depoimento que pareça real:
1. `avaliacoes={[]}` → aparece `Publicamos apenas avaliações reais de clientes.`, o eyebrow
   `NENHUMA AVALIAÇÃO PUBLICADA`, as 4 linhas de `ESTRUTURA DA AVALIAÇÃO`, e o CTA com
   `href="/pt-BR/solicitar-orcamento"`; e **nenhum** `<figure>` é renderizado.
2. `avaliacoes` com 2 itens → 2 `<figure>`, e o estado vazio **não** aparece.
3. Avaliação com `nota: 5` e `locale="pt-BR"` → o texto `5,0` e o sufixo `/ 5,0` aparecem; a mesma avaliação com
   `locale="en"` → `5.0` e `/ 5.0`.
4. Avaliação com `empresa: null` → o `<figcaption>` não contém linha de empresa (afirmar por ausência de um texto
   conhecido, ou contando os parágrafos do figcaption).
5. Avaliação com `nota: null` → o bloco de nota não é renderizado e nada lança.
6. `axe` sem violações nos dois estados.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/AvaliacoesBloco.test.tsx && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/AvaliacoesBloco.test.tsx` passa com 6 testes ou mais, incluindo os dois locales da nota.
    - `grep -c "Publicamos apenas avaliações reais de clientes." src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "NENHUMA AVALIAÇÃO PUBLICADA" src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "Intl.NumberFormat" src/components/blocos/AvaliacoesBloco.tsx` retorna 1 e `grep -c "minimumFractionDigits: 1" src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "1px dashed" src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "clamp(28px, 4vw, 48px)" src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "minmax(260px, 1fr)" src/components/blocos/AvaliacoesBloco.tsx` retorna 2 (grade cheia + estado vazio em 2 colunas).
    - `grep -Ec "Marina|Rodrigo|Camila|Diego" src/components/blocos/AvaliacoesBloco.tsx src/components/blocos/AvaliacoesBloco.test.tsx` retorna 0 em cada arquivo — nenhum depoimento do layout-fonte foi copiado.
    - `git diff --name-only` **não** lista `src/components/feedback/EmptyState.tsx` (decisão E6 rota 2 respeitada).
    - `grep -Ec "use client|@media|window.innerWidth" src/components/blocos/AvaliacoesBloco.tsx` retorna 0.
  </acceptance_criteria>
  <done>Com o CMS vazio, o bloco 8 mostra o estado vazio completo em 2 colunas com a cópia exata e o CTA para a rota real; com avaliações publicadas, mostra nota formatada pelo locale, citação, nome, empresa condicional e cidade/tipo — sem tocar no primitivo `EmptyState` e sem nenhum depoimento fictício no código.</done>
</task>

<task type="auto">
  <name>Task 3: AvaliacaoSkeleton — estado carregando como componente testável, na showcase</name>
  <files>src/components/blocos/AvaliacoesBloco.tsx, src/components/blocos/AvaliacoesBloco.test.tsx, src/components/showcase/Showcase.tsx</files>
  <read_first>
    - `src/components/feedback/Skeleton.tsx` (arquivo inteiro) — `SkeletonBar` (props `$altura`, `$largura`, linhas 6-12) e o `CardEsqueleto` de `ProductCardSkeleton` (linhas 14-21), com `animation: amrPulse 1.3s ease-in-out infinite` e `animation-delay` por índice.
    - `.planning/phases/04-home/04-UI-SPEC.md` subseção "Estado 'carregando' (skeleton)" do Bloco 8 — as larguras e alturas exatas dos 3 cards.
    - `.planning/REQUIREMENTS.md` linha de **HOME-03** — o texto reescrito em 2026-08-18 que define o aceite deste estado.
    - `src/components/showcase/Showcase.tsx` linhas 85-135 — padrão de `<Secao>`, e a seção acrescentada pelo plano 04-02, para manter o estilo.
  </read_first>
  <action>
**Em `src/components/blocos/AvaliacoesBloco.tsx`**, acrescentar e **exportar** o componente
`export function AvaliacaoSkeleton()`. Ele é exportado (não interno) porque a showcase precisa importá-lo — é
exatamente o que torna HOME-03 verificável sem prometer visibilidade em produção.

Estrutura:
- Grade: `display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 24px;`
  — **280px aqui**, diferente dos 260px do estado cheio: o layout-fonte já usa `auto-fit` neste estado, com esse
  valor. Não unificar "por consistência"; é assim na fonte.
- **Exatamente 3** cards-esqueleto, nem 4. Comentário obrigatório: "3, não 4 — é a contagem do layout-fonte,
  mesmo o estado cheio mostrando 4 por linha. Não completar para 4 'para bater'."
- Cada card: `border: 1px solid ${theme.cor.borda}; background: ${theme.cor.branco}; padding: 24px;
  border-radius: ${theme.raio.base}; display: grid; gap: 12px; animation: amrPulse 1.3s ease-in-out infinite;`
  com `animation-delay` por índice no mesmo padrão de `CardEsqueleto` (`${indice * 0.09}s`). **Sem** bloco de
  foto — este esqueleto é só texto, diferente do `ProductCardSkeleton`.
- 4 `SkeletonBar` por card, reusando o export de `Skeleton.tsx`, com as medidas exatas:
  - Card 1: alturas `14px/12px/12px/12px`, larguras `40% / 60% / 100% / 85%`
  - Card 2: alturas `14px/12px/12px/12px`, larguras `35% / 55% / 100% / 70%`
  - Card 3: alturas `14px/12px/12px/12px`, larguras `45% / 50% / 95% / 80%`
  Modelar como uma constante local `const ESQUELETOS = [{ larguras: ['40%','60%','100%','85%'] }, ...]` e as
  alturas como `['14px','12px','12px','12px']` compartilhadas.
- Marcar a grade com `aria-hidden="true"` e `role` nenhum: é placeholder visual, não conteúdo.
- **`amrPulse` já existe** em `GlobalStyle.ts` — não redeclarar.

**Não** renderizar `AvaliacaoSkeleton` dentro de `AvaliacoesBloco` na Home, e **não** envolver o bloco em
`<Suspense>`. Comentário obrigatório no export, citando a decisão travada: "Estado testável, não garantia de
produção. Sem `cacheComponents` habilitado e com busca em Server Component, um `<Suspense>` ao redor de um bloco
assíncrono é resolvido no próprio prerender — o visitante de uma rota estática nunca vê o fallback. Habilitar
`cacheComponents` é decisão de arquitetura da Fase 14 (afeta as Fases 5–17) e está explicitamente fora do escopo
desta fase."

**Em `src/components/showcase/Showcase.tsx`**, acrescentar uma `<Secao>` "Avaliações — estado carregando" que
renderiza `<AvaliacaoSkeleton />`, no padrão das seções existentes.

**Em `src/components/blocos/AvaliacoesBloco.test.tsx`**, acrescentar:
1. `renderComProviders(<AvaliacaoSkeleton />)` → existem exatamente **3** cards-esqueleto (contar pelo container
   dos `SkeletonBar`, ex.: `container.querySelectorAll('[aria-hidden="true"] > div')`) e **12** `SkeletonBar` no
   total;
2. o primeiro card tem barras com as larguras `40%`, `60%`, `100%`, `85%` — afirmar lendo o `style`/atributo
   resolvido das barras (ou, se o padrão do projeto for asserção via `toHaveStyleRule`, seguir o padrão do
   arquivo de teste de `Skeleton` mais próximo);
3. `AvaliacoesBloco` com `avaliacoes={[]}` **não** renderiza o esqueleto (o estado vazio é o real em produção) —
   afirmar por ausência de `SkeletonBar` no DOM;
4. `axe` sem violações em `<AvaliacaoSkeleton />`.
  </action>
  <verify>
    <automated>npx jest src/components/blocos/AvaliacoesBloco.test.tsx && npx tsc --noEmit && npx eslint . && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/components/blocos/AvaliacoesBloco.test.tsx` passa com os 4 testes novos, incluindo a contagem de 3 cards e 12 barras.
    - `grep -c "export function AvaliacaoSkeleton" src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "minmax(280px, 1fr)" src/components/blocos/AvaliacoesBloco.tsx` retorna 1.
    - `grep -c "amrPulse 1.3s ease-in-out infinite" src/components/blocos/AvaliacoesBloco.tsx` retorna 1 e `grep -c "keyframes" src/components/blocos/AvaliacoesBloco.tsx` retorna 0.
    - `grep -c "AvaliacaoSkeleton" src/components/showcase/Showcase.tsx` retorna 2 (import + uso).
    - `grep -c "Suspense" src/components/blocos/AvaliacoesBloco.tsx` retorna 0.
    - `grep -c "cacheComponents" next.config.ts` retorna 0 — a fase não habilita a flag.
    - `grep -Ec "40%|85%" src/components/blocos/AvaliacoesBloco.tsx` retorna 2 ou mais (larguras do card 1).
    - `npm run build` conclui.
  </acceptance_criteria>
  <done>`AvaliacaoSkeleton` existe, é exportado, reproduz 3 cards com as 12 barras nas larguras exatas do layout, aparece na showcase do design system, é coberto por teste, e nem ele nem `<Suspense>` entram na Home publicada — exatamente o aceite reescrito de HOME-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strapi → HTML renderizado | Texto de depoimento, nome, empresa, cidade, tipo de evento e o `aviso` legal vêm do CMS |
| Strapi → `Intl.NumberFormat` | `nota` é `number` validado por Zod antes de chegar ao formatador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Tampering (XSS) | `AvaliacoesBloco` (texto de depoimento em `<blockquote>`) | mitigate | `avaliacao.texto` é `string` no schema (não rich text) e é interpolado como filho JSX. **Zero** `dangerouslySetInnerHTML` — este é o campo de texto livre mais longo da fase e o alvo mais provável, então o critério de aceite verifica a ausência por `grep` |
| T-04-23 | Tampering (XSS) | `ComoFuncionaBloco` (campo `aviso`) | mitigate | `aviso` é `string` no schema, renderizado como filho de `Notice`. Nenhum HTML é interpretado |
| T-04-24 | Repudiation / integridade de conteúdo | avaliações exibidas | mitigate | `getAvaliacoes` já filtra `publicada` no servidor; o componente **nunca** tem fixture de produção. A guarda de aceite verifica por `grep` que nenhum dos 4 nomes do layout-fonte entrou no código — impede que exemplo de design seja confundido com depoimento real |
| T-04-25 | Denial of service | `Intl.NumberFormat` por avaliação | mitigate | Formatador criado **uma vez por render** do bloco, não por avaliação — instanciar `Intl.NumberFormat` em laço é caro |
| T-04-26 | Information disclosure | dados pessoais em depoimento | accept | Nome, cidade e empresa são publicados deliberadamente pelo editor, com consentimento fora do sistema. Nenhum e-mail/telefone de cliente é representável no schema de avaliação |
| T-04-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `npm run check` verde e `npm run build` concluindo.
- `npx jest src/components/blocos` passa com os testes deste plano.
- `grep -rn "dangerouslySetInnerHTML" src/components/blocos/` retorna vazio.
- `git diff --name-only` não lista `src/components/feedback/EmptyState.tsx` nem `src/components/feedback/Skeleton.tsx` (reuso, não modificação).
- Nenhum dos três arquivos de bloco contém `'use client'`, `@media` ou `window.innerWidth`.
</verification>

<success_criteria>
- Blocos 6, 7 e 8 existem como Server Components, seguindo o contrato de props da fase.
- O estado vazio das avaliações é completo, com a cópia exata e o CTA para a rota real — é o que o visitante vê hoje com o CMS sem depoimento publicado.
- Nenhum depoimento fictício existe no código de produção; a verificação é por `grep` dos 4 nomes do layout-fonte.
- A nota é formatada por locale (`5,0` em pt-BR, `5.0` em en/es), sem vírgula fixa no código.
- `AvaliacaoSkeleton` é exportado, testado e visível na showcase; não entra na Home e não há `<Suspense>` nem `cacheComponents`.
- A extensão E6 foi resolvida por composição local, sem tocar em `EmptyState.tsx`.
</success_criteria>

<output>
Criar `.planning/phases/04-home/04-06-SUMMARY.md` ao concluir.
</output>
