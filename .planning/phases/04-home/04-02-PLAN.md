---
phase: 04-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/primitives/Typography.tsx
  - src/components/primitives/Button.tsx
  - src/components/feedback/Spinner.tsx
  - src/lib/theme/GlobalStyle.ts
  - src/components/showcase/Showcase.tsx
  - src/components/primitives/primitives.test.tsx
  - src/components/chrome/TopBar.tsx
  - src/components/chrome/Footer.tsx
  - src/components/chrome/chrome-cms.test.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/error.tsx
autonomous: true
requirements: [HOME-01, HOME-04]

must_haves:
  truths:
    - "O visitante vê telefone, e-mail e tagline vindos do Strapi na TopBar, não do módulo estático"
    - "Os itens de navegação do cabeçalho apontam para os slugs reais das categorias, não para âncoras #led/#luzsom"
    - "Com o CMS vazio, o chrome ainda renderiza navegação (cai no default estático) em vez de aparecer sem menu"
    - "Um erro ao buscar a página não derruba TopBar/Header/Footer, e o visitante tem um botão que tenta de novo"
    - "Eyebrow sobre fundo escuro sai em teal e não em tealLink; o H1 do hero tem line-height 0.92"
  artifacts:
    - path: "src/app/[locale]/layout.tsx"
      provides: "Busca única de nav/rodapé/settings e montagem do chrome para todas as páginas"
      contains: "getNavPrincipal"
    - path: "src/app/[locale]/error.tsx"
      provides: "Error boundary do segmento com prop retry"
      contains: "retry"
    - path: "src/components/feedback/Spinner.tsx"
      provides: "Primitivo de spinner do botão de busca (E4)"
      contains: "amrSpin"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/lib/cms/adapters.ts"
      via: "await getNavPrincipal / getColunasRodape / getSettingsGlobais"
      pattern: "from '@/lib/cms/adapters'"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/chrome/TopBar.tsx"
      via: "props tagline e contato"
      pattern: "<TopBar"
---

<objective>
Fechar os dois pré-requisitos estruturais que não estavam no ROADMAP e que as Fases 5 a 11 herdam:
(a) o **chrome alimentado pelo CMS**, montado uma única vez em `src/app/[locale]/layout.tsx`, e (b) as
**extensões E1–E4 do design system** que os blocos da Home consomem. Mais o primeiro `error.tsx` do projeto.

Propósito: `TopBar`/`Header`/`Footer` **não estão montados em nenhum lugar da árvore hoje** —
`[locale]/layout.tsx` só renderiza `StyledRegistry` + `StoreProvider` + `children`. E `TopBar` não tem
nenhuma prop: importa `contato`/`textosLegais` direto do módulo estático `src/lib/site/navigation.ts`. Sem
esta correção, o item 6 de `docs/00-divergencias.md` (hrefs de âncora `#led`/`#luzsom` em vez dos slugs reais
das 5 categorias) fica aberto e nenhuma página da Fase 4 em diante tem cabeçalho.

Saída: chrome no layout com dados do Strapi, `TopBar`/`Footer` com props, `error.tsx` com `retry`,
`Eyebrow $sobreEscuro`, `Heading $nivel="h1"` com 0.92, `Button $variante="pretoSolido"`, `Spinner`, keyframe
`amrMod`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-home/04-UI-SPEC.md
@.planning/phases/04-home/04-PATTERNS.md
@docs/divergencias.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extensões E1-E4 do design system e keyframe amrMod</name>
  <files>src/components/primitives/Typography.tsx, src/components/primitives/Button.tsx, src/components/feedback/Spinner.tsx, src/lib/theme/GlobalStyle.ts, src/components/showcase/Showcase.tsx, src/components/primitives/primitives.test.tsx</files>
  <read_first>
    - `src/components/primitives/Typography.tsx` (arquivo inteiro, 58 linhas) — `Heading` (styled.h2 com `$nivel`), `Eyebrow` (styled.p com cor fixa `tealLink`), `Body`, `Mono`.
    - `src/components/primitives/Button.tsx` linhas 13-14 (tipo `VarianteBotao`) e linhas 84-135 (o `switch ($variante)` com os casos `primario`/`outlinePreto`/`outlineClaro`) — copiar exatamente o formato dos `case` existentes, incluindo o bloco `&:focus-visible`.
    - `src/components/feedback/Skeleton.tsx` linhas 1-12 — molde de "primitivo visual simples, sem estado" (`SkeletonBar` como `styled.span`).
    - `src/lib/theme/GlobalStyle.ts` linhas 40-76 — o bloco fechado de 6 `@keyframes` e o bloco `@media (prefers-reduced-motion: reduce)`.
    - `.planning/phases/04-home/04-UI-SPEC.md` seção "Extensões necessárias ao Design System" (tabela E1–E5).
    - `src/components/showcase/Showcase.tsx` linhas 85-135 — padrão de `<Secao>` para acrescentar entradas.
  </read_first>
  <action>
**E1 — `Eyebrow` (`Typography.tsx`).** Trocar `styled.p` por `styled.p<{ $sobreEscuro?: boolean }>` e a linha
de cor fixa por `color: ${({ theme, $sobreEscuro }) => ($sobreEscuro ? theme.cor.teal : theme.cor.tealLink)};`.
Motivo (colocar como comentário): no layout-fonte o eyebrow é `#1A7F82` (`tealLink`) sobre fundo claro e
`#2FB6B9` (`teal`) sobre fundo escuro (Hero, LED). Mesmo padrão de prop que `Button` já usa com `$sobreEscuro`.
Não mudar `font-size` (fica em `theme.tamanho[13]`, decisão da Fase 2 — não reabrir).

**E2 — `Heading` (`Typography.tsx`).** Trocar a linha `line-height: ${({ theme }) => theme.leading.display};`
por `line-height: ${({ theme, $nivel = 'h2' }) => ($nivel === 'h1' ? theme.leading.displayApertado : theme.leading.display)};`.
Só `h1`. Os níveis `displayHero`/`display`/`h2`/`h3`/`h4` continuam em `0.98` — no layout-fonte apenas o H1 do
hero usa `0.92`, todos os H2 de seção usam `0.98`. `theme.leading.displayApertado` já existe (`'0.92'`), não
criar token.

**E3 — `Button` (`Button.tsx`).** Adicionar `'pretoSolido'` ao tipo `VarianteBotao` (linha 13) e um
`case 'pretoSolido':` no `switch` de variante, no mesmo formato dos vizinhos:
`background: ${theme.cor.tinta900}; border: none; color: ${theme.cor.fundo};`, `&:hover { background: ${theme.cor.tealLink}; }`,
`&:focus-visible { outline: 2px solid ${theme.cor.tealLink}; outline-offset: 3px; }`.
É o botão "BUSCAR" colado ao input da busca grande — sólido preto, sem borda. Reusável no Catálogo (Fase 5),
que tem a mesma busca. **Não** adicionar `border-radius` próprio: o recorte do lado esquerdo é
responsabilidade do composto `SearchBarGrande` (plano 04-04), não do primitivo.

**E4 — `src/components/feedback/Spinner.tsx` (novo).** `'use client'`, exportar
`export const Spinner = styled.span.attrs({ 'aria-hidden': true })` com: `display: inline-block; width: 13px;
height: 13px; border-radius: 50%` (usar `theme.raio.circulo`), `border: 2px solid rgba(241, 242, 242, 0.35)`,
`border-top-color: ${({ theme }) => theme.cor.teal}`, `animation: amrSpin 0.7s linear infinite`. `amrSpin` **já
existe** em `GlobalStyle.ts` — não redeclarar keyframe.

**Keyframe `amrMod` (`GlobalStyle.ts`).** Acrescentar como sétimo keyframe do bloco existente, no mesmo estilo
das linhas 40-67, com o valor exato do layout-fonte (linha 20 de `projeto-base/All Music Rentals - Home.dc.html`):
`@keyframes amrMod { from { opacity: 0; transform: scale(1.06); } to { opacity: 1; transform: scale(1); } }`.
É a revelação das células do mosaico do Hero (plano 04-03).

**Uma linha nova no bloco `@media (prefers-reduced-motion: reduce)` de `GlobalStyle.ts`:** acrescentar
`animation-delay: 0s !important;` junto de `animation-duration`/`animation-iteration-count`. Motivo (comentário
obrigatório): o mosaico do Hero e o stagger dos skeletons agendam `animation-delay` por célula/índice; zerar só
a duração deixaria a célula parada no estado `from` (opacidade 0) durante o delay sob preferência de movimento
reduzido. Com esta linha, o mosaico do Hero pode ser 100% CSS renderizado no servidor, sem
`window.matchMedia` e sem `'use client'`.

**Showcase (`src/components/showcase/Showcase.tsx`).** Acrescentar uma `<Secao>` nova, no padrão das
existentes, exibindo: `Eyebrow` normal e `Eyebrow $sobreEscuro` (esta sobre uma div de fundo
`theme.cor.tinta900`), `Heading $nivel="h1"` ao lado de `Heading $nivel="h2"`,
`Button $variante="pretoSolido"`, e `Spinner`.

**Testes (`src/components/primitives/primitives.test.tsx`, arquivo existente).** Acrescentar casos:
1. `Eyebrow $sobreEscuro` renderiza com `color` resolvida para `rgb(47, 182, 185)` (`#2FB6B9`) e `Eyebrow` sem a
   prop para `rgb(26, 127, 130)` (`#1A7F82`) — usar `getComputedStyle` sobre o nó, como já feito nos testes de
   primitivo existentes; se o padrão do arquivo for asserção por snapshot de classe, seguir o padrão do arquivo.
2. `Heading $nivel="h1"` tem `line-height: 0.92` e `Heading $nivel="h2"` tem `0.98`.
3. `Button $variante="pretoSolido"` renderiza e responde a clique (`onClick` chamado uma vez).
  </action>
  <verify>
    <automated>npx jest src/components/primitives && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sobreEscuro ? theme.cor.teal : theme.cor.tealLink" src/components/primitives/Typography.tsx` retorna 1.
    - `grep -c "displayApertado" src/components/primitives/Typography.tsx` retorna 1.
    - `grep -c "pretoSolido" src/components/primitives/Button.tsx` retorna 2 (o tipo e o `case`).
    - `grep -c "amrSpin 0.7s linear infinite" src/components/feedback/Spinner.tsx` retorna 1 e `grep -c "keyframes" src/components/feedback/Spinner.tsx` retorna 0.
    - `grep -v '^\s*/\*' src/lib/theme/GlobalStyle.ts | grep -c "amrMod"` retorna 1 e `grep -c "scale(1.06)" src/lib/theme/GlobalStyle.ts` retorna 1.
    - `grep -c "animation-delay: 0s !important" src/lib/theme/GlobalStyle.ts` retorna 1.
    - `npx jest src/components/primitives` passa, incluindo os 3 casos novos.
  </acceptance_criteria>
  <done>Os quatro primitivos que os blocos da Home precisam existem com a API exata do UI-SPEC, `amrMod` está no inventário central de keyframes, movimento reduzido zera também o delay, e a showcase mostra cada extensão.</done>
</task>

<task type="auto">
  <name>Task 2: TopBar e Footer aceitam dados do CMS por prop</name>
  <files>src/components/chrome/TopBar.tsx, src/components/chrome/Footer.tsx, src/components/chrome/chrome-cms.test.tsx</files>
  <read_first>
    - `src/components/chrome/TopBar.tsx` (arquivo inteiro, ~118 linhas) — hoje só aceita `localeAtual` e importa `contato`/`textosLegais` de `@/lib/site/navigation`.
    - `src/components/chrome/Footer.tsx` linhas 1-10 e 114-160 — molde exato do padrão prop-com-default-estático (`colunas = colunasRodape`) e os usos de `contato.telefoneHref`/`contato.email`/`textosLegais`.
    - `src/lib/site/navigation.ts` linhas 7-28 — interfaces `ItemNav`, `ColunaRodape`, `DadosContato` e a constante `contato`.
    - `src/lib/cms/adapters.ts` linhas 118-142 — `SettingsGlobais` (`nomeSite`, `tagline`, `contato: DadosContato`), para confirmar que o formato devolvido pelo CMS é exatamente o que os componentes já consomem.
    - `src/components/chrome/Header.test.tsx` — padrão de teste de componente de chrome com `renderComProviders`.
  </read_first>
  <action>
**`TopBar.tsx`.** Trocar a assinatura por
`export interface TopBarProps { localeAtual?: string; tagline?: string; contato?: DadosContato }` e
`export function TopBar({ localeAtual = 'pt-BR', tagline = textosLegais.tagline, contato: dadosContato = contato }: TopBarProps)`.
Importar `type DadosContato` de `@/lib/site/navigation` junto dos valores já importados. Substituir
`{textosLegais.tagline}` por `{tagline}` e os três usos de `contato.*` por `dadosContato.*`. O default aponta
para o módulo estático — é o mesmo padrão que `Header`/`Footer`/`MobileMenu` já usam, então nenhum consumidor
atual quebra.

Manter o array `IDIOMAS` e a lógica de `localeAtual` como estão. **Atenção:** os `href` dos idiomas hoje são
`/${idioma.code}` — correto, não mexer.

**`Footer.tsx`.** Acrescentar `contato?: DadosContato` a `FooterProps` e o default
`contato: dadosContato = contato` na desestruturação (renomeando localmente, como acima, para não sombrear o
import). Trocar os usos das linhas ~148-149 por `dadosContato.telefoneHref` / `dadosContato.telefone` /
`dadosContato.email`. **Não** mudar `textosLegais.disclaimer`/`copyright`/`descricaoMarca`: esses textos legais
não vêm de `settings-globais` no schema atual e passar a buscá-los seria escopo novo.

**`src/components/chrome/chrome-cms.test.tsx` (novo).** Com `renderComProviders` de `@/test-utils`:
1. `<TopBar tagline="Tagline do CMS" contato={{ telefone: '(689) 000-0000', telefoneHref: 'tel:+16890000000', email: 'cms@exemplo.com' }} />` — afirmar que os três valores aparecem na tela (`screen.getByText`) e que o telefone estático `(689) 242-1871` **não** aparece.
2. `<TopBar />` sem props — afirmar que o telefone do módulo estático aparece (o default continua agindo).
3. `<Footer contato={{ ... }} />` — mesma verificação de override, e `<Footer />` mantém o default.
4. `<TopBar localeAtual="en" />` — o link `EN` está presente (a lógica de idioma não regrediu).
  </action>
  <verify>
    <automated>npx jest src/components/chrome && npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface TopBarProps" src/components/chrome/TopBar.tsx` retorna 1.
    - `grep -c "tagline = textosLegais.tagline" src/components/chrome/TopBar.tsx` retorna 1.
    - `grep -c "contato?: DadosContato" src/components/chrome/TopBar.tsx src/components/chrome/Footer.tsx` retorna 1 em cada arquivo.
    - `npx jest src/components/chrome/chrome-cms.test.tsx` passa com 4 ou mais asserções, incluindo o caso sem props.
    - `npx jest src/components/chrome/Header.test.tsx` e `src/components/__tests__/a11y.test.tsx` continuam passando (o `<Footer />` sem props do teste de a11y não regride).
  </acceptance_criteria>
  <done>`TopBar` e `Footer` aceitam contato/tagline por prop com fallback no módulo estático, sem quebrar nenhum consumidor existente, e há teste provando tanto o override quanto o default.</done>
</task>

<task type="auto">
  <name>Task 3: Montar o chrome do CMS no layout do locale e criar o error boundary</name>
  <files>src/app/[locale]/layout.tsx, src/app/[locale]/error.tsx</files>
  <read_first>
    - `src/app/[locale]/layout.tsx` (arquivo inteiro, 33 linhas) — esqueleto `async` a preservar: `await params`, `isLocale`/`notFound`, `<html lang>` + `<body>` + `StyledRegistry` + `StoreProvider`, e `generateStaticParams`.
    - `src/lib/cms/adapters.ts` linhas 76-142 — assinaturas de `getNavPrincipal(locale)`, `getColunasRodape(locale)`, `getSettingsGlobais(locale)` e o tipo `SettingsGlobais`.
    - `src/components/chrome/Header.tsx` — **arquivo inteiro**, não só a interface. As linhas 183-201 têm `HeaderProps` (`itens`, `ctaHref`, `ctaLabel`, `orcamentoHref`, `ativoHref`) e os defaults atuais (`ctaHref = '#solicitar'`, `orcamentoHref = '/meu-orcamento'`), mas a linha **248** é a que decide esta tarefa: é onde `Header` renderiza `<MobileMenu>` internamente. Ler só a interface levaria a montar o menu duas vezes.
    - `src/components/chrome/MobileMenu.tsx` linhas 90-102 — `MobileMenuProps` com os mesmos defaults de âncora (recebidas via `Header`, não montadas no layout).
    - `.planning/phases/04-home/04-RESEARCH.md` seção "Code Examples" → bloco `error.tsx` (usa a prop `retry`) e o parágrafo "Importante:" sobre `error.js` envolver o `page.js` do segmento, não o `layout.js`.
    - `docs/divergencias.md` item 6 — a divergência de hrefs de âncora que esta tarefa fecha.
  </read_first>
  <action>
**`src/app/[locale]/layout.tsx`.** Entre `const lang: Locale = locale;` e o `return`, acrescentar as três
buscas em paralelo:
`const [nav, colunas, settings] = await Promise.all([getNavPrincipal(lang), getColunasRodape(lang), getSettingsGlobais(lang)]);`
(importar de `@/lib/cms/adapters`). `Promise.all` e não três `await` sequenciais — são três requisições
independentes ao Strapi em toda página do site.

Dentro do `<StoreProvider>`, envolver `{children}` assim, nesta ordem exata:
`<TopBar localeAtual={lang} tagline={settings?.tagline} contato={settings?.contato} />`,
`<Header itens={nav.length > 0 ? nav : undefined} ctaHref={`/${lang}/solicitar-orcamento`} orcamentoHref={`/${lang}/meu-orcamento`} />`,
`{children}`,
`<Footer colunas={colunas.length > 0 ? colunas : undefined} contato={settings?.contato} />`.

**NÃO montar `<MobileMenu>` no layout.** `Header.tsx` linha 248 já renderiza
`<MobileMenu itens={itens} ativoHref={ativoHref} ctaHref={ctaHref} ctaLabel={ctaLabel} />` internamente,
repassando as próprias props. Montar de novo aqui produziria dois `id="menu-mobile"` no DOM e landmark
duplicado — as props do menu mobile chegam pelo `Header`, não por montagem direta.

Duas regras não-óbvias, documentar como comentário no arquivo:
- **`nav.length > 0 ? nav : undefined`** — passar `[]` sobrescreveria o default e o site ficaria sem navegação
  quando o CMS não tem menu publicado. Passando `undefined`, o default estático de `Header`/`MobileMenu` age.
  Mesma regra para `colunas` no `Footer`.
- **`settings?.tagline` / `settings?.contato`** — `getSettingsGlobais` devolve `SettingsGlobais | null`; com
  `null`, `undefined` chega à prop e o default estático age.
- **`ctaHref` com prefixo de locale** — os defaults atuais são as âncoras `#solicitar` e o caminho sem locale
  `/meu-orcamento`. Passar as rotas reais `/${lang}/solicitar-orcamento` e `/${lang}/meu-orcamento` fecha o item
  6 de `docs/divergencias.md` no cabeçalho. Essas rotas dão 404 até as Fases 5/8 — aceito por decisão travada do
  CONTEXT.md ("CTAs apontam para as rotas finais, mesmo antes de existirem"); **não** desabilitar nem stubar.

Confirmar no `Header.tsx` (linha 248, já verificado em 2026-08-18) que o `<MobileMenu>` continua sendo
renderizado ali antes de decidir qualquer coisa sobre montagem. Se em algum momento essa linha deixar de
existir, então — e só então — montar `MobileMenu` diretamente no layout. Registrar no SUMMARY o que foi
encontrado.

**`src/app/[locale]/error.tsx` (novo).** `'use client'` na primeira linha (error boundary precisa ser Client
Component). Assinatura `export default function ErroDoSegmento({ error, retry }: { error: Error & { digest?: string }; retry: () => void })`.
Usar a prop **`retry`**, não `reset` — `retry` é a recomendada a partir do Next 16.3, que é a versão exata do
projeto (`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md`). A maioria dos
tutoriais mostra `reset` e o TypeScript não reclama, então isso só apareceria em revisão.

Conteúdo: um `Container` com `Notice` (de `@/components/feedback/Notice`, variante `escuro`) com
`rotulo="ERRO"` e o texto "Não foi possível carregar o conteúdo desta página agora." e, abaixo,
`<Button $variante="primario" type="button" onClick={() => retry()}>TENTAR NOVAMENTE</Button>`.
`padding-block` de `clamp(64px, 9vw, 144px)` para não colapsar entre o cabeçalho e o rodapé.

Comentário obrigatório no topo explicando a decisão de granularidade (Q2 do RESEARCH): **um único boundary no
segmento `[locale]`**, e não um por bloco, porque `getPagina` é uma chamada única — a falha é tudo-ou-nada e não
há modo de falha independente por bloco a isolar. O chrome sobrevive porque vive em `layout.tsx`, e o
`error.tsx` de um segmento envolve o `page.tsx` daquele segmento, não o `layout.tsx`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Promise.all" "src/app/[locale]/layout.tsx"` retorna 1 e `grep -Ec "getNavPrincipal|getColunasRodape|getSettingsGlobais" "src/app/[locale]/layout.tsx"` retorna 3 ou mais.
    - `grep -c "nav.length > 0 ? nav : undefined" "src/app/[locale]/layout.tsx"` retorna 1 ou mais.
    - `grep -c "solicitar-orcamento" "src/app/[locale]/layout.tsx"` retorna 1 ou mais e `grep -c "#solicitar" "src/app/[locale]/layout.tsx"` retorna 0.
    - `grep -c "retry" "src/app/[locale]/error.tsx"` retorna 2 ou mais e `grep -c "reset" "src/app/[locale]/error.tsx"` retorna 0.
    - `head -1 "src/app/[locale]/error.tsx"` contém `'use client'`.
    - `npm run build` conclui e `npm run check` fica verde.
  </acceptance_criteria>
  <done>Toda página sob `/[locale]` recebe TopBar, Header, MobileMenu e Footer com dados do Strapi buscados uma única vez; navegação e contato caem no default estático se o CMS estiver vazio; um erro no `page.tsx` mostra um aviso com botão "TENTAR NOVAMENTE" sem derrubar o chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strapi → layout do servidor | `rotulo`/`url` de itens de menu e colunas de rodapé vêm do CMS e viram `href` renderizado |
| Server Component → Client Component (error boundary) | `error.message`/`error.digest` cruzam para o cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (XSS via href) | `src/app/[locale]/layout.tsx` → `Header`/`Footer`/`MobileMenu` | mitigate | Os `href` vêm de `getNavPrincipal`/`getColunasRodape`, que já validam a resposta com Zod na borda (`fetchStrapi`, Fase 3), e são interpolados como atributo em JSX — React escapa. Nenhum uso de `dangerouslySetInnerHTML` nestes componentes; nenhum `href` é construído por concatenação de string vinda do CMS |
| T-04-06 | Information disclosure | `src/app/[locale]/error.tsx` | mitigate | Renderizar apenas cópia fixa em português. **Não** renderizar `error.message`, `error.stack` nem `error.digest` na tela — um erro de `fetchStrapi` pode conter URL interna do Strapi e cabeçalhos. A prop `error` fica sem uso visual, só disponível para logging futuro (Fase 14) |
| T-04-07 | Spoofing | `TopBar` com contato do CMS | accept | Telefone e e-mail são dados públicos de contato; um editor do Strapi já é ator confiável do modelo de ameaça (mesmo nível de quem edita a Home) |
| T-04-08 | Denial of service | 3 chamadas ao Strapi por render de layout | mitigate | `Promise.all` (paralelo, não serial) + as três chamadas já usam `next: { tags }` do `fetchStrapi`, então ficam cacheadas e são revalidadas por webhook, não a cada requisição |
| T-04-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo neste plano. Se o executor achar que precisa de um, PARAR e escalar |
</threat_model>

<verification>
- `npm run check` verde e `npm run build` concluindo.
- `npx jest src/components/primitives src/components/chrome` passa, incluindo os casos novos.
- Nenhuma `@media` de breakpoint nova: `grep -c "@media" src/lib/theme/GlobalStyle.ts` não aumenta além da adição de `animation-delay` dentro do bloco de `prefers-reduced-motion` já existente.
- `src/app/[locale]/error.tsx` não menciona `error.message`, `error.stack` nem `error.digest`.
</verification>

<success_criteria>
- Toda rota sob `/[locale]` renderiza o chrome com nav/rodapé/contato do Strapi, com fallback estático em CMS vazio.
- Os hrefs de CTA do cabeçalho apontam para rotas reais com prefixo de locale, fechando o item 6 de `docs/divergencias.md`.
- Um erro na página mostra aviso + botão que chama `retry()`, com o chrome intacto e sem vazar detalhe de erro.
- As extensões E1, E2, E3 e E4 existem com a API exata do UI-SPEC, aparecem na showcase e têm teste.
- `amrMod` está em `GlobalStyle.ts` e `prefers-reduced-motion` zera também `animation-delay`.
</success_criteria>

<output>
Criar `.planning/phases/04-home/04-02-SUMMARY.md` ao concluir.
</output>
