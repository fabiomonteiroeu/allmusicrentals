---
phase: 03-strapi-cms
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/00-divergencias.md
  - docs/divergencias.md
  - docs/cms-fluxo-editorial.md
  - docs/PLANO.md
  - docs/00-inventario.md
  - docs/adr/003-rota-canonica-produto.md
  - docs/adr/004-deploy-ghcr-caddy.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "Nenhuma das divergências 5, 6, 7, 9, 11 e 12 continua marcada como ⏳ AGUARDA DECISÃO"
    - "Cada divergência fechada cita o arquivo real que a implementa, não a proposta original"
    - "Onde o código implementado divergir da proposta, existe registro datado em docs/divergencias.md"
    - "docs/PLANO.md não afirma mais 9 etapas de formulário, nem 'Fase 00 em andamento', nem decisão aberta na Fase 17"
    - "A rota canônica de produto e a decisão de deploy existem como ADR datado em docs/adr/"
  artifacts:
    - path: "docs/adr/003-rota-canonica-produto.md"
      provides: "Decisão travada da rota canônica de produto e suas consequências"
      contains: "/[locale]/[categoria]/[slug]"
    - path: "docs/adr/004-deploy-ghcr-caddy.md"
      provides: "Decisão travada de registry e reverse proxy do deploy"
      contains: "GHCR"
    - path: "docs/00-divergencias.md"
      provides: "Registro das 6 divergências fechadas contra o código real"
      contains: "RESOLVIDA"
  key_links:
    - from: "docs/00-divergencias.md"
      to: "src/components/chrome/Header.tsx"
      via: "citação do componente que implementa a divergência 5"
      pattern: "Header\\.tsx"
    - from: "docs/00-divergencias.md"
      to: "cms/src/api/product/content-types/product/schema.json"
      via: "citação do schema que implementa a divergência 11"
      pattern: "product/schema\\.json"
    - from: "docs/PLANO.md"
      to: "docs/adr/004-deploy-ghcr-caddy.md"
      via: "substituição da nota 'Decisão aberta' da Fase 17 pelo ponteiro para o ADR"
      pattern: "004-deploy-ghcr-caddy"
---

<objective>
Fechar as pendências documentais que o ingest levantou: seis divergências que continuam "aguardando
decisão" apesar de já terem sido implementadas, um `docs/PLANO.md` que afirma coisas que deixaram de
ser verdade, e duas decisões travadas pelo usuário que ainda não existem como ADR.

Propósito: `docs/` é a fonte da verdade que as fases 4 a 17 vão consultar. Enquanto ele disser
"form 9 etapas", "Fase 00 em andamento" e "Decisão aberta: registry", cada fase seguinte paga o custo
de descobrir de novo o que já foi decidido — e corre o risco de implementar o valor errado.
Saída: 6 divergências fechadas citando o código real, `docs/PLANO.md` e `docs/00-inventario.md`
sincronizados com o estado real, e os ADRs 003 e 004.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strapi-cms/03-CONTEXT.md
@.planning/INGEST-CONFLICTS.md

@docs/00-divergencias.md
@docs/divergencias.md
@docs/adr/002-locale-padrao.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fechar as divergências 5, 6, 7 e 9 contra os componentes da Fase 2</name>
  <files>docs/00-divergencias.md, docs/divergencias.md</files>

  <read_first>
    - `docs/00-divergencias.md` — itens 5, 6, 7 e 9 e a seção final "Resumo das decisões" / "Ainda em aberto"
    - `docs/divergencias.md` — a entrada D1 inteira: é o padrão obrigatório (título `## Dn — …`,
      linha `**Fase:** NN · **Data:** AAAA-MM-DD`, e os blocos `**No layout:**`, `**Divergência:**`,
      `**Motivo (técnico):**`, `**Escopo:**`, `**Reversível:**`)
    - `src/components/chrome/Header.tsx` — `HeaderProps` (`itens?: ItemNav[]`, `ativoHref?: string`),
      o `NavLink` com `$ativo`, e a passagem de `itens`/`ativoHref` para `MobileMenu`
    - `src/components/chrome/MobileMenu.tsx` — confirmar que recebe os mesmos props
    - `src/components/chrome/Footer.tsx` — `FooterProps` (`colunas?: ColunaRodape[]`) e o `map` das colunas
    - `src/lib/site/navigation.ts` — `navPrincipal`, `colunasRodape`, `coresProduto`, `textosLegais`;
      conferir se os `href` de produto usam os slugs reais das 5 categorias
    - `src/components/product/ProductCard.tsx` — a interface `ProdutoResumo` inteira (`ehServico`,
      `escopo`, `cores`) e onde `ColorSwatches` e `QuantityStepper` são usados
    - `src/components/feedback/Toast.tsx` — `ToastProps.offsetBarra` e o `styled.div` `Camada`
      (`bottom: 96px` quando `$offsetBarra`, senão `theme.espaco[20]`)
  </read_first>

  <action>
    Editar `docs/00-divergencias.md` trocando a marca ⏳ dos itens 5, 6, 7 e 9, sempre citando o caminho
    do arquivo que implementa a decisão e datando `*Fechado 2026-08-17 contra o código da Fase 02.*`

    Item 5 (Header/nav): o componente real recebe `itens` e `ativoHref` por props com default vindo de
    `navPrincipal`, e repassa os dois ao `MobileMenu` — é exatamente a proposta "um só componente
    Header recebendo itens[] e ativo por props". Marcar ✅ RESOLVIDA citando
    `src/components/chrome/Header.tsx` (`HeaderProps.itens`, `HeaderProps.ativoHref`) e registrar em
    uma frase que a origem dos itens hoje é o módulo estático `src/lib/site/navigation.ts` e passa a ser
    o content-type `menu-item` na Fase 4, pelo adaptador que já existe em `src/lib/cms/adapters.ts`.

    Item 6 (Rodapé): o `Footer` recebe `colunas` por props. Conferir em `navigation.ts` se os `href` de
    produto usam os slugs reais (`estruturas`, `telas-de-led`, `luz-e-som`, `tendas`, `moveis`) em vez
    dos âncoras antigos (`#led`, `#luzsom`). Marcar ✅ RESOLVIDA citando
    `src/components/chrome/Footer.tsx` (`FooterProps.colunas`) e `src/lib/site/navigation.ts`
    (`colunasRodape`). Sobre o `border-top` extra da Home mencionado na divergência: se o `Footer` não
    tiver prop nem variante para isso, não inventar nenhuma — registrar a diferença como nota do item,
    dizendo que o `border-top` da Home fica a cargo da seção que antecede o rodapé na Fase 4.

    Item 7 (Card de produto): a proposta pedia variantes controladas por `tipoDeItem` e presença de
    `variações`/`cor`. Ler `ProdutoResumo` e verificar quais props existem de fato — hoje há
    `ehServico`, `escopo` e `cores`, e não há `tipoDeItem`; o seletor de cor é sempre `ColorSwatches`,
    e não o `<select>` que a Home do layout usava. Se essa leitura se confirmar, marcar o item como
    ✅ RESOLVIDA COM DESVIO e abrir a entrada `## D2 — Card de produto: variantes por props booleanas,
    não por `tipoDeItem`` em `docs/divergencias.md`, no formato exato de D1, com `**Fase:** 02 ·
    **Data:** 2026-08-17`, explicando: (a) que a variação de controle foi unificada em swatches +
    `ehServico`/`escopo`; (b) que o `tipoDeItem` do CMS (`fisico`, `com-variacao`, `servico-tecnico`,
    `pacote`, em `cms/src/api/product/content-types/product/schema.json`) é traduzido para essas props
    no adaptador, e não dentro do componente, para manter o card sem conhecimento do modelo do CMS;
    (c) que a tradução acontece nas fases 5 e 7, quando as páginas de catálogo e produto forem ligadas.
    Se a leitura do código mostrar que `tipoDeItem` já existe como prop, marcar apenas ✅ RESOLVIDA e
    não criar a entrada D2.

    Item 9 (Toast): `ToastProps.offsetBarra` alterna `bottom` entre `96px` e `theme.espaco[20]` — é a
    proposta literal. Marcar ✅ RESOLVIDA citando `src/components/feedback/Toast.tsx` (`offsetBarra`).

    Atualizar a tabela "Resumo das decisões" do fim do arquivo acrescentando uma linha para 5, 6, 7 e 9
    com o mesmo formato das linhas existentes, e reescrever a linha
    "**Ainda em aberto (não bloqueiam): 5, 6, 7, 9, 11, 12**" para listar somente 11 e 12 (a Task 2
    remove os dois restantes).
  </action>

  <verify>
    <automated>grep -nE "^(5|6|7|9)\. ⏳" docs/00-divergencias.md; test $(grep -cE "^(5|6|7|9)\. ⏳" docs/00-divergencias.md) -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "^(5|6|7|9)\. ⏳" docs/00-divergencias.md` retorna 0
    - `grep -cE "^(5|6|7|9)\. ✅" docs/00-divergencias.md` retorna 4
    - `grep -n "Header.tsx" docs/00-divergencias.md` retorna ao menos 1 linha
    - `grep -n "Footer.tsx" docs/00-divergencias.md` retorna ao menos 1 linha
    - `grep -n "ProductCard.tsx" docs/00-divergencias.md` retorna ao menos 1 linha
    - `grep -n "Toast.tsx" docs/00-divergencias.md` retorna ao menos 1 linha
    - `grep -n "Ainda em aberto" docs/00-divergencias.md` retorna uma linha que não contém "5", "6", "7" nem "9"
    - Se o item 7 foi marcado COM DESVIO: `grep -n "^## D2 —" docs/divergencias.md` retorna 1 linha, e
      essa entrada contém `**Fase:**`, `**Data:** 2026-08-17`, `**Divergência:**`, `**Motivo (técnico):**`,
      `**Escopo:**` e `**Reversível:**`
  </acceptance_criteria>

  <done>
    Os quatro itens de componente saíram de "aguarda decisão" e passaram a apontar para o arquivo que os
    implementa. Qualquer desvio entre a proposta da Fase 0 e o que a Fase 2 construiu está registrado no
    formato D1, com motivo técnico e reversibilidade.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fechar as divergências 11 e 12 contra o modelo do Strapi</name>
  <files>docs/00-divergencias.md, docs/divergencias.md, docs/cms-fluxo-editorial.md</files>

  <read_first>
    - `docs/00-divergencias.md` — itens 11 e 12 e a seção final atualizada pela Task 1
    - `cms/src/api/product/content-types/product/schema.json` — atributos `categoria` (relation
      manyToOne para `api::category.category`), `medidas` (component repetível `shared.medida`),
      `ambiente` (enum `interno`/`externo`/`interno-ou-externo`), `tipoDeItem` (enum de 4 valores)
    - `cms/src/api/category/content-types/category/schema.json` — o inverso da relação e as subcategorias
    - `cms/src/components/shared/medida.json` e `cms/src/components/shared/subcategoria.json`
    - `cms/src/api/settings-globais/content-types/settings-globais/schema.json` — o atributo
      `textosLegais` (tipo `json`, `i18n.localized: true`)
    - `cms/src/index.ts` linhas 72-96 — o seed de `settings-globais`, que hoje grava as chaves
      `disclaimer`, `copyright` e `descricaoMarca` dentro de `textosLegais`
    - `src/lib/site/navigation.ts` — a constante `textosLegais` do lado Next, para comparar as chaves
    - `docs/cms-fluxo-editorial.md` — a seção "Content-types e onde editar", item "Configurações Globais"
  </read_first>

  <action>
    Item 11 (produto como fonte única de metadados): conferir no schema real que a categoria do produto
    é uma única relação `manyToOne` e que "TENDAS vs ÁREA EXTERNA" e "MEDIDAS vs BASE SOB CONSULTA"
    passam a derivar de três atributos do mesmo registro — `categoria`, `ambiente` e `medidas`. Marcar
    o item ✅ RESOLVIDA, datar `*Fechado 2026-08-17 contra o modelo da Fase 03.*` e citar
    `cms/src/api/product/content-types/product/schema.json` nomeando os atributos `categoria`,
    `ambiente`, `medidas` e `tipoDeItem`. Registrar em uma frase a consequência operacional: um produto
    nunca tem duas categorias, então a diferença que aparecia entre as páginas do layout vira erro de
    dado, não variação legítima.

    Item 12 (microcopy legal repetido): conferir que o campo único proposto existe como
    `settings-globais.textosLegais` (JSON localizado). Comparar as chaves que o seed grava
    (`disclaimer`, `copyright`, `descricaoMarca`) com os dois microcopies citados na divergência. O
    aviso de carrinho ("Os produtos não ficam reservados ao serem adicionados ao carrinho. A
    disponibilidade será confirmada pela equipe.") não está entre as chaves semeadas — confirmar isso
    lendo `cms/src/index.ts` antes de escrever qualquer coisa.

    Fechar o item 12 como ✅ RESOLVIDA citando `settings-globais.textosLegais` e fixando o contrato de
    chaves: `disclaimer`, `copyright`, `descricaoMarca` e `avisoCarrinho`. Registrar explicitamente que
    `avisoCarrinho` ainda não é preenchido pelo seed e é conteúdo editorial a ser cadastrado no painel —
    não inventar texto novo no seed nesta fase, porque o seed é de estrutura.

    Documentar o mesmo contrato de chaves em `docs/cms-fluxo-editorial.md`, na linha de "Configurações
    Globais" da seção "Content-types e onde editar", listando as quatro chaves de `textosLegais` e onde
    cada uma aparece (rodapé, carrinho da Fase 8, formulário da Fase 9).

    Reclassificar os itens 10 e 13, que hoje continuam ⏳ mas nunca foram divergências de decisão —
    ambos já trazem a própria conclusão no texto e não fazem parte de DOC-01/DOC-02:
    - Item 10 (Barra fixa de orçamento): trocar ⏳ por ℹ️ INTENCIONAL. O texto já diz "Não é conflito —
      é presença condicional. *Sem ação além de documentar.*". Acrescentar que a presença condicional
      é entrega da Fase 8 (barra fixa mobile do orçamento).
    - Item 13 (Dados mockados nos exports): trocar ⏳ por ℹ️ INTENCIONAL. O texto já fixa a regra
      ("não semear conteúdo fictício"). Acrescentar que a regra está implementada e provada: o seed de
      `cms/src/index.ts` é de estrutura, e o plano 03-06 verifica por `curl` que
      `/api/avaliacoes` devolve zero itens.

    Fechar a linha "Ainda em aberto" de `docs/00-divergencias.md`: depois desta tarefa nenhum item
    permanece em ⏳ além do item 4, que o INGEST-CONFLICTS registra como superado por D1 — apontar isso
    na própria linha, citando `docs/divergencias.md` D1.
  </action>

  <verify>
    <automated>test $(grep -cE "^(11|12)\. ⏳" docs/00-divergencias.md) -eq 0 && grep -q "textosLegais" docs/cms-fluxo-editorial.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "^(11|12)\. ⏳" docs/00-divergencias.md` retorna 0
    - `grep -cE "^(11|12)\. ✅" docs/00-divergencias.md` retorna 2
    - `grep -n "product/schema.json" docs/00-divergencias.md` retorna ao menos 1 linha
    - `grep -n "textosLegais" docs/00-divergencias.md` retorna ao menos 1 linha
    - `grep -n "avisoCarrinho" docs/00-divergencias.md docs/cms-fluxo-editorial.md` retorna ao menos 2 linhas
    - `grep -cE "^(10|13)\. ⏳" docs/00-divergencias.md` retorna 0 e `grep -cE "^(10|13)\. ℹ️" docs/00-divergencias.md` retorna 2
    - `grep -c "⏳" docs/00-divergencias.md` retorna no máximo 2 (só o item 4 e a legenda do topo)
    - `git diff --name-only cms/` não retorna nada (nenhum schema nem o seed do Strapi é alterado)
  </acceptance_criteria>

  <done>
    As duas divergências de dados apontam para os atributos reais do modelo do Strapi, o campo único de
    microcopy legal tem contrato de chaves escrito e o guia editorial diz ao editor onde cada texto
    aparece. Nenhum arquivo de `cms/` foi tocado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sincronizar docs/PLANO.md e criar os ADRs 003 e 004</name>
  <files>docs/PLANO.md, docs/00-inventario.md, docs/adr/003-rota-canonica-produto.md, docs/adr/004-deploy-ghcr-caddy.md</files>

  <read_first>
    - `docs/PLANO.md` linhas 1-6 (cabeçalho "**Estado:**"), 61-70 (FASE 07 e FASE 09), 88-95 (FASE 16)
      e 99-116 (FASE 17 completa, incluindo a linha 110 "Decisão aberta")
    - `docs/00-inventario.md` linha 23 (rota de produto registrada com duas alternativas), linha 80
      (cabeçalho "Produto (`/produto/[slug]`)") e linhas 190-202 (checklist §10 com dois itens desmarcados)
    - `docs/adr/002-locale-padrao.md` — formato obrigatório de ADR: `# ADR NNN — título`, linha
      `**Status:** Aceito (Fase NN) · **Data:** AAAA-MM-DD`, e as seções `## Contexto`, `## Decisão`,
      `## Consequências` (o ADR 001 acrescenta `## Gatilho de reversão`)
    - `.planning/intel/decisions.md` — o texto travado das duas decisões novas do usuário
    - `.planning/INGEST-CONFLICTS.md` — os warnings "Rota canônica de produto indefinida" e
      "Fase 17 — registry de imagens e reverse proxy em aberto", que dão o contexto de cada ADR
  </read_first>

  <action>
    Em `docs/PLANO.md`, aplicar cinco correções pontuais:

    1. Linha 3 (`> **Estado:** Fase 00 em andamento. Nenhuma fase de aplicação começa antes da aprovação
       deste plano.`): substituir por
       `> **Estado (2026-08-17):** Fases 00, 01 e 02 concluídas · Fase 03 (Strapi) implementada e em
       verificação · Fases 04–17 não iniciadas.`
    2. Linha 68: remover apenas o sufixo `*Confirmar antes de executar.*` — a decisão de 5 etapas está
       aprovada no item 14 de `docs/00-divergencias.md` e o restante da linha continua correto.
    3. Linha 70: remover o sufixo `*Confirmar.*` pelo mesmo motivo (item 15, faixa de investimento US$).
    4. Linha 92 (FASE 16): trocar `form 9 etapas` por `form 5 etapas` dentro da descrição do fluxo e2e.
       Nenhuma outra palavra da linha muda.
    5. FASE 17: apagar a linha 110 inteira (`- **Decisão aberta:** registry (GHCR vs. build direto na
       VPS) e proxy (Caddy é o mais simples para TLS automático). *A confirmar antes da fase.*`) e pôr
       no lugar `- **Decisão travada:** registry **GHCR** + proxy **Caddy** — ver
       `docs/adr/004-deploy-ghcr-caddy.md`.` Na lista de Entregas da mesma fase, trocar
       `reverse proxy** com TLS — Caddy/Traefik/Nginx` por `reverse proxy **Caddy** com TLS automático`.

    Acrescentar na FASE 07 do mesmo arquivo a rota canônica travada, referenciando o ADR:
    `- **Rota canônica:** `/[locale]/[categoria]/[slug]` — ver `docs/adr/003-rota-canonica-produto.md`.`

    Em `docs/00-inventario.md`: na linha 23 da tabela de páginas, substituir
    ``/[locale]/produto/[slug]` (ou `/[categoria]/[slug]`)`` por ``/[locale]/[categoria]/[slug]`` e
    ajustar o cabeçalho da linha 80 para `### Produto (`/[categoria]/[slug]`)`. No checklist §10, marcar
    `[x]` os dois itens hoje desmarcados (aprovação das 2 decisões abertas e aprovação do `docs/PLANO.md`),
    acrescentando `— aprovado 2026-08-13` no primeiro e `— aprovado 2026-08-17` no segundo.

    Criar `docs/adr/003-rota-canonica-produto.md` no formato do ADR 002, com
    `**Status:** Aceito (Fase 03) · **Data:** 2026-08-17`:
    - Contexto: `docs/00-inventario.md` registrava duas alternativas para a rota de produto; a escolha
      afeta roteamento (Fase 7), breadcrumb e `BreadcrumbList`, canônica e sitemap por locale (Fase 12)
      e o e2e (Fase 16); adiar custa reescrita de rotas e redirecionamento com perda de SEO.
    - Decisão: `/[locale]/[categoria]/[slug]`, pelo sinal semântico de URL.
    - Consequências, nomeando cada uma como trabalho de fase: (a) guarda de colisão de slug — um produto
      com slug igual ao de uma das 5 categorias quebra o roteamento, então a guarda roda no CI e não só
      em runtime (Fase 7); (b) mudança de categoria de um produto exige redirect 301 da URL antiga para
      a nova (Fase 7), porque a categoria faz parte do caminho; (c) canônica, `hreflang` e sitemap por
      locale seguem o mesmo padrão (Fase 12); (d) o slug do produto é localizado no schema
      (`product.slug` tem `i18n.localized: true`), então há uma URL por locale.
    - Gatilho de reversão: se a taxa de colisão de slugs se mostrar operacionalmente inviável para o
      editor, o fallback é `/[locale]/produto/[slug]` com 301 em massa — reversível, mas com custo de SEO.

    Criar `docs/adr/004-deploy-ghcr-caddy.md` no mesmo formato:
    - Contexto: `docs/PLANO.md` Fase 17 se contradizia — as Entregas já pressupunham GHCR enquanto a nota
      dizia que registry e proxy estavam em aberto; a escolha determina o desenho do GitHub Actions, a
      RAM necessária na VPS Hostinger e a configuração de TLS/headers em produção.
    - Decisão: build da imagem no GitHub Actions, push para **GHCR**, e a VPS faz apenas `pull` — a VPS
      nunca compila; **Caddy** como reverse proxy, com TLS automático via ACME.
    - Consequências: (a) a VPS não precisa de RAM para build, só para runtime de Next + Strapi +
      Postgres + Caddy — o dimensionamento continua sendo risco declarado da Fase 17; (b) credenciais
      ficam em GitHub Secrets e em `.env` na VPS, fora do git, e o ADR cita apenas os **nomes** das
      variáveis, nunca valores; (c) os headers de segurança da Fase 15 passam a ter dois pontos de
      aplicação (Next e Caddy) e o ADR fixa que a origem é o Next, com o Caddy sem sobrescrever;
      (d) a imagem do Strapi e a do Next são publicadas separadamente, com tag por SHA de commit.
    - Gatilho de reversão: se o GHCR ficar indisponível ou a organização exigir registry próprio, o
      `docker-compose.prod.yml` troca só a referência da imagem — o pipeline não muda de forma.
  </action>

  <verify>
    <automated>test $(grep -c "9 etapas" docs/PLANO.md) -eq 0 && test $(grep -c "Decisão aberta" docs/PLANO.md) -eq 0 && test -f docs/adr/003-rota-canonica-produto.md && test -f docs/adr/004-deploy-ghcr-caddy.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "9 etapas" docs/PLANO.md` retorna 0 e `grep -c "5 etapas" docs/PLANO.md` retorna 2 ou mais
    - `grep -c "Fase 00 em andamento" docs/PLANO.md` retorna 0
    - `grep -c "Decisão aberta" docs/PLANO.md` retorna 0 e `grep -c "004-deploy-ghcr-caddy" docs/PLANO.md` retorna 1 ou mais
    - `grep -c "Confirmar antes de executar" docs/PLANO.md` retorna 0 e `grep -c "\*Confirmar\.\*" docs/PLANO.md` retorna 0
    - `grep -c "003-rota-canonica-produto" docs/PLANO.md` retorna 1 ou mais
    - `grep -c "(ou \`/\[categoria\]/\[slug\]\`)" docs/00-inventario.md` retorna 0
    - `grep -c "^- \[ \]" docs/00-inventario.md` retorna 0
    - `docs/adr/003-rota-canonica-produto.md` contém a string `/[locale]/[categoria]/[slug]`, as seções
      `## Contexto`, `## Decisão` e `## Consequências`, e as palavras `colisão` e `301`
    - `docs/adr/004-deploy-ghcr-caddy.md` contém `GHCR`, `Caddy`, `## Contexto`, `## Decisão` e `## Consequências`
    - `grep -rniE "(ghp_|ghcr_pat|senha=|password=)[a-z0-9]" docs/adr/` retorna 0 linhas (nenhum valor de credencial nos ADRs)
    - `npm run format:check` sai com código 0 — atenção: isto **não** valida os arquivos editados aqui.
      `.prettierignore` linha 10 tem `*.md`, então o Prettier ignora todo Markdown do repositório por
      decisão do projeto. O critério fica como guarda de regressão para arquivos não-Markdown; a
      formatação dos `docs/*.md` é responsabilidade da revisão humana, não do Prettier
  </acceptance_criteria>

  <done>
    `docs/PLANO.md` e `docs/00-inventario.md` descrevem o estado real do projeto e as decisões
    aprovadas, sem nenhuma nota de "confirmar" ou "decisão aberta" pendurada, e as duas decisões novas
    do usuário existem como ADR datado, no mesmo formato dos ADRs 001 e 002, com consequências
    atribuídas às fases que vão pagá-las.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório público no GitHub → leitor externo | Todo conteúdo de `docs/` é publicado junto com o código |
| Documento de decisão → fases futuras | O que estiver escrito aqui vira premissa de implementação sem nova checagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | `docs/adr/004-deploy-ghcr-caddy.md` | mitigate | O ADR cita apenas nomes de variáveis (GitHub Secrets, `.env` da VPS); critério de aceite roda `grep -rniE "(ghp_\|ghcr_pat\|senha=\|password=)[a-z0-9]" docs/adr/` esperando 0 linhas |
| T-03-12 | Repudiation | Decisões travadas sem registro rastreável | mitigate | Cada divergência fechada é datada e cita o arquivo que a implementa; cada ADR traz `**Status:** Aceito (Fase 03) · **Data:** 2026-08-17` |
| T-03-13 | Tampering | Documentação afirmando o contrário do código | mitigate | Nenhum item é fechado por leitura da proposta: as três tarefas exigem ler o componente ou o schema real na lista de leitura obrigatória antes de escrever, e os critérios exigem citação do caminho do arquivo |
| T-03-14 | Information Disclosure | `docs/00-inventario.md` com rota canônica ambígua | mitigate | Rota unificada para `/[locale]/[categoria]/[slug]` no inventário e no ADR 003, eliminando a ambiguidade que a Fase 12 herdaria na canônica e no sitemap |
| T-03-SC | Tampering | Instalações npm/pip/cargo | mitigate | Plano exclusivamente documental — nenhum pacote é instalado. Se algum passo exigir instalação, parar e abrir checkpoint humano bloqueante antes do install |
</threat_model>

<verification>
1. `grep -c "⏳" docs/00-divergencias.md` retorna no máximo 2 (item 4 superado + legenda do topo) —
   itens 5, 6, 7, 9, 11 e 12 fechados como ✅; itens 10 e 13 reclassificados como ℹ️ INTENCIONAL
2. `grep -c "9 etapas\|Fase 00 em andamento\|Decisão aberta" docs/PLANO.md` retorna 0
3. `ls docs/adr/` lista 4 arquivos (001, 002, 003, 004)
4. `git diff --name-only` lista apenas arquivos sob `docs/` — nenhum arquivo de `src/` ou `cms/`
5. `npm run format:check` verde — guarda de regressão para arquivos não-Markdown; o Prettier ignora
   `*.md` por configuração do projeto (`.prettierignore:10`)
</verification>

<success_criteria>
- As seis divergências (5, 6, 7, 9, 11, 12) estão fechadas citando o arquivo real que as implementa
- Todo desvio entre proposta e implementação está registrado em `docs/divergencias.md` no formato D1
- `docs/PLANO.md` e `docs/00-inventario.md` refletem o estado real e as decisões aprovadas
- Os ADRs 003 e 004 existem, datados, no formato dos ADRs 001/002, sem nenhum valor de credencial
- Nenhum arquivo fora de `docs/` foi modificado
</success_criteria>

<output>
Criar `.planning/phases/03-strapi-cms/03-05-SUMMARY.md` ao concluir.
</output>
