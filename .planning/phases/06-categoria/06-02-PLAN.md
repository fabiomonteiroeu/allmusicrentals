---
phase: 06-categoria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/catalogo/filtros.ts
  - src/lib/catalogo/filtros.test.ts
  - src/components/analytics/EmissorFiltroAplicado.tsx
  - src/components/analytics/EmissoresDoCatalogo.test.tsx
  - src/components/catalogo/ToolbarDoCatalogo.test.tsx
autonomous: true
requirements: [CATG-02]

user_setup: []

must_haves:
  truths:
    - "`?sub=palcos` na URL vira um valor de filtro saneado, e um `?sub=<qualquer coisa>` fora da allowlist é descartado em silêncio"
    - "Os 58 testes e2e do catálogo e os testes unitários existentes continuam verdes sem edição de asserção de comportamento"
    - "Nenhum call site existente de `descreverChips` ou `serializarFiltros` muda de forma — os dois ganham parâmetro NOVO ao final, nunca trocam o significado de um parâmetro existente"
    - "`npm run typecheck` passa com `IdGrupoFiltro` ampliado — nenhum `Record<IdGrupoFiltro,…>` duplicado sobra no repositório"
  artifacts:
    - path: "src/lib/catalogo/filtros.ts"
      provides: "Grupo de filtro `sub` parametrizado e `CAMPO_POR_GRUPO` exportado"
      contains: "export const CAMPO_POR_GRUPO"
    - path: "src/components/analytics/EmissorFiltroAplicado.tsx"
      provides: "Emissor sem cópia local do mapa de grupos"
      contains: "CAMPO_POR_GRUPO } from '@/lib/catalogo/filtros'"
  key_links:
    - from: "src/components/analytics/EmissorFiltroAplicado.tsx"
      to: "src/lib/catalogo/filtros.ts"
      via: "import de CAMPO_POR_GRUPO"
      pattern: "from '@/lib/catalogo/filtros'"
---

<objective>
Generalizar `src/lib/catalogo/filtros.ts` por ADIÇÃO PURA para suportar o eixo `sub` (subcategoria)
com rótulo variável por categoria (D-10), e eliminar no MESMO commit a duplicação de
`Record<IdGrupoFiltro, …>` em `EmissorFiltroAplicado.tsx` que quebraria `tsc` no instante em que o
union for ampliado.

Purpose: a categoria e o catálogo compartilham uma única fonte de verdade de estado de filtro na
URL; criar um módulo paralelo foi explicitamente proibido por D-10.
Output: `filtros.ts` com o grupo `sub`, `CAMPO_POR_GRUPO` exportado, e o emissor consumindo o mapa
único.
</objective>

<execution_context>
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-categoria/06-CONTEXT.md
@.planning/phases/06-categoria/06-PATTERNS.md
</context>

<interfaces>
Estado ATUAL do módulo — assinaturas conferidas linha a linha no código em disco, **não
reexplorar e não presumir**:

```ts
export type IdGrupoFiltro = 'categoria' | 'tipo' | 'cor' | 'evento' | 'ambiente';

export interface FiltroCatalogo {
  q: string | null;
  categorias: string[];
  tiposDeItem: string[];
  cores: string[];
  tiposDeEvento: string[];
  ambientes: string[];
  ordenar: ChaveDeOrdenacao | null;
}

// PRIVADO hoje — precisa virar `export`
const CAMPO_POR_GRUPO: Record<IdGrupoFiltro,
  'categorias'|'tiposDeItem'|'cores'|'tiposDeEvento'|'ambientes'> = { … };

const CHAVES_ACEITAS = ['q','categoria','tipo','cor','evento','ambiente','ordenar'] as const;

export interface AllowlistDinamica { categorias: string[]; tiposDeEvento: string[]; cores: string[]; }

export function parseFiltrosDaUrl(sp, allowlists: AllowlistDinamica): FiltroCatalogo

// linha 239 — UM único parâmetro hoje
export function serializarFiltros(filtro: FiltroCatalogo): URLSearchParams

// linhas 282-285 — DOIS parâmetros hoje; o segundo JÁ EXISTE e JÁ está em uso
export function descreverChips(
  filtro: FiltroCatalogo,
  rotulosDinamicos: Partial<Record<IdGrupoFiltro, Record<string, string>>> = {},
): ChipAtivo[]

export function contarFiltrosAtivos(filtro: FiltroCatalogo): number
export const GRUPOS_DE_FILTRO: GrupoDeFiltro[]  // 5 grupos; `categoria`, `cor` e `evento` têm
                                                // `opcoes: []` em escopo de módulo — as opções
                                                // reais chegam por prop em tempo de render
```

**Call sites vivos de `descreverChips` que este plano NÃO pode quebrar:**
- `src/components/catalogo/ChipsDeFiltroAtivo.tsx:118` —
  `descreverChips(filtro, { categoria: mapaDeRotulos(categorias), evento: mapaDeRotulos(tiposDeEvento) })`
- `src/lib/catalogo/filtros.test.ts:219` — `descreverChips(filtro, { categoria: { moveis: 'Móveis' } })`
- `src/lib/catalogo/filtros.test.ts:201, 207, 213` — chamadas com um argumento só

`serializarFiltros` tem call site apenas em `src/lib/catalogo/filtros.test.ts:166, 173, 179`.

Os 3 helpers `filtroVazio()` locais de teste que constroem literais de `FiltroCatalogo` e vão
quebrar ao ganhar um campo obrigatório novo:
- `src/lib/catalogo/filtros.test.ts:20`
- `src/components/catalogo/ToolbarDoCatalogo.test.tsx:15`
- `src/components/analytics/EmissoresDoCatalogo.test.tsx:11`
</interfaces>

<decisoes_travadas_neste_plano>
1. **`AllowlistDinamica.subcategorias` é OPCIONAL** (`subcategorias?: string[]`), com fallback `[]`
   dentro de `parseFiltrosDaUrl`. Assim os call sites existentes do catálogo não mudam uma linha.
2. **`FiltroCatalogo.subcategorias` é OBRIGATÓRIO** (`string[]`), consistente com os outros 5 campos.
   Os 3 helpers `filtroVazio()` de teste listados acima ganham `subcategorias: []` no mesmo commit.
3. **`GRUPOS_DE_FILTRO` NÃO ganha o grupo `sub`** — o catálogo não filtra por subcategoria nesta
   fase. A categoria monta o próprio array de grupos (`montarGruposDaCategoria`, plano **06-08**).
4. **`rotulosDinamicos` de `descreverChips` PERMANECE como está, na mesma posição, com o mesmo
   tipo.** O parâmetro de grupos entra como **terceiro** parâmetro opcional, nunca no lugar do
   segundo. Motivo concreto: os 3 grupos dinâmicos de `GRUPOS_DE_FILTRO` (`categoria`, `cor`,
   `evento`) carregam `opcoes: []` em escopo de módulo — quem resolve o rótulo amigável deles hoje é
   exatamente o `rotulosDinamicos`. Trocar o segundo parâmetro por um array de grupos com default
   `GRUPOS_DE_FILTRO` regrediria silenciosamente o rótulo desses 3 grupos no catálogo em produção,
   além de quebrar `ChipsDeFiltroAtivo.tsx:118` e `filtros.test.ts:219`.
5. **`ChipsDeFiltroAtivo.tsx` não é tocado por este plano** e por isso não está em `files_modified`.
   O modo baseado em grupos que a categoria precisa é entregue pelo plano **06-12**, que é o dono
   desse arquivo. Se, ao executar este plano, `ChipsDeFiltroAtivo.tsx` precisar de qualquer edição
   para o `tsc` passar, isso é sinal de que a mudança deixou de ser aditiva — parar e revisar, não
   editar o componente aqui.
6. Nenhum campo, nome de função ou assinatura existente é removido ou renomeado.
</decisoes_travadas_neste_plano>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ampliar filtros.ts por adição pura para o eixo `sub`</name>
  <files>src/lib/catalogo/filtros.ts, src/lib/catalogo/filtros.test.ts</files>
  <read_first>
    - src/lib/catalogo/filtros.ts (integralmente — 308 linhas, todos os pontos de extensão)
    - src/lib/catalogo/filtros.test.ts (o helper `filtroVazio()` e as 4 chamadas existentes de `descreverChips`, incluindo a da linha 219 com `rotulosDinamicos`)
    - .planning/phases/06-categoria/06-RESEARCH.md §"Pattern 1: Generalização aditiva" (os 8 passos)
  </read_first>
  <behavior>
    - `parseFiltrosDaUrl({ sub: 'palcos' }, { …, subcategorias: ['palcos'] })` devolve `filtro.subcategorias === ['palcos']`
    - `parseFiltrosDaUrl({ sub: 'nao-existe' }, { …, subcategorias: ['palcos'] })` devolve `filtro.subcategorias === []`
    - `parseFiltrosDaUrl({ sub: ['a','a','b'] }, { …, subcategorias: ['a','b'] })` deduplica para `['a','b']`
    - `parseFiltrosDaUrl({ sub: 'palcos' }, { categorias: [], tiposDeEvento: [], cores: [] })` — allowlist sem `subcategorias` — devolve `[]` e não lança
    - `contarFiltrosAtivos` soma `subcategorias.length` junto com os outros 5 grupos
    - `serializarFiltros(filtro)` sem o segundo argumento produz exatamente a mesma query string de antes desta mudança
    - `descreverChips(filtro, { categoria: { moveis: 'Móveis' } })` — chamada de DOIS argumentos, forma idêntica à do teste da linha 219 e à do `ChipsDeFiltroAtivo` — continua resolvendo o rótulo dinâmico exatamente como hoje
    - `descreverChips(filtro, {}, gruposDaCategoria)` com um array de grupos passado como TERCEIRO argumento descreve os chips do grupo `sub` usando o `rotulo` do grupo e o `rotulo` da `OpcaoDeFiltro` daquele array
    - `descreverChips(filtro, { sub: { palcos: 'Palcos' } }, gruposDaCategoria)` — se a `OpcaoDeFiltro` do grupo `sub` não tiver `rotulo` casando com o valor, o mapa de rótulos dinâmicos ainda funciona como fallback, na mesma ordem de precedência de hoje (`opcoes` → `rotulosDinamicos` → valor cru)
  </behavior>
  <action>
Aplicar, em `src/lib/catalogo/filtros.ts`, as seguintes extensões — todas aditivas:

(1) `IdGrupoFiltro` ganha o membro `'sub'`.
(2) `CAMPO_POR_GRUPO` passa a ser exportado (`export const`), o tipo do valor ganha
    `'subcategorias'` na união, e o objeto ganha a chave `sub: 'subcategorias'`.
(3) `FiltroCatalogo` ganha `subcategorias: string[]`; `filtroVazio()` interno ganha
    `subcategorias: []`.
(4) `CHAVES_ACEITAS` ganha `'sub'`; `parseFiltrosDaUrl` ganha um `case 'sub':` no switch, com a
    mesma forma do `case 'evento':` — `dedup(valoresBrutos.filter((v) => allowlist.includes(v)))`,
    onde a allowlist é `allowlists.subcategorias ?? []`.
(5) `AllowlistDinamica` ganha `subcategorias?: string[]` (opcional, ver decisão travada 1). Atualizar
    o comentário de contrato do bloco para explicar por que este campo é opcional enquanto os 3
    anteriores não são.
(6) **`serializarFiltros` ganha um SEGUNDO parâmetro opcional** —
    `serializarFiltros(filtro: FiltroCatalogo, grupos: GrupoDeFiltro[] = GRUPOS_DE_FILTRO)` — e passa
    a iterar `grupos` no lugar da constante global. A aridade anterior continua válida.
(7) **`descreverChips` ganha um TERCEIRO parâmetro opcional**, preservando os dois atuais na mesma
    ordem e com o mesmo tipo:
    `descreverChips(filtro, rotulosDinamicos = {}, grupos: GrupoDeFiltro[] = GRUPOS_DE_FILTRO)`.
    O corpo troca `for (const grupo of GRUPOS_DE_FILTRO)` por `for (const grupo of grupos)` e mantém
    intacta a cadeia de precedência de rótulo já existente
    (`grupo.opcoes.find(...)?.rotulo ?? rotulosDinamicos[grupo.id]?.[valor] ?? valor`). É essa cadeia
    preservada que faz o grupo `sub` funcionar sem caso especial: os grupos da categoria chegam com
    `opcoes` preenchidas pelo CMS. Atualizar o comentário JSDoc da função para descrever os três
    parâmetros e registrar por que `rotulosDinamicos` não foi substituído (decisão travada 4).
(8) `contarFiltrosAtivos` soma `filtro.subcategorias.length`.
(9) `GRUPOS_DE_FILTRO` permanece com os mesmos 5 grupos e as mesmas opções — nenhuma linha alterada.
(10) `alternarValor` não muda de corpo.

Em `src/lib/catalogo/filtros.test.ts`: acrescentar `subcategorias: []` ao helper `filtroVazio()` da
linha 20 e escrever os testes descritos em `<behavior>`. **Nenhuma asserção existente é alterada, e
nenhuma chamada existente de `descreverChips` ou `serializarFiltros` muda de aridade** — as das
linhas 166/173/179, 201/207/213 e 219 ficam exatamente como estão. Se alguma delas precisar mudar
para compilar, a extensão não foi aditiva: corrigir a assinatura, não o teste.
  </action>
  <verify>
    <automated>npm test -- src/lib/catalogo/filtros.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const CAMPO_POR_GRUPO" src/lib/catalogo/filtros.ts` retorna `1`
    - `grep -v '^\s*[*/]' src/lib/catalogo/filtros.ts | grep -c "sub: 'subcategorias'"` retorna `1`
    - A assinatura de `descreverChips` tem 3 parâmetros, nesta ordem: `filtro`, `rotulosDinamicos`, `grupos` — e o tipo de `rotulosDinamicos` continua sendo `Partial<Record<IdGrupoFiltro, Record<string, string>>>`
    - `git diff src/lib/catalogo/filtros.test.ts | grep -c "^-.*descreverChips"` retorna `0` — nenhuma chamada existente foi reescrita
    - `git diff src/lib/catalogo/filtros.test.ts | grep -c "^-.*serializarFiltros"` retorna `0`
    - `git diff --name-only` NÃO inclui `src/components/catalogo/ChipsDeFiltroAtivo.tsx`
    - `AllowlistDinamica` declara `subcategorias?: string[]` (com `?`), e `categorias`/`tiposDeEvento`/`cores` continuam obrigatórios
    - `GRUPOS_DE_FILTRO` continua com 5 entradas e `git diff` não mostra alteração dentro do literal desse array
    - `npm test -- src/lib/catalogo/filtros.test.ts` passa, incluindo os 8 comportamentos de `<behavior>`
    - Nenhuma linha removida de uma assinatura pública exportada — `git diff src/lib/catalogo/filtros.ts | grep -c '^-.*export'` retorna `0`
  </acceptance_criteria>
  <done>`filtros.ts` aceita, serializa, conta e descreve o eixo `sub` sem que nenhum call site do catálogo precise mudar.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar a cópia local de CAMPO_POR_GRUPO no emissor e destravar o typecheck</name>
  <files>
    src/components/analytics/EmissorFiltroAplicado.tsx,
    src/components/analytics/EmissoresDoCatalogo.test.tsx,
    src/components/catalogo/ToolbarDoCatalogo.test.tsx
  </files>
  <read_first>
    - src/components/analytics/EmissorFiltroAplicado.tsx (integralmente — a cópia local nas linhas 26-37 e como `conjuntosPorGrupo` a consome)
    - src/components/analytics/EmissoresDoCatalogo.test.tsx (helper `filtroVazio()` da linha 11)
    - src/components/catalogo/ToolbarDoCatalogo.test.tsx (helper `filtroVazio()` da linha 15)
    - src/lib/catalogo/filtros.ts (o `CAMPO_POR_GRUPO` recém-exportado)
  </read_first>
  <action>
Em `src/components/analytics/EmissorFiltroAplicado.tsx`: apagar a declaração local de
`CAMPO_POR_GRUPO` e passar a importá-la de `@/lib/catalogo/filtros`, junto dos tipos
`FiltroCatalogo` e `IdGrupoFiltro` que já vêm de lá. O restante do arquivo (`GRUPOS`,
`conjuntosPorGrupo`, o `useEffect` de diff) não muda: ele já itera sobre `Object.keys(
CAMPO_POR_GRUPO)`, então o grupo `sub` passa a ser coberto automaticamente.

Acrescentar `subcategorias: []` aos dois helpers `filtroVazio()` de teste
(`EmissoresDoCatalogo.test.tsx:11` e `ToolbarDoCatalogo.test.tsx:15`), sem alterar nenhuma asserção
existente desses arquivos.

Depois, rodar `npm run typecheck` e `grep -rn "Record<IdGrupoFiltro" src/` para confirmar que
nenhuma outra cópia do mapa sobrou fora de `filtros.ts`.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- src/components/analytics src/components/catalogo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CAMPO_POR_GRUPO" src/components/analytics/EmissorFiltroAplicado.tsx` retorna um valor > 0, e `grep -c "^const CAMPO_POR_GRUPO" src/components/analytics/EmissorFiltroAplicado.tsx` retorna `0`
    - `src/components/analytics/EmissorFiltroAplicado.tsx` contém a string `CAMPO_POR_GRUPO } from '@/lib/catalogo/filtros'` ou uma linha de import equivalente que traga o símbolo desse módulo
    - `grep -rn "Record<IdGrupoFiltro" src/ | grep -v "src/lib/catalogo/filtros.ts"` não retorna nenhuma linha
    - `npm run typecheck` sai com código 0
    - `npm test -- src/components/analytics src/components/catalogo` passa sem nenhuma asserção pré-existente modificada — inclusive `ChipsDeFiltroAtivo.test.tsx`, que este plano não toca
    - Os 3 helpers `filtroVazio()` de teste contêm `subcategorias: []`
  </acceptance_criteria>
  <done>Existe um único `Record<IdGrupoFiltro, …>` no repositório, o typecheck passa com o union ampliado, e a suíte unitária do catálogo continua verde.</done>
</task>

<task type="auto">
  <name>Task 3: Provar não-regressão do catálogo em produção com a suíte e2e completa</name>
  <files>src/lib/catalogo/filtros.ts</files>
  <read_first>
    - e2e/catalogo-filtros.spec.ts (as asserções literais de query string que o RESEARCH aponta como frágeis)
    - e2e/catalogo-acessibilidade.spec.ts
    - .planning/phases/06-categoria/06-RESEARCH.md §"Pitfall 2" (os dois testes que comparam query string literalmente)
  </read_first>
  <action>
Rodar `npm run check` (typecheck + lint + jest) e `npx playwright test` inteiros — não um
subconjunto. A generalização de `filtros.ts` toca o módulo que os 58 testes e2e do catálogo
exercitam de ponta a ponta; a rede de segurança de D-10 só vale se a suíte inteira rodar.

Se algum e2e do catálogo falhar, a causa é regressão introduzida aqui e a correção é em
`filtros.ts` — NUNCA relaxar a asserção do teste e2e. Os dois testes que comparam query string
literalmente (`aplicar dois filtros, remover um, reload e goBack nunca dessincronizam` e o de
`?q=` já ativo) são o sinal exato de que a ordem de serialização mudou; nesse caso, a ordem de
`CHAVES_ACEITAS` / da montagem em `serializarFiltros` precisa preservar a ordem anterior para as 5
chaves antigas, com `sub` acrescentado ao fim.

Registrar no SUMMARY a contagem de testes que passaram nas duas suítes.
  </action>
  <verify>
    <automated>npm run check && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` sai com código 0
    - `npx playwright test` sai com código 0 e reporta pelo menos os 58 testes do catálogo passando (desktop + mobile)
    - Nenhum arquivo em `e2e/` aparece em `git diff --name-only` deste plano
    - A ordem das 5 chaves antigas na query string produzida por `serializarFiltros` é idêntica à de antes do plano (provada pelos dois e2e de query string literal passando sem edição)
  </acceptance_criteria>
  <done>Suítes unitária e e2e verdes com o union ampliado, provando que a generalização foi aditiva de fato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → rota (searchParams) | Valor de URL controlado pelo visitante entra no sistema |
| `parseFiltrosDaUrl` → `getProdutos` | Único ponto em que entrada crua vira valor de query do CMS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | `parseFiltrosDaUrl`, chave `sub` | mitigate | `?sub=` só passa se o valor estiver na allowlist `subcategorias` resolvida no servidor; valor fora da lista é descartado em silêncio, nunca vira chave de query Strapi |
| T-06-06 | Tampering | `CHAVES_ACEITAS` | mitigate | Chave desconhecida na URL continua descartada antes do switch — o comportamento é coberto pelo teste existente `uma chave desconhecida (?admin=1) não aparece no FiltroCatalogo resultante` |
| T-06-07 | Denial of Service | `parseFiltrosDaUrl` com `?sub` repetido milhares de vezes | accept | `dedup` limita o conjunto ao tamanho da allowlist real; a rota é dinâmica e o custo é O(n) sobre um array pequeno — mesmo perfil de risco já aceito no catálogo |
| T-06-42 | Tampering | rótulo de chip resolvido a partir de array de grupos recebido | mitigate | O terceiro parâmetro de `descreverChips` só influencia texto renderizado, nunca a allowlist de parse; a decisão de o que é valor válido continua exclusivamente em `parseFiltrosDaUrl` |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova nesta fase — nada a auditar |
</threat_model>

<verification>
- `npm run check` verde
- `npx playwright test` verde (58 testes do catálogo)
- Um único `Record<IdGrupoFiltro, …>` no repositório
- `ChipsDeFiltroAtivo.tsx` intocado por este plano
</verification>

<success_criteria>
O eixo `sub` existe no contrato de URL compartilhado, com rótulo parametrizável por um terceiro
parâmetro novo, sem que o catálogo em produção mude de comportamento em nenhum dos 58 e2e e sem que
nenhum call site existente precise ser reescrito.
</success_criteria>

<output>
Create `.planning/phases/06-categoria/06-02-SUMMARY.md` when done
</output>
