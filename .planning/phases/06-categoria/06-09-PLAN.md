---
phase: 06-categoria
plan: 09
type: execute
wave: 3
depends_on: ["06-05"]
files_modified:
  - src/components/categoria/ComparativoLed.tsx
  - src/components/categoria/ComparativoLed.test.tsx
  - src/components/categoria/ReguaDeDistancia.tsx
  - src/components/categoria/ReguaDeDistancia.test.tsx
autonomous: true
requirements: [CATG-03]

user_setup: []

must_haves:
  truths:
    - "A régua 0 M → 10 M é anunciada a leitores de tela como um único gráfico descrito, com os valores 1,9 m e 3,9 m também em texto visível"
    - "A tabela de 7 critérios empilha em telas estreitas sem nenhuma media query nova e sem medir viewport em JavaScript"
    - "O comparativo renderiza as 7 linhas vindas do single type do CMS, e não linhas escritas no código"
    - "O bloco tem o CTA `NÃO SEI QUAL ESCOLHER — QUERO AJUDA`"
  artifacts:
    - path: "src/components/categoria/ReguaDeDistancia.tsx"
      provides: "Régua acessível 0–10 m com marcadores em 1,9 e 3,9"
      contains: "role=\"img\""
    - path: "src/components/categoria/ComparativoLed.tsx"
      provides: "Seção escura P1.9 × P3.9 com cartões, régua, tabela e CTA"
      contains: "auto-fit"
  key_links:
    - from: "src/components/categoria/ComparativoLed.tsx"
      to: "src/components/categoria/ReguaDeDistancia.tsx"
      via: "render da régua dentro da seção"
      pattern: "ReguaDeDistancia"
---

<objective>
Entregar o bloco comparativo P1.9 × P3.9 (CATG-03): régua de distância acessível, cartões de pixel
pitch, tabela de 7 critérios que empilha por CSS puro e o CTA de ajuda.

Purpose: é o conteúdo de maior valor informativo de `telas-de-led` e o requisito mais específico da
fase. A guarda de que ele só aparece em `telas-de-led` é aplicada pela rota (plano 06-10); este
plano entrega o componente.
Output: `ReguaDeDistancia.tsx` e `ComparativoLed.tsx`, com testes.
</objective>

<execution_context>
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-categoria/06-UI-SPEC.md
@.planning/phases/06-categoria/06-PATTERNS.md
</context>

<interfaces>
```ts
// src/lib/cms/adapters.ts (plano 06-05)
export interface ComparativoLed {
  linhas: { criterio: string; valorP19: string; valorP39: string }[];
}
export async function getComparativoLed(locale: Locale): Promise<ComparativoLed | null>;
```

Precedente a copiar (`src/components/blocos/DestaqueLedBloco.tsx`, linhas 108-113) — pixel pitch é
"conteúdo de design, não do CMS":
```ts
const PIXEL_PITCH = [
  { valor: 'P1.9', legenda: 'Painéis P1.9mm' },
  { valor: 'P3.9', legenda: 'Painéis P3.9mm' },
] as const;
```
</interfaces>

<escopo_explicitamente_fora>
O single type Comparativo LED cobre **somente as 7 linhas da tabela** (D-08). A régua, os dois
valores de pixel pitch (1.9 e 3.9), os textos institucionais, o eyebrow, o título, o parágrafo
didático e o rótulo do CTA são fixos no código, pelo mesmo precedente de `DestaqueLedBloco`. Uma
tarefa que tente puxar qualquer um desses do CMS está fora do escopo desta fase.
</escopo_explicitamente_fora>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Régua de distância 0–10 m acessível</name>
  <files>src/components/categoria/ReguaDeDistancia.tsx, src/components/categoria/ReguaDeDistancia.test.tsx</files>
  <read_first>
    - src/components/blocos/DestaqueLedBloco.tsx (a seção escura, os tokens `tinta800`/`tinta700`, o padrão de constante de design fixa no componente)
    - src/lib/theme/theme.ts (`cor.tinta900`, `cor.tinta700`, `cor.teal`, `cor.fundo`, `cor.textoMutedClaro`, `fonte.mono`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"1. Régua LED (0 M → 10 M) — dado, não decoração" (o contrato completo de acessibilidade)
  </read_first>
  <behavior>
    - Renderiza um elemento com `role="img"` cujo `aria-label` cita 0, 10 metros, 1,9 metro/P1.9 e 3,9 metros/P3.9
    - Os 4 marcadores posicionados têm `aria-hidden="true"`
    - Os textos `1,9 M · P1.9` e `3,9 M · P3.9` são texto visível real, encontráveis por consulta de texto no teste
    - A posição de cada marcador é calculada como `(pitch / 10) * 100` por cento — 19% para 1.9 e 39% para 3.9 — a partir das constantes numéricas, não de strings `'19%'` escritas à mão
    - As legendas `0 M` e `10 M` usam `theme.cor.textoMutedClaro`, nunca `theme.cor.textoMuted`
  </behavior>
  <action>
Criar `src/components/categoria/ReguaDeDistancia.tsx` com `'use client'`.

Constantes de design fixas no módulo, no mesmo espírito de `PIXEL_PITCH` de `DestaqueLedBloco.tsx`:
`const ESCALA_MAXIMA_M = 10` e os dois marcadores `{ pitch: 1.9, rotulo: 'P1.9' }` e
`{ pitch: 3.9, rotulo: 'P3.9' }`. A posição percentual é derivada:
`(pitch / ESCALA_MAXIMA_M) * 100`.

Estrutura: um contêiner `role="img"` com
`aria-label="Régua de distância de visualização de 0 a 10 metros, com marcador em 1,9 metro para a
tela P1.9 e em 3,9 metros para a tela P3.9"`. Dentro dele, o trilho e os 4 `div` posicionados
(`0%`, `19%`, `39%`, `100%`) todos com `aria-hidden="true"` — eles são o desenho já descrito pelo
`aria-label`.

Abaixo do trilho, o dado numérico como texto visível real: `1,9 M · P1.9` e `3,9 M · P3.9`, e as
legendas de extremidade `0 M` e `10 M`.

Cores: trilho e marcadores de extremidade em `theme.cor.tinta700`; marcador de 1,9 em
`theme.cor.teal`; marcador de 3,9 em `theme.cor.fundo`. Legendas e rótulos em
`theme.cor.textoMutedClaro` — o layout-fonte usa `#6B7072` aqui, que reprova em 3.85:1 sobre
`tinta900` e é exatamente o defeito corrigido no rodapé em 2026-08-21. **Não copiar as cores inline
do `.dc.html`.**

Sem `@media` nova e sem nenhuma leitura de `window.innerWidth`. Escrever o teste cobrindo os 5
comportamentos.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/ReguaDeDistancia.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo contém `role="img"` e um `aria-label` que inclui as strings `0`, `10 metros`, `1,9` e `3,9`
    - `grep -c "aria-hidden" src/components/categoria/ReguaDeDistancia.tsx` retorna um valor maior ou igual a `1`
    - `grep -c "cor.textoMuted\b" src/components/categoria/ReguaDeDistancia.tsx` retorna `0`
    - `grep -c "innerWidth\|@media" src/components/categoria/ReguaDeDistancia.tsx` retorna `0`
    - O arquivo contém a expressão de cálculo da posição a partir do pitch (divisão por 10 e multiplicação por 100), e não os literais `'19%'`/`'39%'`
    - `getByText('1,9 M · P1.9')` e `getByText('3,9 M · P3.9')` encontram os nós no teste
    - `npm test -- src/components/categoria/ReguaDeDistancia.test.tsx` passa com os 5 casos
  </acceptance_criteria>
  <done>A régua é dado acessível, não decoração: descrita para leitor de tela e com os números também em texto visível.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seção escura do comparativo com cartões, tabela de 7 critérios e CTA</name>
  <files>src/components/categoria/ComparativoLed.tsx, src/components/categoria/ComparativoLed.test.tsx</files>
  <read_first>
    - src/components/blocos/DestaqueLedBloco.tsx (integralmente — seção escura, `SectionDivider`, `CardPixelPitch` com borda teal, e o comentário sobre pixel pitch ser conteúdo de design)
    - src/components/categoria/ReguaDeDistancia.tsx (criado na Task 1)
    - src/lib/cms/adapters.ts (interface `ComparativoLed`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"2. Tabela de 7 critérios" (o cálculo do `minmax(220px,1fr)` e a decisão sobre os rótulos inline) e §Color (a tabela de contraste da zebra)
    - .planning/phases/06-categoria/06-CONTEXT.md §specifics (os textos institucionais do bloco)
  </read_first>
  <behavior>
    - Com 7 linhas vindas por prop, renderiza 7 linhas de tabela, cada uma com critério, valor P1.9 e valor P3.9
    - Cada célula de valor traz um chip mono inline `P1.9` ou `P3.9` antes do valor, renderizado sempre, em qualquer largura
    - O cabeçalho `CRITÉRIO` / `P1.9MM` / `P3.9MM` está sempre presente — nunca é escondido
    - Renderiza os dois cartões de pixel pitch, com `DENSIDADE DE PONTOS ALTA` no P1.9 e `DENSIDADE DE PONTOS MÉDIA` no P3.9
    - Renderiza a `ReguaDeDistancia`
    - Renderiza o CTA com o texto `NÃO SEI QUAL ESCOLHER — QUERO AJUDA`
    - Com `linhas: []`, a seção não renderiza tabela vazia nem cabeçalho órfão
  </behavior>
  <action>
Criar `src/components/categoria/ComparativoLed.tsx` com `'use client'`.

Props: `{ linhas: { criterio: string; valorP19: string; valorP39: string }[] }`. As 7 linhas vêm do
single type do CMS — o componente não declara nenhuma linha literal.

Conteúdo fixo no código (D-08 — só a tabela é do CMS): eyebrow `ESCOLHA DA TELA`, título
`P1.9 ou P3.9: qual tela o seu evento pede`, o parágrafo didático sobre pixel pitch, o bloco
`REGRA PRÁTICA`, as duas notas de público (menos de 2 m / mais de 4 m) e o rótulo do CTA. Copiar os
textos do §specifics do CONTEXT sem paráfrase.

Seção escura sobre `theme.cor.tinta900`, com `SectionDivider` acima e abaixo (padrão de
`DestaqueLedBloco.tsx`) e `padding-block: clamp(56px, 8vw, 144px)`.

Dois cartões de pixel pitch lado a lado em `repeat(auto-fit, minmax(240px, 1fr))` com
`gap: theme.espaco[24]`. O número `1.9`/`3.9` em 52px `peso.display` e o sufixo `mm` em 26px. O
cartão P1.9 leva `border: 1px solid theme.cor.teal` e o rótulo `DENSIDADE DE PONTOS ALTA`; o cartão
P3.9 leva borda neutra (`theme.cor.tinta700`) e `DENSIDADE DE PONTOS MÉDIA` — só o P1.9 usa o accent.

Tabela: **cada linha é um grid próprio** com
`grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 16px`, aplicado por linha (3
células), não à tabela inteira. O cabeçalho usa o MESMO `grid-template-columns` e permanece sempre
renderizado. **Proibido** reproduzir a medição de viewport em JavaScript do layout-fonte
(`vw < 760`) e proibido criar `@media` nova — o empilhamento sai do `auto-fit`, como a Fase 4 já
decidiu (divergência D3).

Os rótulos inline `P1.9`/`P3.9` nas células de valor são renderizados SEMPRE, como chip mono de 12px
antes do valor (`P1.9` em `theme.cor.teal`, `P3.9` em `theme.cor.textoMutedClaro`) — em telas largas
é redundância aceitável com o cabeçalho, em telas estreitas é o que dá contexto ao valor empilhado.

Zebra: linhas alternando `theme.cor.tinta800` e `#141618`. Todo rótulo de critério e legenda usa
`theme.cor.textoMutedClaro`, nunca `theme.cor.textoMuted`.

CTA `NÃO SEI QUAL ESCOLHER — QUERO AJUDA` usando o primitivo `Button` na variante primária, inerte
até a Fase 9, com comentário citando a fase.

Quando `linhas.length === 0`, a seção renderiza os cartões, a régua e o CTA, mas nem tabela nem
cabeçalho de tabela.

Escrever o teste cobrindo os 7 comportamentos.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/ComparativoLed.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@media\|innerWidth\|window.inner" src/components/categoria/ComparativoLed.tsx` retorna `0`
    - O arquivo contém `repeat(auto-fit, minmax(220px, 1fr))`
    - `grep -c "cor.textoMuted\b" src/components/categoria/ComparativoLed.tsx` retorna `0`
    - `grep -c "Distância mínima confortável" src/components/categoria/ComparativoLed.tsx` retorna `0` — nenhuma linha da tabela é literal no código
    - O arquivo contém a string `NÃO SEI QUAL ESCOLHER — QUERO AJUDA`
    - `grep -ci "investimento" src/components/categoria/ComparativoLed.tsx` retorna `0`
    - O teste com 7 linhas encontra os 7 critérios renderizados, e o teste com `linhas: []` confirma ausência de cabeçalho de tabela
    - `npm test -- src/components/categoria/ComparativoLed.test.tsx` passa com os 7 casos
  </acceptance_criteria>
  <done>O bloco comparativo renderiza as 7 linhas do CMS, empilha por CSS puro e mantém contraste AA sobre a superfície escura.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS (single type) → DOM | As 7 linhas da tabela vêm do editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-29 | Tampering | `ComparativoLed` renderizando linhas do CMS | mitigate | `criterio`/`valorP19`/`valorP39` são `z.string()` validados na borda e renderizados como texto JSX escapado — nunca `dangerouslySetInnerHTML` |
| T-06-30 | Tampering | comparativo aparecendo fora de `telas-de-led` | mitigate | D-08 escolheu single type justamente para tornar a duplicação estruturalmente impossível; a guarda de slug é aplicada pela rota (plano 06-10) e coberta por e2e no plano 06-11 |
| T-06-31 | Information Disclosure | conteúdo com termo de preço | mitigate | O 7º critério foi reescrito no plano 06-04 e o gate anti-preço roda tanto sobre o JSON de seed quanto sobre este componente (`grep -ci investimento` retorna 0) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `npm test -- src/components/categoria/ComparativoLed.test.tsx src/components/categoria/ReguaDeDistancia.test.tsx` verde
- `npm run typecheck` verde
- Nenhuma `@media` nova e nenhuma leitura de viewport em JS nos dois arquivos
</verification>

<success_criteria>
O comparativo P1.9 × P3.9 existe com régua acessível, tabela de 7 critérios que empilha por CSS puro
e CTA, com todo o conteúdo variável vindo do CMS e todo o conteúdo de design fixo no código.
</success_criteria>

<output>
Create `.planning/phases/06-categoria/06-09-SUMMARY.md` when done
</output>
