---
phase: 06-categoria
plan: 07
type: execute
wave: 3
depends_on: ["06-02", "06-05"]
files_modified:
  - src/components/categoria/SubcategoriasNumeradas.tsx
  - src/components/categoria/SubcategoriasNumeradas.test.tsx
  - src/components/categoria/AplicacoesDaCategoria.tsx
  - src/components/categoria/AplicacoesDaCategoria.test.tsx
  - src/components/categoria/FaqDaCategoria.tsx
  - src/components/categoria/FaqDaCategoria.test.tsx
autonomous: true
requirements: [CATG-01]

user_setup: []

must_haves:
  truths:
    - "O visitante vê as subcategorias numeradas (01, 02, 03…) com nome e descrição vindos do CMS"
    - "Clicar numa subcategoria aplica o filtro `sub` na URL e leva o visitante até a grade, sem trocar de rota"
    - "O visitante vê as aplicações da categoria, ou um estado de conteúdo em preparação quando não há nenhuma"
    - "O FAQ abre uma pergunta por vez e é operável por teclado"
  artifacts:
    - path: "src/components/categoria/SubcategoriasNumeradas.tsx"
      provides: "Grade numerada de subcategorias que filtra a grade"
      contains: "auto-fit"
    - path: "src/components/categoria/AplicacoesDaCategoria.tsx"
      provides: "Lista de aplicações com estado vazio próprio"
    - path: "src/components/categoria/FaqDaCategoria.tsx"
      provides: "Accordion exclusivo do FAQ da categoria"
      contains: "collapsible"
  key_links:
    - from: "src/components/categoria/SubcategoriasNumeradas.tsx"
      to: "src/lib/catalogo/filtros.ts"
      via: "alternarValor sobre o grupo `sub`"
      pattern: "alternarValor"
    - from: "src/components/categoria/FaqDaCategoria.tsx"
      to: "@radix-ui/react-accordion"
      via: "Accordion.Root type=single collapsible"
      pattern: "react-accordion"
---

<objective>
Entregar as três seções de conteúdo editorial da página de categoria que CATG-01 exige: as
subcategorias numeradas (que também são o eixo de filtro estruturante da página), as aplicações e o
FAQ da categoria.

Purpose: são as seções que provam "hero, subcategorias numeradas, aplicações e FAQ próprios do CMS".
Output: três componentes em `src/components/categoria/`, com testes.
</objective>

<execution_context>
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-categoria/06-UI-SPEC.md
@.planning/phases/06-categoria/06-PATTERNS.md
</context>

<interfaces>
Contratos já disponíveis (não reexplorar o codebase para descobri-los):

```ts
// src/lib/cms/adapters.ts (estendido no plano 06-05)
export interface Subcategoria { id: number; nome: string; slug: string; descricao: string | null; ordem: number }
export interface Categoria { …; aplicacoes: { nome: string; descricao: string | null }[]; emPreparacao: boolean }
export interface FaqItem { id: number; pergunta: string; respostaHtml: HtmlSeguro; destaque: boolean }
export async function getFaq(locale, { categoria?: string }): Promise<FaqItem[]>

// src/lib/catalogo/filtros.ts (estendido no plano 06-02)
export type IdGrupoFiltro = 'categoria'|'tipo'|'cor'|'evento'|'ambiente'|'sub';
export function alternarValor(sp: URLSearchParams, chave: IdGrupoFiltro, valor: string): URLSearchParams
```

`respostaHtml` já vem sanitizado por `sanitizarRichText` no adaptador — o componente renderiza o
valor já saneado pelo mesmo caminho que `GradeDeProdutos`/`SliderDeProdutos` usam hoje, sem
sanitizar de novo e sem construir HTML por concatenação.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grade de subcategorias numeradas que filtra a grade de produtos</name>
  <files>src/components/categoria/SubcategoriasNumeradas.tsx, src/components/categoria/SubcategoriasNumeradas.test.tsx</files>
  <read_first>
    - src/components/catalogo/PainelDeFiltros.tsx (o padrão `useSearchParams` + `alternarValor` + `useTransition` + `router.push`)
    - src/components/blocos/GradeDeCategoriasBloco.tsx linhas 100-130 (o grid `auto-fit`/`minmax` e o card)
    - src/lib/theme/theme.ts (tokens `borda`, `textoMuted`, `tinta600`, `tealLink`, `fonte.mono`, `tamanho[12]`, `raio.base`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"4. Subcategorias numeradas" e §Copywriting (o subtítulo que varia por categoria)
  </read_first>
  <behavior>
    - Com 5 subcategorias, renderiza 5 células numeradas `01`…`05` (dois dígitos, zero à esquerda)
    - Cada célula mostra `nome` e `descricao`; célula com `descricao` nula renderiza sem parágrafo vazio
    - A contagem ao lado do título usa o comprimento real do array, nunca um número fixo
    - Clicar numa célula chama `router.push` com a query resultante de `alternarValor(searchParams, 'sub', slug)` acrescida do fragmento `#produtos`
    - Clicar de novo na mesma célula remove aquele valor da URL (o toggle é o mesmo `alternarValor`)
    - Cada célula é um `<button type="button">` com `aria-pressed` refletindo se aquele slug está ativo na URL
    - O componente aceita `subtitulo` por prop e o renderiza; quando a prop não é passada, usa o default `Navegue por subcategoria`
  </behavior>
  <action>
Criar `src/components/categoria/SubcategoriasNumeradas.tsx` com `'use client'`.

Props: `{ subcategorias: Subcategoria[]; titulo: string; subtitulo?: string }`. O `subtitulo` tem
default `'Navegue por subcategoria'`; a rota passa `'O que compõe uma tela de LED'` quando o slug é
`telas-de-led` (Copywriting do UI-SPEC).

Cabeçalho da seção: título em Archivo caixa-alta `theme.fluido.h2`, e ao lado a contagem
`${subcategorias.length} SUBCATEGORIAS` em `theme.fonte.mono`, `theme.tamanho[13]`,
`theme.cor.textoMuted` — derivada do array, nunca hardcoded.

Grade: `display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1px;
background: theme.cor.borda`, com cada célula de fundo `theme.cor.fundo` — é essa combinação que
produz a linha divisória fina de 1px sem `border` por célula. Nenhuma `@media` nova.

Cada célula é um `<button type="button" aria-pressed={ativo}>` contendo: o número
`String(indice + 1).padStart(2, '0')` em mono 12px `theme.cor.textoMuted`; o `nome` em Public Sans
`peso.medio` 17px; a `descricao` em Public Sans 15px `theme.cor.tinta600` (renderizada só quando
não-nula); e o rótulo final `VER ITENS →` em Archivo 800 13px `theme.cor.tealLink` (correto aqui —
a célula tem fundo claro). `min-height: 44px` no botão. Foco visível com
`outline: 2px solid theme.cor.tealLink`.

Comportamento de clique: `const proximos = alternarValor(searchParams, 'sub', sub.slug)` e
`iniciarTransicao(() => router.push(\`?${proximos.toString()}#produtos\`))`, no mesmo formato de
`PainelDeFiltros.tsx`. O componente NÃO navega para outra rota.

Escrever o teste com Testing Library mockando `next/navigation` (`useRouter`, `useSearchParams`) no
mesmo padrão já usado em `src/components/catalogo/PainelDeFiltros.test.tsx`, cobrindo os 7
comportamentos.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/SubcategoriasNumeradas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo começa com `'use client'` e `grep -c "@media" src/components/categoria/SubcategoriasNumeradas.tsx` retorna `0`
    - Contém `repeat(auto-fit, minmax(240px, 1fr))` e `gap: 1px`
    - Contém `padStart(2, '0')` — a numeração é derivada do índice, não escrita à mão
    - Contém `aria-pressed` e `alternarValor`
    - Não contém `router.push('/` nem `next/link` — a célula filtra, não navega para outra rota
    - O teste prova que o push inclui `#produtos` no destino
    - `npm test -- src/components/categoria/SubcategoriasNumeradas.test.tsx` passa com os 7 casos
  </acceptance_criteria>
  <done>As subcategorias numeradas exibem o conteúdo do CMS e funcionam como o eixo de filtro estruturante da página.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seção de aplicações com estado vazio próprio</name>
  <files>src/components/categoria/AplicacoesDaCategoria.tsx, src/components/categoria/AplicacoesDaCategoria.test.tsx</files>
  <read_first>
    - src/components/feedback/EmptyState.tsx (props `eyebrow`/`titulo`/`texto`/`children`)
    - src/components/blocos/GradeDeCategoriasBloco.tsx (grid `auto-fit` sobre fundo claro)
    - .planning/phases/06-categoria/06-UI-SPEC.md §Copywriting linha "Empty state 'sem aplicações'/'sem FAQ'" (a cópia exata)
    - src/lib/theme/theme.ts (`clamp(40px,5vw,64px)` como padding de seção clara)
  </read_first>
  <behavior>
    - Com 6 aplicações, renderiza 6 itens com `nome` e `descricao`
    - Com 0 aplicações, renderiza o `EmptyState` com o eyebrow `CONTEÚDO EM PREPARAÇÃO` e o corpo exato do UI-SPEC, e nenhum item de lista
    - Item com `descricao` nula renderiza só o nome, sem parágrafo vazio
    - O título da seção vem por prop
  </behavior>
  <action>
Criar `src/components/categoria/AplicacoesDaCategoria.tsx` com `'use client'`.

Props: `{ aplicacoes: { nome: string; descricao: string | null }[]; titulo: string; subtitulo?: string }`.

Seção clara (`theme.cor.fundo`) com `padding-block: clamp(40px, 5vw, 64px)`. Grade
`repeat(auto-fit, minmax(240px, 1fr))` com `gap: theme.espaco[16]`. Cada item: `nome` em Public Sans
`peso.medio`, `descricao` em Public Sans 15px `theme.cor.tinta600`.

Quando `aplicacoes.length === 0`, renderizar o `EmptyState` existente com:
eyebrow `CONTEÚDO EM PREPARAÇÃO`, e texto exatamente
"As aplicações desta categoria serão publicadas junto com os produtos. Descreva o tipo de evento na
solicitação e a equipe indica a configuração adequada." (cópia literal do UI-SPEC — não parafrasear).
Não renderizar grade vazia nem lista de zero itens.

Sem `@media` nova. Escrever o teste cobrindo os 4 comportamentos, incluindo a asserção da cópia
literal do estado vazio.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/AplicacoesDaCategoria.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo começa com `'use client'` e não contém `@media`
    - Contém a string exata `CONTEÚDO EM PREPARAÇÃO`
    - Contém a frase exata "As aplicações desta categoria serão publicadas junto com os produtos."
    - Com `aplicacoes: []`, o teste confirma que nenhum item de lista é renderizado
    - `npm test -- src/components/categoria/AplicacoesDaCategoria.test.tsx` passa com os 4 casos
  </acceptance_criteria>
  <done>A seção de aplicações exibe o conteúdo do CMS e degrada com cópia própria nas 3 categorias que ainda não têm aplicações cadastradas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FAQ da categoria em accordion exclusivo</name>
  <files>src/components/categoria/FaqDaCategoria.tsx, src/components/categoria/FaqDaCategoria.test.tsx</files>
  <read_first>
    - src/components/catalogo/PainelDeFiltros.tsx linhas 30-100 (o uso de `@radix-ui/react-accordion` e o CSS de `Trigger`/`Content`/`Item` a reaproveitar — lá é `type="multiple"`)
    - src/lib/cms/adapters.ts (interface `FaqItem` e o campo `respostaHtml` já sanitizado)
    - src/components/catalogo/GradeDeProdutos.tsx (como o projeto renderiza um valor `HtmlSeguro`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §Copywriting (estado vazio do FAQ) e §Acessibilidade
  </read_first>
  <behavior>
    - Com 6 itens, renderiza 6 gatilhos com o texto da pergunta
    - Abrir a segunda pergunta fecha a primeira — só uma aberta por vez
    - Clicar na pergunta aberta fecha ela (comportamento `collapsible`)
    - Cada gatilho tem `aria-expanded` refletindo o estado
    - Com 0 itens, renderiza o `EmptyState` com o eyebrow `CONTEÚDO EM PREPARAÇÃO` e o corpo do UI-SPEC interpolando o nome da categoria, e as linhas de telefone e e-mail quando essas props forem informadas
    - Quando `telefone` e `email` não são informados, o estado vazio renderiza sem a linha de contato, sem placeholder inventado
  </behavior>
  <action>
Criar `src/components/categoria/FaqDaCategoria.tsx` com `'use client'`.

Props: `{ itens: FaqItem[]; titulo: string; nomeCategoria: string; telefone?: string | null;
email?: string | null }`.

Usar `@radix-ui/react-accordion` com `<Accordion.Root type="single" collapsible>` — exclusividade é
a diferença deliberada em relação a `PainelDeFiltros.tsx`, que é `type="multiple"`. Reaproveitar o
CSS de `Trigger`/`Content`/`Item` daquele arquivo como ponto de partida; o indicador é o glifo `▾`
com `aria-hidden="true"` (o projeto não usa icon set). `value` de cada item é `String(item.id)`.

O corpo da resposta renderiza `item.respostaHtml`, que já vem sanitizado por `sanitizarRichText` no
adaptador — usar o mesmo mecanismo de render de `HtmlSeguro` já empregado em
`GradeDeProdutos.tsx`/`SliderDeProdutos.tsx`. Não chamar sanitizador de novo e não montar HTML por
concatenação de string.

Quando `itens.length === 0`, renderizar o `EmptyState` com eyebrow `CONTEÚDO EM PREPARAÇÃO` e o
texto literal do UI-SPEC: "Ainda não publicamos as perguntas frequentes de {nomeCategoria}. Enquanto
isso, fale direto com a equipe — respondemos por telefone, e-mail ou junto com a proposta.",
interpolando `nomeCategoria`. Telefone e e-mail aparecem como linhas de contato só quando as props
correspondentes vierem preenchidas (a rota as lê de `getSettingsGlobais`, que já expõe `telefone` e
`email`); ausentes, a linha simplesmente não é renderizada.

Sem `@media` nova. Foco visível nos gatilhos. Escrever o teste cobrindo os 6 comportamentos.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/FaqDaCategoria.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo começa com `'use client'` e importa de `@radix-ui/react-accordion`
    - `grep -c 'type="single"' src/components/categoria/FaqDaCategoria.tsx` retorna `1` e o arquivo contém `collapsible`
    - `grep -c 'type="multiple"' src/components/categoria/FaqDaCategoria.tsx` retorna `0`
    - O teste prova que abrir o segundo item fecha o primeiro
    - O teste prova que o estado vazio interpola o nome da categoria e omite a linha de contato quando `telefone`/`email` não são passados
    - O componente não chama `sanitizarRichText` nem `sanitizarHtml` — o valor já chega saneado
    - `npm test -- src/components/categoria/FaqDaCategoria.test.tsx` passa com os 6 casos
  </acceptance_criteria>
  <done>O FAQ da categoria usa accordion exclusivo acessível e degrada com cópia própria nas categorias sem perguntas cadastradas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS (rich text) → DOM | `respostaHtml` do FAQ é HTML vindo do editor |
| clique do visitante → URL | Célula de subcategoria escreve query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-22 | Tampering | `FaqDaCategoria` renderizando `respostaHtml` | mitigate | O HTML já passou por `sanitizarRichText` em `adaptarFaqItem`; o componente consome o tipo `HtmlSeguro` pelo mesmo mecanismo de `GradeDeProdutos` e nunca concatena string em HTML |
| T-06-23 | Tampering | `SubcategoriasNumeradas` escrevendo na URL | mitigate | O valor vem de `Subcategoria.slug` do CMS (validado por Zod no plano 06-05), passa por `alternarValor` (que opera sobre `URLSearchParams`, com escape automático) e é reconferido pela allowlist de `parseFiltrosDaUrl` na próxima renderização |
| T-06-24 | Information Disclosure | estado vazio do FAQ com telefone/e-mail | accept | `telefone` e `email` de `settings-globais` já são públicos no rodapé do site desde a Fase 2 — nenhum dado novo é exposto |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova — `@radix-ui/react-accordion` já está em `package.json` desde a Fase 5 |
</threat_model>

<verification>
- `npm test -- src/components/categoria` verde
- `npm run typecheck` verde
- `grep -rc "@media" src/components/categoria/` retorna `0` em todos os arquivos deste plano
</verification>

<success_criteria>
As três seções editoriais da categoria renderizam conteúdo do CMS, degradam com cópia própria quando
vazias, e a lista de subcategorias funciona como controle de filtro sem trocar de rota.
</success_criteria>

<output>
Create `.planning/phases/06-categoria/06-07-SUMMARY.md` when done
</output>
