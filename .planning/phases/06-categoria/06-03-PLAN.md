---
phase: 06-categoria
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/categoria/trilha.ts
  - src/lib/categoria/trilha.test.ts
  - src/components/chrome/Breadcrumb.tsx
  - src/components/chrome/Breadcrumb.test.tsx
  - src/components/categoria/HeroDaCategoria.tsx
  - src/components/categoria/HeroDaCategoria.test.tsx
autonomous: true
requirements: [CATG-01, CATG-05]

user_setup: []

must_haves:
  truths:
    - "O visitante vê a trilha `Início / Catálogo / {nome da categoria}` no topo da página e consegue navegar por ela pelo teclado"
    - "O último nível da trilha não é link e é anunciado como página atual"
    - "O hero da categoria mostra eyebrow, título e descrição vindos do CMS sobre a imagem de hero"
    - "A lista ordenada de produtos da categoria existe como estrutura de dados tipada, pronta para a Fase 12 serializar em JSON-LD"
  artifacts:
    - path: "src/lib/categoria/trilha.ts"
      provides: "construirTrilha e construirItemList — dados de navegação e ItemList"
      exports: ["construirTrilha", "construirItemList", "NivelDeTrilha", "ItemDeLista"]
    - path: "src/components/chrome/Breadcrumb.tsx"
      provides: "Componente visual de trilha reutilizável (Fase 7 também consome)"
      contains: "aria-current"
    - path: "src/components/categoria/HeroDaCategoria.tsx"
      provides: "Hero escuro da categoria com breadcrumb, eyebrow, título, descrição e CTAs"
  key_links:
    - from: "src/components/categoria/HeroDaCategoria.tsx"
      to: "src/components/chrome/Breadcrumb.tsx"
      via: "import e render do Breadcrumb dentro do hero"
      pattern: "Breadcrumb"
---

<objective>
Entregar os dois artefatos de navegação que CATG-05 exige — o componente `Breadcrumb` (primeiro do
projeto) e a estrutura de dados `ItemList` — mais o hero escuro da categoria, que o UI-SPEC define
como a âncora visual da página.

Purpose: são as peças sem dependência de CMS novo; entregá-las na onda 1 tira o caminho crítico da
rota. A Fase 7 (Produto) reusa o mesmo `Breadcrumb`.
Output: `src/lib/categoria/trilha.ts`, `src/components/chrome/Breadcrumb.tsx`,
`src/components/categoria/HeroDaCategoria.tsx`, com testes.
</objective>

<execution_context>
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-categoria/06-UI-SPEC.md
@.planning/phases/06-categoria/06-PATTERNS.md
</context>

<escopo_explicitamente_fora>
**Nenhum `<script type="application/ld+json">` é escrito nesta fase.** CATG-05 pede a estrutura de
dados; a serialização em JSON-LD (`BreadcrumbList`/`ItemList`) é entrega da Fase 12. Uma tarefa que
mencione `application/ld+json` é escopo vazando — ver Pitfall 5 do RESEARCH.
</escopo_explicitamente_fora>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo puro de trilha e ItemList</name>
  <files>src/lib/categoria/trilha.ts, src/lib/categoria/trilha.test.ts</files>
  <read_first>
    - src/lib/catalogo/filtros.ts (cabeçalho do arquivo — o padrão de "módulo PURO: sem styled, sem React, sem diretiva de servidor")
    - src/i18n/config.ts (tipo `Locale`)
    - src/lib/cms/adapters.ts linhas 455-490 (interface `Categoria` e `Produto` — os campos `slug` e `nome`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"5. Breadcrumb (componente novo)" (trilha de 3 níveis fixos)
  </read_first>
  <behavior>
    - `construirTrilha('pt-BR', { nome: 'Telas de LED', slug: 'telas-de-led' })` devolve 3 níveis, nesta ordem: `{ nome: 'Início', href: '/pt-BR' }`, `{ nome: 'Catálogo', href: '/pt-BR/catalogo' }`, `{ nome: 'Telas de LED', href: null }`
    - O último nível sempre tem `href: null` — é a página atual, nunca um link
    - `construirTrilha('en', …)` produz os mesmos rótulos pt-BR com hrefs prefixados por `/en` (mesma regra do catálogo: cópia de UI em pt-BR primeiro, locale só muda o caminho)
    - `construirItemList('pt-BR', 'telas-de-led', [produtoA, produtoB])` devolve `[{ posicao: 1, nome, href: '/pt-BR/telas-de-led/<slug-a>' }, { posicao: 2, … }]`
    - `posicao` começa em 1 e é sequencial e contínua
    - `construirItemList(locale, slug, [])` devolve `[]` e não lança
    - Nenhum item do ItemList contém campo de preço — a interface `ItemDeLista` não declara nenhum
  </behavior>
  <action>
Criar `src/lib/categoria/trilha.ts` como módulo puro (sem `'use client'`, sem `server-only`, sem
`styled`, importável dos dois lados — mesmo contrato do cabeçalho de `src/lib/catalogo/filtros.ts`).

Exportar a interface `NivelDeTrilha { nome: string; href: string | null }` e a função
`construirTrilha(locale: Locale, categoria: { nome: string; slug: string }): NivelDeTrilha[]`,
devolvendo os 3 níveis fixos descritos em `<behavior>`. Os rótulos `Início` e `Catálogo` são
constantes de módulo em pt-BR, seguindo a mesma via dos outros textos de UI do catálogo (a cópia
não vem do dicionário de `src/i18n/dictionaries/` — ele hoje só tem `meta` e `common`).

Exportar a interface `ItemDeLista { posicao: number; nome: string; href: string }` e a função
`construirItemList(locale: Locale, categoriaSlug: string, produtos: { slug: string; nome: string }[]):
ItemDeLista[]`. O `href` usa a rota canônica de produto `/{locale}/{categoriaSlug}/{produtoSlug}`
(fixada em 2026-08-17; dá 404 até a Fase 7, mesma regra já aceita nas Fases 4 e 5). `ItemDeLista`
NÃO declara nenhum campo de preço, valor ou moeda.

Escrever `src/lib/categoria/trilha.test.ts` cobrindo os 7 comportamentos de `<behavior>`.
  </action>
  <verify>
    <automated>npm test -- src/lib/categoria/trilha.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/categoria/trilha.ts` não contém `'use client'`, `server-only`, `styled` nem `import React`
    - `construirTrilha` devolve array de tamanho 3 com o terceiro elemento tendo `href === null`
    - `construirItemList` devolve `posicao` iniciando em 1 e `href` no formato `/{locale}/{categoriaSlug}/{produtoSlug}`
    - `grep -iE "preco|price|valor|moeda|currency" src/lib/categoria/trilha.ts` não retorna nenhuma linha
    - `npm test -- src/lib/categoria/trilha.test.ts` passa com os 7 casos
  </acceptance_criteria>
  <done>A trilha e o ItemList existem como dado tipado e testado, sem nenhuma marcação JSON-LD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Componente Breadcrumb sobre fundo escuro</name>
  <files>src/components/chrome/Breadcrumb.tsx, src/components/chrome/Breadcrumb.test.tsx</files>
  <read_first>
    - src/components/chrome/Header.tsx (padrão `NavLink` de link mono sobre `tinta900`, e `IconeBotao`/`CartLink` para o alvo de toque de 44px com texto pequeno)
    - src/components/chrome/Footer.tsx (padrão de prop com fallback estático, `FooterProps`)
    - src/lib/theme/theme.ts (tokens `textoMutedClaro`, `teal`, `fundo`, `fonte.mono`, `tamanho[13]`, `tracking.rotulo`, `cor.tealLink`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"5. Breadcrumb" e §Color (a tabela de contraste que proíbe `tealLink` sobre fundo escuro)
    - src/lib/categoria/trilha.ts (o tipo `NivelDeTrilha` criado na Task 1)
  </read_first>
  <behavior>
    - Renderiza `<nav aria-label="Trilha de navegação">` contendo um `<ol>` com um `<li>` por nível
    - Níveis com `href` não-nulo renderizam `<a href>`; o nível com `href === null` renderiza texto com `aria-current="page"` e NÃO é um `<a>`
    - Os separadores `/` entre níveis têm `aria-hidden="true"`
    - Recebe `niveis: NivelDeTrilha[]` por prop — o componente não constrói trilha nenhuma
    - Cada link é focável por teclado e recebe outline visível no `:focus-visible`
  </behavior>
  <action>
Criar `src/components/chrome/Breadcrumb.tsx` com `'use client'` no topo (declara `styled`, logo lê o
`ThemeContext` — hard constraint do projeto).

Interface: `BreadcrumbProps { niveis: NivelDeTrilha[]; rotuloDaNavegacao?: string }`, com
`rotuloDaNavegacao` default `'Trilha de navegação'` (padrão de prop com fallback estático de
`Footer.tsx`).

Estrutura semântica exata: `<nav aria-label={rotuloDaNavegacao}><ol>`, `<li>` por nível. Link para
`href` não-nulo; `<span aria-current="page">` para o nível final. Separador `/` como elemento
`aria-hidden="true"` com `padding: 0 8px` e cor `textoMutedClaro`.

Estilo (tokens exatos do UI-SPEC — não usar os tokens de `NavLink` do Header, que são outros):
`font-family: theme.fonte.mono`, `font-size: theme.tamanho[13]`,
`letter-spacing: theme.tracking.rotulo`, sem `text-transform` forçado. Link inativo:
`theme.cor.textoMutedClaro`. Hover: `theme.cor.teal`. Nível atual: `theme.cor.fundo`.
Alvo de toque: `min-height: 44px` com `display: inline-flex; align-items: center` em cada elemento
clicável. Foco: `outline: 2px solid theme.cor.teal` — o componente vive sobre `tinta900`, e
`tealLink` é PROIBIDO sobre fundo escuro (3.83:1, reprovado na tabela de Color do UI-SPEC).

Nenhuma media query nova. Nenhum ícone SVG — o separador é o glifo `/`, como no layout-fonte.

Escrever `src/components/chrome/Breadcrumb.test.tsx` com Testing Library, cobrindo os 5
comportamentos de `<behavior>`, incluindo a asserção de que o nível final não é um elemento `a`.
  </action>
  <verify>
    <automated>npm test -- src/components/chrome/Breadcrumb.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/chrome/Breadcrumb.tsx` começa com `'use client'`
    - O arquivo contém `aria-label`, `aria-current="page"` e `aria-hidden="true"`
    - `grep -c "tealLink" src/components/chrome/Breadcrumb.tsx` retorna `0`
    - `grep -c "@media" src/components/chrome/Breadcrumb.tsx` retorna `0`
    - O componente contém `min-height: 44px` (ou o token equivalente) no elemento clicável
    - `npm test -- src/components/chrome/Breadcrumb.test.tsx` passa com os 5 casos, incluindo a asserção de que o nível atual não é `a`
    - O componente não importa `construirTrilha` — recebe `niveis` por prop
  </acceptance_criteria>
  <done>Existe um `Breadcrumb` acessível e reutilizável em `src/components/chrome/`, pronto para a Fase 7 consumir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hero escuro da categoria</name>
  <files>src/components/categoria/HeroDaCategoria.tsx, src/components/categoria/HeroDaCategoria.test.tsx</files>
  <read_first>
    - src/components/blocos/DestaqueLedBloco.tsx (padrão de seção escura, `SectionDivider`, uso de `tinta900`/`tinta800`)
    - src/components/blocos/GradeDeCategoriasBloco.tsx (padrão de card com imagem do CMS e `next/image`, e o link `/${locale}/categoria/${slug}`)
    - src/lib/cms/adapters.ts (interfaces `Categoria` e `Imagem` — campos `hero`, `descricao`, `nome`, `slug`)
    - src/components/chrome/Breadcrumb.tsx (criado na Task 2)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"Foco Visual Primário", §Typography, §Color, §"Copywriting Contract" (CTA secundário `VER PRODUTOS`)
  </read_first>
  <behavior>
    - Renderiza o `Breadcrumb` recebendo `niveis` por prop
    - Renderiza o `nome` e a `descricao` da categoria vindos do CMS
    - Quando `categoria.hero` é `null`, o hero renderiza sem quebrar e sem `<img>` órfã
    - O CTA secundário `VER PRODUTOS` é uma âncora para `#produtos`
    - O CTA primário `SOLICITAR ORÇAMENTO` está presente e inerte (sem destino real até a Fase 9)
    - Nenhum texto renderizado contém símbolo de moeda ou dígito precedido de `$`
  </behavior>
  <action>
Criar `src/components/categoria/HeroDaCategoria.tsx` com `'use client'`.

Props: `{ categoria: Categoria; niveis: NivelDeTrilha[]; locale: Locale; eyebrow?: string }`. O
`eyebrow` é opcional porque o schema do CMS não tem campo de eyebrow por categoria; quando ausente,
o hero não renderiza a linha de eyebrow (não inventar texto).

Seção escura sobre `theme.cor.tinta900`, com a imagem `categoria.hero` como fundo/ilustração via
`next/image` no mesmo padrão já usado em `GradeDeCategoriasBloco.tsx` (incluindo o `alt` vindo de
`Imagem.alt`). Padding vertical `clamp(56px, 8vw, 144px)` (escala de seção escura do UI-SPEC).

Tipografia: título em `theme.fluido.h2` com `peso.display`, Archivo caixa-alta,
`font-variation-settings: 'wdth' 75`, cor `theme.cor.fundo`. Descrição em `theme.tamanho[16]`,
`leading.corpo`, cor `theme.cor.textoMutedClaro` (7.68:1 sobre `tinta900` — NUNCA `textoMuted`, que
reprova em 3.85:1).

O `Breadcrumb` é renderizado no topo do hero, acima do eyebrow/título.

Dois CTAs, usando o primitivo `Button` já existente: `SOLICITAR ORÇAMENTO` (variante primária,
inerte — sem `href`, sem `onClick` que navegue, com comentário citando a Fase 9) e `VER PRODUTOS`
(variante secundária/outline, `href="#produtos"`).

Sem media query nova; fluidez por `clamp()`.

Escrever `src/components/categoria/HeroDaCategoria.test.tsx` cobrindo os 6 comportamentos, incluindo
o caso `hero: null`.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/HeroDaCategoria.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/categoria/HeroDaCategoria.tsx` começa com `'use client'`
    - `grep -c "@media" src/components/categoria/HeroDaCategoria.tsx` retorna `0`
    - `grep -c "cor.textoMuted\b" src/components/categoria/HeroDaCategoria.tsx` retorna `0` (só `textoMutedClaro` é permitido sobre fundo escuro)
    - O arquivo contém `href="#produtos"` e a string `VER PRODUTOS`
    - O arquivo contém a string `SOLICITAR ORÇAMENTO` e um comentário citando a Fase 9 como destino futuro
    - O teste do caso `hero: null` passa sem erro de render
    - `npm test -- src/components/categoria/HeroDaCategoria.test.tsx` passa com os 6 casos
  </acceptance_criteria>
  <done>O hero — âncora visual da página segundo o UI-SPEC — renderiza breadcrumb, conteúdo do CMS e os dois CTAs, com contraste correto sobre fundo escuro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS → componente React | `nome`/`descricao`/`alt` vindos do Strapi são renderizados como texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | `HeroDaCategoria` renderizando texto do CMS | mitigate | Todo texto do CMS é renderizado como filho de JSX (escapado pelo React), nunca por `dangerouslySetInnerHTML`; o hero não recebe rich text |
| T-06-09 | Information Disclosure | `construirItemList` gerando hrefs | accept | Os hrefs são derivados de slugs públicos já expostos no catálogo; nenhum identificador interno (`documentId`, id numérico) entra na URL |
| T-06-10 | Spoofing | Breadcrumb com nível controlado por dado do CMS | mitigate | O componente recebe `niveis` prontos e só o último nível vem do CMS (`categoria.nome`), renderizado como texto e nunca como `href` |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `npm test -- src/lib/categoria src/components/chrome/Breadcrumb.test.tsx src/components/categoria` verde
- `npm run typecheck` verde
- Nenhuma ocorrência de `application/ld+json` nos arquivos deste plano
</verification>

<success_criteria>
Breadcrumb acessível, trilha e ItemList tipados e testados, e hero escuro da categoria com contraste
AA — todos independentes das mudanças de CMS, prontos para a rota montar na onda 4.
</success_criteria>

<output>
Create `.planning/phases/06-categoria/06-03-SUMMARY.md` when done
</output>
