---
phase: 06-categoria
plan: 08
type: execute
wave: 3
depends_on: ["06-02", "06-05"]
files_modified:
  - src/lib/categoria/grupos.ts
  - src/lib/categoria/grupos.test.ts
  - src/components/categoria/FiltrosToggleDaCategoria.tsx
  - src/components/categoria/FiltrosToggleDaCategoria.test.tsx
  - src/components/categoria/EstadoEmPreparacao.tsx
  - src/components/categoria/EstadoEmPreparacao.test.tsx
  - src/components/catalogo/EstadoSemResultados.tsx
  - src/components/catalogo/EstadoSemResultados.test.tsx
autonomous: true
requirements: [CATG-02, CATG-04]

user_setup: []

must_haves:
  truths:
    - "Os filtros da categoria são botões toggle horizontais sempre visíveis, visualmente distintos do acordeão do catálogo"
    - "O rótulo do grupo de subcategoria muda por categoria sem hardcode de slug no componente"
    - "Uma categoria sem produtos publicados, ou com a flag `emPreparacao` marcada, mostra o card 'em preparação' com a cópia travada em D-03"
    - "Uma combinação de filtros sem resultado mostra o estado 'sem resultado' com eyebrow e corpo próprios da categoria"
    - "O catálogo continua exibindo exatamente o mesmo estado 'sem resultado' de antes"
  artifacts:
    - path: "src/lib/categoria/grupos.ts"
      provides: "Monta os 3 grupos de filtro da categoria a partir das subcategorias do CMS"
      exports: ["montarGruposDaCategoria", "ROTULO_SUB_PADRAO"]
    - path: "src/components/categoria/FiltrosToggleDaCategoria.tsx"
      provides: "Botões toggle horizontais com aria-pressed, sem accordion"
      contains: "aria-pressed"
    - path: "src/components/categoria/EstadoEmPreparacao.tsx"
      provides: "Card do estado em preparação com cópia fixa em código"
  key_links:
    - from: "src/components/categoria/FiltrosToggleDaCategoria.tsx"
      to: "src/lib/catalogo/filtros.ts"
      via: "alternarValor sobre os grupos sub/ambiente/tipo"
      pattern: "alternarValor"
    - from: "src/lib/categoria/grupos.ts"
      to: "src/lib/catalogo/filtros.ts"
      via: "reuso das opções fixas de GRUPOS_DE_FILTRO"
      pattern: "GRUPOS_DE_FILTRO"
---

<objective>
Entregar o painel de filtros toggle da categoria (CATG-02) e os dois estados da grade (CATG-04): o
card "em preparação" — componente novo com cópia fixa — e o "sem resultado", que reusa
`EstadoSemResultados.tsx` parametrizado, sem duplicar componente.

Purpose: CATG-02 exige contraste de padrão explícito com o acordeão do catálogo, e D-04 proibiu
criar um segundo componente de estado vazio.
Output: `src/lib/categoria/grupos.ts`, dois componentes novos e a extensão de props de
`EstadoSemResultados.tsx`.
</objective>

<execution_context>
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-categoria/06-UI-SPEC.md
@.planning/phases/06-categoria/06-PATTERNS.md
</context>

<interfaces>
Estado ATUAL de `src/components/catalogo/EstadoSemResultados.tsx` (144 linhas), confirmado por
leitura direta — **não é um componente que já aceita props de conteúdo**:

```ts
export interface EstadoSemResultadosProps {
  locale: Locale;                    // única prop hoje
}
const TEXTO_SEM_CORRESPONDENCIA = 'Nenhum produto do catálogo combina com todos os filtros ' +
  'aplicados ao mesmo tempo. Tente remover o filtro mais específico, ou envie sua necessidade — ' +
  'trabalhamos com itens que ainda não estão publicados.';
// eyebrow 'BUSCA SEM CORRESPONDÊNCIA', título e as 3 sugestões estão HARDCODED no arquivo
// `removerTodosOsFiltros` (linhas 87-96) já preserva `q` e `ordenar`
```

Contratos vindos de outros planos:
```ts
// 06-02
export interface GrupoDeFiltro { id: IdGrupoFiltro; rotulo: string; abertoPorPadrao: boolean;
  swatch?: true; nota?: string; opcoes: { rotulo: string; valor: string }[] }
export const GRUPOS_DE_FILTRO: GrupoDeFiltro[];   // 5 grupos; `tipo` e `ambiente` com opções fixas
export function alternarValor(sp: URLSearchParams, chave: IdGrupoFiltro, valor: string): URLSearchParams
export interface FiltroCatalogo { …; subcategorias: string[] }
// 06-05
export interface Subcategoria { id: number; nome: string; slug: string; descricao: string | null; ordem: number }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Montador dos 3 grupos de filtro da categoria com rótulo variável</name>
  <files>src/lib/categoria/grupos.ts, src/lib/categoria/grupos.test.ts</files>
  <read_first>
    - src/lib/catalogo/filtros.ts (`GrupoDeFiltro`, `GRUPOS_DE_FILTRO` e as opções fixas de `tipo` e `ambiente`)
    - src/lib/cms/adapters.ts (interface `Subcategoria`)
    - .planning/phases/06-categoria/06-CONTEXT.md §D-09 e §specifics (os 3 eixos entregues e o rótulo que varia)
    - .planning/phases/06-categoria/06-RESEARCH.md §"Don't Hand-Roll" (por que o rótulo não pode ser `if (slug === 'telas-de-led')` dentro do componente React)
  </read_first>
  <behavior>
    - `montarGruposDaCategoria(subcategorias, 'Configuração')` devolve 3 grupos, nesta ordem: `sub`, `ambiente`, `tipo`
    - O grupo `sub` recebe o rótulo passado por argumento; sem argumento, usa `ROTULO_SUB_PADRAO` (`'Subcategoria'`)
    - As opções do grupo `sub` são `{ rotulo: sub.nome, valor: sub.slug }`, na ordem em que as subcategorias chegam
    - Os grupos `ambiente` e `tipo` reaproveitam as opções fixas já declaradas em `GRUPOS_DE_FILTRO` — o módulo não declara literal de opção nenhum para eles
    - Com `subcategorias: []`, o grupo `sub` não é incluído (não renderizar um grupo vazio), e o resultado tem 2 grupos
    - Nenhum dos 3 grupos é `categoria`, `cor` ou `evento` — a categoria não filtra por esses eixos
  </behavior>
  <action>
Criar `src/lib/categoria/grupos.ts` como módulo puro (sem `styled`, sem React, sem diretiva de
servidor — mesmo contrato de `src/lib/catalogo/filtros.ts`).

Exportar a constante `ROTULO_SUB_PADRAO = 'Subcategoria'` e a função
`montarGruposDaCategoria(subcategorias: Subcategoria[], rotuloSub: string = ROTULO_SUB_PADRAO):
GrupoDeFiltro[]`.

O grupo `sub` é montado a partir das subcategorias recebidas, com `abertoPorPadrao: true` (o valor
não tem efeito visual aqui, já que não há accordion, mas o tipo o exige). Os grupos `ambiente` e
`tipo` são obtidos de `GRUPOS_DE_FILTRO` por `find((g) => g.id === 'ambiente')` /
`find((g) => g.id === 'tipo')` — copiados, nunca reescritos, para que as opções fixas continuem com
uma única fonte de verdade. Se algum `find` devolver `undefined`, omitir aquele grupo em vez de
lançar.

O rótulo `'Configuração'` de `telas-de-led` NÃO aparece neste módulo — quem decide o rótulo é a rota
(plano 06-10), que passa a string. Este módulo não conhece slug de categoria nenhum.

Escrever `src/lib/categoria/grupos.test.ts` cobrindo os 6 comportamentos.
  </action>
  <verify>
    <automated>npm test -- src/lib/categoria/grupos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "telas-de-led" src/lib/categoria/grupos.ts` retorna `0`
    - `grep -c "Interno ou externo" src/lib/categoria/grupos.ts` retorna `0` — as opções de `ambiente` vêm de `GRUPOS_DE_FILTRO`, não são reescritas
    - O módulo não contém `'use client'`, `styled` nem `import React`
    - `montarGruposDaCategoria([], 'X')` devolve exatamente 2 grupos
    - A ordem devolvida com subcategorias é `['sub','ambiente','tipo']`
    - `npm test -- src/lib/categoria/grupos.test.ts` passa com os 6 casos
  </acceptance_criteria>
  <done>Os 3 eixos de filtro da categoria são montados a partir do dado do CMS, com rótulo parametrizado e sem duplicar as opções fixas do catálogo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Botões toggle horizontais, deliberadamente distintos do acordeão do catálogo</name>
  <files>src/components/categoria/FiltrosToggleDaCategoria.tsx, src/components/categoria/FiltrosToggleDaCategoria.test.tsx</files>
  <read_first>
    - src/components/catalogo/PainelDeFiltros.tsx (o mecanismo de URL a reusar — `useSearchParams`/`useRouter`/`useTransition`/`alternarValor` — e o padrão de accordion a NÃO reproduzir)
    - src/components/catalogo/SwatchesDeCor.tsx (o único componente do projeto com botão `aria-pressed` fora de checkbox — copiar o padrão de interação, não o CSS do swatch)
    - src/lib/theme/theme.ts (`cor.borda`, `cor.branco`, `cor.tinta600`, `cor.tinta900`, `cor.fundo`, `raio.base`, `tamanho[15]`, `tamanho[12]`, `tracking.rotuloForte`, `fonte.corpo`, `fonte.mono`)
    - .planning/phases/06-categoria/06-UI-SPEC.md §"3. Filtros toggle da categoria" (as regras de estado e a proibição de usar `teal` no estado ativo)
  </read_first>
  <behavior>
    - Renderiza todos os grupos recebidos ao mesmo tempo, sem nenhum controle de expandir/colapsar
    - Cada opção é um `<button type="button" aria-pressed>` — nunca um `<input type="checkbox">`
    - Clicar numa opção chama `router.push` com a query de `alternarValor(searchParams, grupo.id, opcao.valor)`
    - Clicar numa opção já ativa a desativa
    - O rótulo de cada grupo é renderizado acima dos botões daquele grupo
    - Não existe nenhum elemento com `role="region"` de accordion nem componente Radix Accordion na árvore
  </behavior>
  <action>
Criar `src/components/categoria/FiltrosToggleDaCategoria.tsx` com `'use client'`.

Props: `{ grupos: GrupoDeFiltro[]; filtro: FiltroCatalogo }`. O componente lê o estado ativo de
`filtro` (via `CAMPO_POR_GRUPO[grupo.id]`, agora exportado por `filtros.ts`), não de estado local —
a URL continua sendo a única fonte de verdade.

Layout: contêiner `display: flex; flex-wrap: wrap; gap: 24px 32px` entre grupos. Dentro de cada
grupo, rótulo acima (mono, `tamanho[12]`, `tracking.rotuloForte`, `cor.textoMuted`, caixa-alta) e os
botões em `display: flex; flex-wrap: wrap; gap: theme.espaco[8]`. **Nenhum `Accordion.Root`,
`Accordion.Item` ou import de `@radix-ui/react-accordion`** — é exatamente o contraste que CATG-02
exige.

Estado do botão, com os tokens exatos do UI-SPEC:
- desligado: `border: 1px solid theme.cor.borda`, `background: theme.cor.branco`,
  `color: theme.cor.tinta600`;
- ligado: `border: 1px solid theme.cor.tinta900`, `background: theme.cor.tinta900`,
  `color: theme.cor.fundo`. **Nunca `teal`** — o accent é reservado a CTA/eyebrow e usá-lo aqui
  colidiria com o botão de orçamento logo abaixo;
- `min-height: 36px`, `padding: 0 12px`, `border-radius: theme.raio.base`,
  `font-size: theme.tamanho[15]`, `font-family: theme.fonte.corpo` (Public Sans, não mono — outro
  ponto de distinção em relação ao chip mono do catálogo);
- foco: `outline: 2px solid theme.cor.tealLink` (o componente vive sobre fundo claro).

Interação: `const proximos = alternarValor(searchParams, grupo.id, opcao.valor)` e
`iniciarTransicao(() => router.push(\`?${proximos.toString()}\`))`, no mesmo formato de
`PainelDeFiltros.tsx`.

Nenhuma `@media` nova. Escrever o teste cobrindo os 6 comportamentos, incluindo a asserção negativa
de que não há checkbox nem accordion na árvore renderizada.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/FiltrosToggleDaCategoria.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "react-accordion" src/components/categoria/FiltrosToggleDaCategoria.tsx` retorna `0`
    - `grep -c 'type="checkbox"' src/components/categoria/FiltrosToggleDaCategoria.tsx` retorna `0`
    - `grep -c "cor.teal\b" src/components/categoria/FiltrosToggleDaCategoria.tsx` retorna `0` (o estado ativo usa `tinta900`/`fundo`, não o accent)
    - `grep -c "@media" src/components/categoria/FiltrosToggleDaCategoria.tsx` retorna `0`
    - O arquivo contém `aria-pressed`, `alternarValor` e `flex-wrap`
    - O teste confirma que `screen.queryAllByRole('checkbox')` tem comprimento 0
    - `npm test -- src/components/categoria/FiltrosToggleDaCategoria.test.tsx` passa com os 6 casos
  </acceptance_criteria>
  <done>O painel de filtros da categoria é toggle horizontal sempre visível, com mecânica de URL idêntica à do catálogo e apresentação deliberadamente distinta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Estado "em preparação" novo e parametrização real de EstadoSemResultados</name>
  <files>
    src/components/categoria/EstadoEmPreparacao.tsx,
    src/components/categoria/EstadoEmPreparacao.test.tsx,
    src/components/catalogo/EstadoSemResultados.tsx,
    src/components/catalogo/EstadoSemResultados.test.tsx
  </files>
  <read_first>
    - src/components/catalogo/EstadoSemResultados.tsx (integralmente, 144 linhas — a interface de UMA prop, as 3 constantes hardcoded e `removerTodosOsFiltros`)
    - src/components/catalogo/EstadoSemResultados.test.tsx (as asserções existentes, que precisam continuar passando SEM edição)
    - src/components/chrome/Footer.tsx linhas 124-134 (o padrão de prop com fallback estático a replicar)
    - src/components/feedback/EmptyState.tsx (props do invólucro)
    - .planning/phases/06-categoria/06-CONTEXT.md §specifics (as duas cópias exatas) e §D-03/D-04
  </read_first>
  <behavior>
    - `EstadoEmPreparacao` renderiza o eyebrow `{N} SUBCATEGORIAS MAPEADAS · CADASTRO EM ANDAMENTO` com `N` vindo de prop, o título e o corpo exatos de D-03
    - `EstadoEmPreparacao` renderiza dois botões: `SOLICITAR ORÇAMENTO` (inerte) e `VER TODO O CATÁLOGO` apontando para `/{locale}/catalogo`
    - `EstadoSemResultados` sem nenhuma prop nova renderiza exatamente o mesmo eyebrow, título, corpo e as 3 sugestões de hoje
    - `EstadoSemResultados` com `eyebrow`/`texto` informados renderiza os valores passados
    - `EstadoSemResultados` com `mostrarSugestoesDeCategoria={false}` renderiza só o botão `Remover todos os filtros`, sem `Ver painéis de LED` nem `Ver mesas de coquetel`
    - `removerTodosOsFiltros` continua preservando `q` e `ordenar` nos dois modos
  </behavior>
  <action>
Criar `src/components/categoria/EstadoEmPreparacao.tsx` com `'use client'`. Props:
`{ locale: Locale; totalDeSubcategorias: number }`. A cópia é FIXA no código (D-03 — é cópia
institucional, não varia por categoria); só o `{N}` do eyebrow vem de prop.

Textos literais, sem paráfrase:
- eyebrow: `${totalDeSubcategorias} SUBCATEGORIAS MAPEADAS · CADASTRO EM ANDAMENTO`
- título: `Os itens desta categoria ainda não estão publicados`
- corpo: `Já trabalhamos com os equipamentos listados acima. Enquanto o cadastro é concluído,
  descreva o que seu evento precisa e a equipe responde com o que temos disponível na data.`
- ações: `SOLICITAR ORÇAMENTO` (inerte até a Fase 9 — sem `href`, com comentário citando a fase) e
  `VER TODO O CATÁLOGO` com destino real `/${locale}/catalogo`.

Usar o `EmptyState` existente como invólucro. Contêiner com
`border: 1px solid theme.cor.borda`, `background: theme.cor.branco`,
`border-radius: theme.raio.base`, `padding: clamp(28px, 4vw, 48px)`, `max-width: 720px`,
`display: grid`, `gap: theme.espaco[24]` — o UI-SPEC exige que este contêiner seja idêntico ao do
"sem resultado", porque os dois estados compartilham o invólucro e diferem só no texto.

Em `src/components/catalogo/EstadoSemResultados.tsx`, estender a interface para
`{ locale: Locale; eyebrow?: string; texto?: string; mostrarSugestoesDeCategoria?: boolean }`, com
os defaults apontando para os valores atuais: `eyebrow = 'BUSCA SEM CORRESPONDÊNCIA'`,
`texto = TEXTO_SEM_CORRESPONDENCIA`, `mostrarSugestoesDeCategoria = true`. Isto é uma extensão real
da interface — hoje o componente aceita só `locale`, e eyebrow/título/corpo/sugestões estão
hardcoded. O título (`Amplie a busca ou fale com a equipe`) **não vira prop**: o UI-SPEC confirma que
a categoria reusa o mesmo título literal. Quando `mostrarSugestoesDeCategoria` é `false`, só a
primeira sugestão (`Remover todos os filtros`) é renderizada. `removerTodosOsFiltros` não muda de
lógica.

Em `EstadoSemResultados.test.tsx`, acrescentar os 3 casos novos de `<behavior>`. **Nenhuma asserção
existente do arquivo pode ser alterada** — elas são a prova de que o catálogo em produção não
regrediu.
  </action>
  <verify>
    <automated>npm test -- src/components/categoria/EstadoEmPreparacao.test.tsx src/components/catalogo/EstadoSemResultados.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EstadoSemResultadosProps` declara `eyebrow?`, `texto?` e `mostrarSugestoesDeCategoria?`, todas opcionais, e `locale` continua obrigatória
    - `git diff src/components/catalogo/EstadoSemResultados.test.tsx | grep -c '^-'` conta apenas linhas de contexto de bloco (nenhuma asserção `expect` pré-existente removida ou alterada)
    - Renderizar `<EstadoSemResultados locale="pt-BR" />` produz o eyebrow `BUSCA SEM CORRESPONDÊNCIA` e as 3 sugestões, exatamente como hoje
    - Renderizar com `mostrarSugestoesDeCategoria={false}` produz exatamente 1 botão de sugestão
    - `EstadoEmPreparacao` contém as três strings literais de D-03, conferidas caractere a caractere no teste
    - `EstadoEmPreparacao` contém `/${locale}/catalogo` como destino do segundo botão e um comentário citando a Fase 9 para o primeiro
    - Os dois arquivos de componente começam com `'use client'` e não contêm `@media`
    - `npm test -- src/components/categoria/EstadoEmPreparacao.test.tsx src/components/catalogo/EstadoSemResultados.test.tsx` passa
  </acceptance_criteria>
  <done>Os dois estados da grade existem: um componente novo para "em preparação" e o de "sem resultado" parametrizado de verdade, sem duplicação e sem regressão no catálogo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clique do visitante → query string | Botão toggle escreve valores de filtro na URL |
| CMS → rótulo de opção | `Subcategoria.nome` vira texto de botão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-25 | Tampering | `FiltrosToggleDaCategoria` → URL | mitigate | Só valores presentes em `grupo.opcoes` (derivados do CMS validado por Zod) podem ser escritos; a leitura de volta passa pela allowlist de `parseFiltrosDaUrl` |
| T-06-26 | Tampering | `EstadoSemResultados.removerTodosOsFiltros` | mitigate | A lógica de preservar `q`/`ordenar` não é alterada por este plano; os testes pré-existentes ficam intactos como guarda de regressão |
| T-06-27 | Information Disclosure | eco de entrada do visitante no estado vazio | mitigate | Nenhum dos dois estados recebe o termo buscado por prop — regra já estabelecida na Fase 5 (T-05-29) e preservada aqui: as props novas de `EstadoSemResultados` são texto institucional, nunca entrada do usuário |
| T-06-28 | Repudiation | flag `emPreparacao` divergindo do que o visitante vê | accept | A decisão do estado é feita no servidor (plano 06-10) por `emPreparacao OU contagem zero`; o componente não decide nada, só renderiza |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `npm test -- src/components/categoria src/components/catalogo src/lib/categoria` verde
- `npm run typecheck` verde
- Nenhum import de `@radix-ui/react-accordion` no componente de filtros da categoria
</verification>

<success_criteria>
Os filtros da categoria são visivelmente e estruturalmente distintos do acordeão do catálogo, e os
dois estados da grade existem com a cópia travada — sem duplicar `EstadoSemResultados` e sem
regressão nas asserções existentes do catálogo.
</success_criteria>

<output>
Create `.planning/phases/06-categoria/06-08-SUMMARY.md` when done
</output>
