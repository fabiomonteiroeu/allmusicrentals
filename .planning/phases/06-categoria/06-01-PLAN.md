---
phase: 06-categoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cms/src/api/subcategoria/content-types/subcategoria/schema.json
  - cms/src/api/subcategoria/controllers/subcategoria.ts
  - cms/src/api/subcategoria/routes/subcategoria.ts
  - cms/src/api/subcategoria/services/subcategoria.ts
  - cms/src/api/comparativo-led/content-types/comparativo-led/schema.json
  - cms/src/api/comparativo-led/controllers/comparativo-led.ts
  - cms/src/api/comparativo-led/routes/comparativo-led.ts
  - cms/src/api/comparativo-led/services/comparativo-led.ts
  - cms/src/components/shared/aplicacao.json
  - cms/src/components/shared/linha-comparativo.json
  - cms/src/components/shared/subcategoria.json
  - cms/src/api/category/content-types/category/schema.json
  - cms/src/api/product/content-types/product/schema.json
  - cms/src/index.ts
  - docs/divergencias.md
autonomous: true
requirements: [CATG-01, CATG-03, CATG-04]

user_setup: []

must_haves:
  truths:
    - "O Strapi local sobe sem erro com os 4 modelos novos/alterados"
    - "`subcategoria` é um content-type real, alvo de relação, e não mais um componente"
    - "Um produto pode pertencer a várias subcategorias ao mesmo tempo"
    - "As 5 categorias existentes passam a ter `emPreparacao` gravado como `false`, não `NULL`"
  artifacts:
    - path: "cms/src/api/subcategoria/content-types/subcategoria/schema.json"
      provides: "Content-type Subcategoria com nome/slug/descricao/ordem + relações"
      contains: "api::category.category"
    - path: "cms/src/api/comparativo-led/content-types/comparativo-led/schema.json"
      provides: "Single type Comparativo LED com as 7 linhas da tabela"
      contains: "singleType"
    - path: "cms/src/components/shared/aplicacao.json"
      provides: "Componente repetível de aplicação da categoria"
      contains: "components_shared_aplicacoes"
    - path: "cms/src/components/shared/linha-comparativo.json"
      provides: "Componente tipado de linha da tabela comparativa"
      contains: "valorP19"
  key_links:
    - from: "cms/src/api/category/content-types/category/schema.json"
      to: "api::subcategoria.subcategoria"
      via: "relation oneToMany mappedBy categoria"
      pattern: "api::subcategoria.subcategoria"
    - from: "cms/src/api/product/content-types/product/schema.json"
      to: "api::subcategoria.subcategoria"
      via: "relation manyToMany inversedBy produtos"
      pattern: "manyToMany"
    - from: "cms/src/index.ts"
      to: "PUBLIC_READ"
      via: "garantirPermissoesPublicas"
      pattern: "'subcategoria'"
---

<objective>
Modelar no Strapi as quatro mudanças de schema que sustentam toda a Fase 6: o componente
`shared.aplicacao` (D-05), a flag `emPreparacao` (D-01), a promoção de `subcategoria` a
content-type relacional (D-09) e o single type Comparativo LED (D-08). Nenhuma linha de front-end
é tocada aqui.

Purpose: sem estes schemas, nada mais da fase existe — o seed (06-04), a camada de leitura (06-05)
e a rota (06-09) leem formas que ainda não existem no CMS.
Output: 2 content-types novos, 2 componentes novos, 2 schemas estendidos, `PUBLIC_READ` ampliado,
backfill idempotente de `emPreparacao`, e a decisão dos dois componentes órfãos registrada.
</objective>

<execution_context>
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomonteiro/projects/AllMusicBr/rentals/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-categoria/06-CONTEXT.md
@.planning/phases/06-categoria/06-PATTERNS.md
</context>

<decisoes_travadas_neste_plano>
Três pontos que o RESEARCH deixou em aberto e que o executor NÃO deve reabrir:

1. **`cms/src/components/shared/subcategoria.json` é REMOVIDO nesta fase.** Ele fica órfão assim
   que `category.subcategorias` deixa de ser componente. Contingência especificada na Task 3.
2. **`cms/src/components/blocos/comparativo-led.json` NÃO é removido e NÃO é reusado.** Ele é
   membro da Dynamic Zone `page.blocos` e tem contraparte Zod (`blocoComparativoLed` em
   `src/lib/cms/schemas.ts`); removê-lo exigiria migração de Dynamic Zone mais mudança de união Zod,
   com zero ganho para CATG-03. A supersessão é registrada em `docs/divergencias.md` (Task 3).
3. **`draftAndPublish: false` no single type do comparativo**, espelhando `settings-globais`
   (assunção A3 do RESEARCH, confirmada aqui como decisão).
</decisoes_travadas_neste_plano>

<tasks>

<task type="auto">
  <name>Task 1: Criar os content-types e componentes novos do Strapi</name>
  <files>
    cms/src/api/subcategoria/content-types/subcategoria/schema.json,
    cms/src/api/subcategoria/controllers/subcategoria.ts,
    cms/src/api/subcategoria/routes/subcategoria.ts,
    cms/src/api/subcategoria/services/subcategoria.ts,
    cms/src/api/comparativo-led/content-types/comparativo-led/schema.json,
    cms/src/api/comparativo-led/controllers/comparativo-led.ts,
    cms/src/api/comparativo-led/routes/comparativo-led.ts,
    cms/src/api/comparativo-led/services/comparativo-led.ts,
    cms/src/components/shared/aplicacao.json,
    cms/src/components/shared/linha-comparativo.json
  </files>
  <read_first>
    - cms/src/api/tipo-de-evento/content-types/tipo-de-evento/schema.json (molde do content-type de taxonomia com relação N:N)
    - cms/src/api/tipo-de-evento/controllers/tipo-de-evento.ts, routes/tipo-de-evento.ts, services/tipo-de-evento.ts (boilerplate de 3 linhas)
    - cms/src/api/settings-globais/content-types/settings-globais/schema.json (único single type do projeto)
    - cms/src/api/settings-globais/controllers/settings-globais.ts (boilerplate de single type)
    - cms/src/components/shared/subcategoria.json (forma a espelhar em aplicacao.json)
    - .planning/phases/06-categoria/06-PATTERNS.md §"Atribuições de Padrão" (excertos literais dos 4 moldes)
  </read_first>
  <action>
Criar `cms/src/api/subcategoria/content-types/subcategoria/schema.json` como `kind: "collectionType"`,
`collectionName: "subcategorias"`, `info.singularName: "subcategoria"`, `info.pluralName:
"subcategorias"`, `info.displayName: "Subcategoria"`, `options.draftAndPublish: true`,
`pluginOptions.i18n.localized: true`. Atributos, todos com `pluginOptions.i18n.localized: true`
exceto onde indicado: `nome` (string, required), `slug` (uid, `targetField: "nome"`, required),
`descricao` (text), `ordem` (integer, `default: 0`, i18n localized **false**), `categoria`
(relation `manyToOne`, `target: "api::category.category"`, `inversedBy: "subcategorias"`) e
`produtos` (relation `manyToMany`, `target: "api::product.product"`, `mappedBy: "subcategorias"`).
NÃO copiar `exibirNoFiltroDoCatalogo` de `tipo-de-evento` — é específico daquele modelo.

Criar os 3 arquivos de boilerplate de `subcategoria` com `factories.createCoreController` /
`createCoreRouter` / `createCoreService` sobre o UID `api::subcategoria.subcategoria`, idênticos em
forma aos de `tipo-de-evento`.

Criar `cms/src/api/comparativo-led/content-types/comparativo-led/schema.json` como
`kind: "singleType"`, `collectionName: "comparativo_led"`, `info.singularName: "comparativo-led"`,
`info.pluralName: "comparativo-led"`, `info.displayName: "Comparativo LED (P1.9 x P3.9)"`,
`options.draftAndPublish: false`, `pluginOptions.i18n.localized: true`, com o único atributo
`linhas` (component repetível `shared.linha-comparativo`, `pluginOptions.i18n.localized: true`).
Criar os 3 arquivos de boilerplate sobre o UID `api::comparativo-led.comparativo-led`.

Criar `cms/src/components/shared/aplicacao.json` com `collectionName:
"components_shared_aplicacoes"`, `info.displayName: "Aplicação"`, `info.icon: "bulletList"`, e os
atributos `nome` (string, required) e `descricao` (text) — mesma forma exata de
`shared/subcategoria.json`.

Criar `cms/src/components/shared/linha-comparativo.json` com `collectionName:
"components_shared_linhas_comparativo"`, `info.displayName: "Linha do Comparativo LED"`, e os
atributos `criterio` (string, required), `valorP19` (string, required), `valorP39` (string,
required).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=JSON.parse(fs.readFileSync('cms/src/api/subcategoria/content-types/subcategoria/schema.json','utf8'));if(s.kind!=='collectionType')throw new Error('kind');if(s.attributes.produtos.relation!=='manyToMany')throw new Error('produtos deve ser manyToMany');if(s.attributes.categoria.relation!=='manyToOne')throw new Error('categoria deve ser manyToOne');if(s.attributes.categoria.inversedBy!=='subcategorias')throw new Error('inversedBy');const c=JSON.parse(fs.readFileSync('cms/src/api/comparativo-led/content-types/comparativo-led/schema.json','utf8'));if(c.kind!=='singleType')throw new Error('comparativo deve ser singleType');if(c.options.draftAndPublish!==false)throw new Error('draftAndPublish deve ser false');if(c.attributes.linhas.component!=='shared.linha-comparativo')throw new Error('component');const l=JSON.parse(fs.readFileSync('cms/src/components/shared/linha-comparativo.json','utf8'));['criterio','valorP19','valorP39'].forEach(k=>{if(!l.attributes[k])throw new Error('falta '+k)});const a=JSON.parse(fs.readFileSync('cms/src/components/shared/aplicacao.json','utf8'));if(a.collectionName!=='components_shared_aplicacoes')throw new Error('collectionName aplicacao');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cms/src/api/subcategoria/content-types/subcategoria/schema.json` existe, `kind === "collectionType"`, e tem exatamente os atributos `nome`, `slug`, `descricao`, `ordem`, `categoria`, `produtos`
    - `subcategoria.attributes.produtos.relation === "manyToMany"` e `mappedBy === "subcategorias"`
    - `subcategoria.attributes.categoria.relation === "manyToOne"` e `inversedBy === "subcategorias"`
    - `subcategoria.attributes.slug.type === "uid"` com `targetField === "nome"`
    - Nenhum dos 6 atributos de `subcategoria` se chama `exibirNoFiltroDoCatalogo`
    - `comparativo-led/schema.json` tem `kind === "singleType"`, `options.draftAndPublish === false`, e um único atributo `linhas` apontando para `shared.linha-comparativo`
    - `cms/src/components/shared/linha-comparativo.json` tem os 3 atributos `criterio`/`valorP19`/`valorP39`, todos `required: true`
    - `cms/src/components/shared/aplicacao.json` tem `nome` (required) e `descricao`, e `collectionName === "components_shared_aplicacoes"`
    - Os 6 arquivos de boilerplate existem e cada um contém a string do UID correspondente (`api::subcategoria.subcategoria` / `api::comparativo-led.comparativo-led`)
    - O comando `node -e` da verificação sai com código 0 imprimindo `OK`
  </acceptance_criteria>
  <done>Os 2 content-types novos e os 2 componentes novos existem em disco com a forma exata acima, sem nenhuma alteração ainda em `category`/`product`.</done>
</task>

<task type="auto">
  <name>Task 2: Estender category e product e ampliar PUBLIC_READ com o backfill de emPreparacao</name>
  <files>
    cms/src/api/category/content-types/category/schema.json,
    cms/src/api/product/content-types/product/schema.json,
    cms/src/index.ts
  </files>
  <read_first>
    - cms/src/api/category/content-types/category/schema.json (o campo `subcategorias` atual e o molde `produtos` oneToMany/mappedBy)
    - cms/src/api/product/content-types/product/schema.json (o campo `tiposDeEvento` como molde exato da nova relação)
    - cms/src/index.ts (integralmente: `PUBLIC_READ`, `garantirPermissoesPublicas`, `garantirContagemSolicitacoes`, `bootstrap`)
    - .planning/STATE.md §Blockers — lição "`default` em schema.json não faz backfill"
  </read_first>
  <action>
Em `cms/src/api/category/content-types/category/schema.json`: trocar o atributo `subcategorias` de
`{ "type": "component", "repeatable": true, "component": "shared.subcategoria", ... }` para
`{ "type": "relation", "relation": "oneToMany", "target": "api::subcategoria.subcategoria",
"mappedBy": "categoria" }` — MESMO nome de campo, sem `pluginOptions` (relação segue o molde de
`category.produtos`). Acrescentar dois atributos novos, sem tocar nos demais: `aplicacoes`
(component repetível `shared.aplicacao`, `pluginOptions.i18n.localized: true`) e `emPreparacao`
(boolean, `default: false`, `pluginOptions.i18n.localized: false`).

Em `cms/src/api/product/content-types/product/schema.json`: acrescentar o atributo `subcategorias`
(relation `manyToMany`, `target: "api::subcategoria.subcategoria"`, `inversedBy: "produtos"`) —
plural e `manyToMany`, porque o layout-fonte tem um produto de serviço (`montagem-estrutura`)
pertencendo a 3 subcategorias ao mesmo tempo. Nenhum atributo existente é renomeado ou removido.

Em `cms/src/index.ts`:
(a) acrescentar `'subcategoria'` e `'comparativo-led'` ao array `PUBLIC_READ`;
(b) em `garantirPermissoesPublicas`, estender a exceção que hoje só cobre `settings-globais` para
    cobrir também `'comparativo-led'` — single type não recebe a ação `findOne`. A condição passa a
    excluir os dois nomes antes de empurrar `api::${name}.${name}.findOne` no array de ações.
    Nenhuma ação `create`/`update`/`delete` é adicionada para o role público;
(c) criar `garantirEmPreparacao(strapi: Core.Strapi)` copiando a estrutura de
    `garantirContagemSolicitacoes`: `strapi.documents('api::category.category').findMany({ locale:
    DEFAULT_LOCALE, status: 'draft', pagination: { pageSize: 100 } })`, pular registros em que
    `emPreparacao` não é `null`/`undefined`, e para os demais chamar `update` gravando
    `{ emPreparacao: false }` seguido de `publish`, contando quantas foram corrigidas e logando
    `[seed] emPreparacao: N categoria(s) inicializada(s)`. Envolver em `try/catch` que loga com
    `strapi.log.error`, igual ao molde;
(d) registrar a chamada de `garantirEmPreparacao` dentro de `bootstrap()`, na mesma lista sequencial
    das outras funções de seed, DEPOIS de `seedEstrutura` (as categorias precisam existir).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=JSON.parse(fs.readFileSync('cms/src/api/category/content-types/category/schema.json','utf8'));if(c.attributes.subcategorias.type!=='relation')throw new Error('subcategorias ainda e component');if(c.attributes.subcategorias.target!=='api::subcategoria.subcategoria')throw new Error('target');if(c.attributes.aplicacoes.component!=='shared.aplicacao')throw new Error('aplicacoes');if(c.attributes.emPreparacao.type!=='boolean')throw new Error('emPreparacao');const p=JSON.parse(fs.readFileSync('cms/src/api/product/content-types/product/schema.json','utf8'));if(p.attributes.subcategorias.relation!=='manyToMany')throw new Error('product.subcategorias');if(p.attributes.subcategorias.inversedBy!=='produtos')throw new Error('inversedBy');const i=fs.readFileSync('cms/src/index.ts','utf8').split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');['\'subcategoria\'','\'comparativo-led\'','garantirEmPreparacao'].forEach(t=>{if(!i.includes(t))throw new Error('index.ts sem '+t)});if((i.match(/garantirEmPreparacao/g)||[]).length<2)throw new Error('garantirEmPreparacao definida mas nao chamada');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `category.attributes.subcategorias.type === "relation"` com `relation === "oneToMany"`, `target === "api::subcategoria.subcategoria"`, `mappedBy === "categoria"`
    - `category.attributes.aplicacoes` é component repetível `shared.aplicacao`
    - `category.attributes.emPreparacao.type === "boolean"` com `default: false`
    - Os atributos pré-existentes de `category` (`nome`, `slug`, `descricao`, `hero`, `ordem`, `produtos`, `seo`) continuam presentes e inalterados
    - `product.attributes.subcategorias.relation === "manyToMany"` com `inversedBy === "produtos"`, e `product.attributes.tiposDeEvento` permanece inalterado
    - `cms/src/index.ts` contém `'subcategoria'` e `'comparativo-led'` dentro do array `PUBLIC_READ` (fora de comentário)
    - A linha que empurra `findOne` em `garantirPermissoesPublicas` exclui tanto `settings-globais` quanto `comparativo-led`
    - `garantirEmPreparacao` aparece ao menos 2 vezes em `cms/src/index.ts` (definição + chamada em `bootstrap`)
    - Nenhuma ação além de `find`/`findOne` é adicionada ao role público
  </acceptance_criteria>
  <done>`category` e `product` refletem os 4 modelos novos, `PUBLIC_READ` cobre os 2 content-types novos com `findOne` suprimido para o single type, e o backfill de `emPreparacao` está registrado no bootstrap.</done>
</task>

<task type="auto">
  <name>Task 3: Remover o componente órfão, subir o Strapi local e registrar as decisões de órfãos</name>
  <files>cms/src/components/shared/subcategoria.json, docs/divergencias.md</files>
  <read_first>
    - cms/src/components/shared/subcategoria.json (o arquivo a remover)
    - cms/src/components/blocos/comparativo-led.json (o segundo órfão, que NÃO é removido)
    - docs/divergencias.md (formato e numeração das divergências já registradas)
    - docker-compose.yml na raiz do repositório (nome do serviço do CMS e como subi-lo localmente)
  </read_first>
  <action>
Apagar `cms/src/components/shared/subcategoria.json` — nenhum schema o referencia depois da Task 2.

Subir o Strapi local (serviço do CMS do `docker-compose.yml`) e observar o log de boot até o Strapi
ficar pronto. O boot precisa criar as tabelas `subcategorias`, `comparativo_led`,
`components_shared_aplicacoes` e `components_shared_linhas_comparativo` sem erro e sem mensagem
sobre componente ausente.

**Contingência especificada** (não é decisão a tomar no momento): se e somente se o boot falhar com
erro citando `shared.subcategoria` ausente, restaurar o arquivo `cms/src/components/shared/
subcategoria.json` via `git checkout -- cms/src/components/shared/subcategoria.json`, manter
`category.subcategorias` como relação (a mudança da Task 2 permanece), subir de novo, e registrar
em `docs/divergencias.md` que o componente ficou no repositório sem uso por limitação de migração
do Strapi.

Acrescentar a `docs/divergencias.md` uma entrada nova, seguindo a numeração e o formato já usados
no arquivo, cobrindo os dois órfãos: (1) `shared.subcategoria` removido porque `subcategoria` virou
content-type (ou mantido sem uso, se a contingência disparou); (2) `blocos.comparativo-led`
DELIBERADAMENTE mantido e não reusado — D-08 escolheu single type dedicado justamente para impedir
que o comparativo seja colado em qualquer `page` da Dynamic Zone; remover o bloco exigiria migração
de Dynamic Zone e mudança da união Zod `blocoSchema` em `src/lib/cms/schemas.ts`, sem ganho para
CATG-03. Datar a entrada em 2026-08-22 e citar D-08/D-09.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(fs.existsSync('cms/src/components/blocos/comparativo-led.json')===false)throw new Error('blocos/comparativo-led.json NAO deve ser removido');const d=fs.readFileSync('docs/divergencias.md','utf8');if(!/comparativo-led/.test(d))throw new Error('divergencias.md sem registro do orfao blocos.comparativo-led');if(!/shared\.subcategoria|shared\/subcategoria/.test(d))throw new Error('divergencias.md sem registro do orfao shared.subcategoria');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cms/src/components/blocos/comparativo-led.json` continua existindo e byte-idêntico ao estado anterior (`git diff --stat` não lista o arquivo)
    - `src/lib/cms/schemas.ts` não foi tocado neste plano (`git diff --name-only` não o lista)
    - O boot do Strapi local termina com o servidor pronto, sem `error` no log referente a componente ausente ou a relação inválida
    - O log de boot contém a linha `[seed] emPreparacao:` com uma contagem numérica
    - `docs/divergencias.md` contém uma entrada datada de 2026-08-22 citando explicitamente `blocos.comparativo-led` como mantido e não reusado, e o destino de `shared.subcategoria`
    - Se a contingência disparou, `cms/src/components/shared/subcategoria.json` existe e a divergência registra o motivo; caso contrário o arquivo não existe mais
  </acceptance_criteria>
  <done>Strapi local sobe limpo com os 4 modelos, o segundo órfão permanece intacto por decisão registrada, e `docs/divergencias.md` documenta as duas decisões.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → Strapi REST público | Leitor anônimo acessa `/api/subcategorias` e `/api/comparativo-led` |
| bootstrap do container → Postgres | Código de boot grava direto em registros de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | `garantirPermissoesPublicas` em `cms/src/index.ts` | mitigate | Só `find`/`findOne` entram em `PUBLIC_READ`; nenhuma ação de escrita é concedida ao role `public`, e a configuração nunca é feita à mão no admin — o bootstrap é a única fonte de verdade |
| T-06-02 | Information Disclosure | single type `comparativo-led` | mitigate | `findOne` suprimido para o single type (mesma exceção de `settings-globais`); `draftAndPublish: false` elimina a existência de versão rascunho a vazar |
| T-06-03 | Tampering | `garantirEmPreparacao` gravando em categorias de produção | mitigate | Guarda de idempotência: só grava quando `emPreparacao` é `null`/`undefined`, nunca sobrescreve valor definido por editor |
| T-06-04 | Denial of Service | boot do Strapi com schema inválido | mitigate | Task 3 sobe o Strapi local antes de qualquer deploy; a contingência de restauração do componente está pré-especificada |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova é instalada nesta fase (RESEARCH §Package Legitimacy Audit) — nenhum pacote a auditar |
</threat_model>

<verification>
- `docker compose` do CMS sobe e o log não tem `error`
- As 4 tabelas novas existem no Postgres local
- `git diff --name-only` não lista `src/lib/cms/schemas.ts` nem `cms/src/components/blocos/comparativo-led.json`
</verification>

<success_criteria>
Os 4 modelos de D-05/D-01/D-09/D-08 existem no schema do Strapi, o Strapi local sobe limpo, o
backfill de `emPreparacao` roda no boot, e as duas decisões de órfão estão registradas em
`docs/divergencias.md`.
</success_criteria>

<output>
Create `.planning/phases/06-categoria/06-01-SUMMARY.md` when done
</output>
